# fx-entry-checklist プロジェクト

FX Entry Checklist（FXエントリーチェックリスト＆トレード記録ツール）

## プロジェクト概要

- **ファイル**: `index.html` （アプリ本体は単一ファイル、CDN依存なし、完全オフライン動作）
  - S28 で PWA 用に `manifest.webmanifest` / `sw.js` / `assets/icon-*.png` が加わったが、**アプリのロジックは index.html に閉じている**（`sw.js` はキャッシュ制御のみ、アプリコード0行）
- **技術スタック**: HTML5 + CSS3 + Vanilla JavaScript
- **データ永続化**: localStorage (キー: `mochipoyo_trades_v1`, `mochipoyo_market_view_v1`, `mochipoyo_theme` ほか)
- **同期**: Supabase（SDK なし・fetch のみ）。localStorage が常に「正」でクラウドは複製（S28）
- **独立性**: FX Trade Tracker とは完全に分離（別の localStorage キーを使用）

⚠️ **`index.html` を更新したら `sw.js` の `CACHE` の版番号を上げること。**
上げ忘れても HTML は network-first なので最新が届くが、`assets/` の差し替えは届かない。

## ユースケース

FX トレーダーが 🔭トレンド一覧タブで全監視通貨ペア（28銘柄）を毎日巡回し、各ペアの上位足トレンド方向・EMAゾーン・グランビルの波を行内タップで記録 → 「エントリー圏内」の足の 🎯「根拠」ボタンをタップすると根拠パネルが開き、根拠16項目（上位足7+エントリー足3ステップ確認フロー）を選択・判定（エントリー/保留/スルー）を決定。**すべて同じ一覧タブ内で完結**し、「📝 記録フォームへ」をタップするとモーダルとしてトレード記録フォームが浮き出す（タブ遷移なし） → フォームでアラート発生日時・結果タグ（REG/BIG/MAX/微益/建値決済/損切）・円建て損益・建値タッチ・チャート画像を入力して保存。モーダルが自動で閉じ一覧タブへ戻る → 📚振り返りタブで結果タグ別・建値タッチ別の成績分析から実力を検証。 アラート〜エントリー経過時間の統計でタイミングを改善

## 機能リスト

### 1. ~~エントリーチェックリスト（15項目）~~ → **セッション21で廃止**
環境認識ボードに根拠チェック（グランビル/RCI/MACD/ラウンドナンバー/ロールリバーサル）を実装した結果内容が重複したため、**セクションごと削除**。関連する進捗バー・NG警告モーダル・「チェック遵守率」統計タイル・履歴のNGバッジ・CSV `check1〜15` 列もすべて撤去した。既存記録の `checks` は `loadTrades()` のマイグレーションで破棄される。

📖解説ドロワーへのジャンプリンク（グランビル・MACD）はチェックリスト内にあったため、**環境認識ボードの該当チェック行のラベルへ移設**（`MV_TF_CHECKS` の `link` プロパティ）。

### 2. エントリーパターン（セッション21で自動導出に変更）
固定4択の入力欄は廃止。環境認識ボードの `tfEntry + '×' + tfHigher` から自動生成される（例：「15分足×日足」）。ボードは上位足4種×エントリー足4種=16通りを表現できるため、旧4択より表現力が高い。CSV の `entryPattern` 列は互換のため維持。

### 3. トレード記録フォーム（セッション21で大幅スリム化、S54でRR/建玉方式から結果タグ・円建て損益方式に全面置換）
**現在の入力項目:**
- **必須**: 区分（検証/リアル）、エントリー日時
- **条件付き必須**: 売買方向（ボードの判定が「エントリー」のときのみ表示・必須）
- **常時表示（任意）**: アラート発生日時（`manualAlertAt`）・アラート発生時間足（`manualAlertTf`、単一select）。🔭一覧のアラート機能（`alertTf`/`alertPair`、ボード由来のスナップショット）とは完全に独立したフォーム内項目
- **判定=エントリーのときのみ表示（任意）**:
  - **結果**（`resultTag`）: REG/BIG/MAX/微益/建値決済/損切 の6ボタン単一選択（`.result-toggle`/`.result-btn` パターン）＋別枠の「その他」プルダウン（`fResultOther`、選ぶと理由が `resultOtherReason` に入る）。ボタンとプルダウンは排他
  - **損益（円）**（`pnlAmount`）: `+`/`-` 符号切替ボタン付きの数値入力
  - **建値タッチ**（`beTouch`）: 無し/タッチ有/2回タッチで決済 の3ボタン単一選択（同じく `.result-toggle` パターン）
  - **チャート画像**（`images.entry`/`images.higher`/`images.others[]`）: エントリー足・上位足の単発アップロード欄＋その他の複数アップロード欄。Firebase Storageへ**fetchのみ**（SDK不使用）でアップロードし、ダウンロードURLを保存。Firebase未設定でも画像なしで保存可能（必須ではない）
- **任意**: エグジット日時、振り返り・備考

**環境認識ボードから自動で引き継がれる項目（フォームに入力欄なし）:**
通貨ペア / アラート発生通貨ペア / アラート発生時間足（複数足を「 / 」区切りで結合、ボード由来の `alertTf`。上記の手入力 `manualAlertAt`/`manualAlertTf` とは別名で衝突しない）/ 上位足 / エントリー足 / エントリーパターン / 判定 / 根拠チェックのスナップショット（`mvChecks`）/ `watchId`

フォーム冒頭の `#formMarketSummary` にこれらが読み取り専用で表示される。ボード未選択や上位足・エントリー足・判定が未設定の場合は警告を出して保存ボタンを `disabled` にする。

⚠️ **編集時はボード由来の項目を保存済み記録から復元し、当時の値を凍結する**（`pickMarketFields()`）。今日のボード状態で過去の記録が上書きされるのを防ぐための最重要仕様。そのため編集で判定やペアは変更できない。アラート・結果・損益・建値タッチ・画像はボード由来ではないのでこの凍結の対象外（`loadTradeIntoForm()` で個別に復元）。

- ※ セッション3変更：スタイル・エントリー価格・決済価格・損切り価格・pips は削除
- ※ セッション6追加：区分、アラート発生フィールド、エントリー/スルー判定
- ※ セッション13追加：判定に「保留」を追加（エントリー/保留/スルーの3択）
- ※ セッション21変更：重複7項目を削除し、環境認識ボードを唯一の入力元に統一
- ※ セッション22変更：建玉ブロック（エントリー価格/損切り価格/損切り根拠・計画ターゲット表示）を追加
- ※ **セッション54で建玉ブロック・決済モーダル・RR計算を全廃**し、アラート日時／結果タグ／円建て損益／建値タッチ／チャート画像の入力に完全置換（詳細は下記「8. トレード結果の記録（旧：3分割エントリーと決済記録）」参照）

### 4. 履歴一覧（セッション11で編集機能追加、S54で決済モーダル関連表示を全廃）
- 新しい順のカード表示、方向別色分け
- ※ セッション21変更：NGバッジ・チェック数バッジを削除し、代わりに**根拠サマリー行**（`.record-basis`）を追加。`mvChecks` から `❌`・未選択を除いた項目だけを上位足/エントリー足に分けて表示する
- ※ セッション6追加：区分フィルタ（全体/検証/リアル）、区分バッジ、判定バッジ、アラート情報表示
- ※ セッション11追加：**「✎ 編集」ボタン**で記録フォーム全フィールドを復元・編集・更新保存可能
- ※ **セッション54で「💰 決済」ボタン・RR表示・分割決済サマリー・損切り根拠行をすべて撤去**。代わりにアラート発生日時・時間足、結果タグバッジ、損益（円、黒字/赤字で色分け）、建値タッチのラベル、添付チャート画像のサムネイル（クリックで別タブ表示）を表示

### 5. 統計サマリー（タブ化、セッション10更新、S54でRRベースから円建て損益ベースに全面刷新）
- 下部タブバー「📊統計」として独立
- トレード数、エントリー数、保留数、スルー数を表示（※セッション21で「チェック遵守率」タイルを削除）
- ※ セッション3変更：勝率・合計pips・平均pips は削除（pips入力欄廃止に伴い）
- ※ セッション6変更：区分フィルタ（全体/検証/リアル）を追加、エントリー数/スルー数タイルを追加
- ※ セッション13追加：**保留数**タイルを追加
- ※ セッション39追加：**「エントリーFio水準別の成績」**（23%/38%/50%/61%/78%ごとの件数・勝率・平均）。S54で平均RR表示を平均損益（円）表示に置換
- ※ **セッション54で平均RR・勝率（RR基準）・最大到達RR・枚目別の成績・損切り根拠別の成績のタイルをすべて撤去**し、以下に置換：
  - **合計損益（円）**／**平均損益（円）**タイル（母数は `resultTag` が設定済みのトレード）
  - **勝率**（`pnlAmount > 0` の割合、母数は同上）
  - **「結果タグ別の成績」**（REG/BIG/MAX/微益/建値決済/損切/その他 ごとの件数・平均損益ランキング）
  - **「建値タッチ別の成績」**（無し/タッチ有/2回タッチで決済 ごとの件数・平均損益）
  - 「エントリーFibo水準別の成績」（`fiboStats`）・「アラート発生足別の判定統計」（`mvAlertStats`）は無関係のため無変更（円建て損益ベースに更新のみ）

### 6. 追加機能
- CSV エクスポート/インポート（BOM付きUTF-8、id重複検出、互換性あり）
- ダーク/ライト切替（localStorage保存）
- タブベースUI（3タブ：🎯トレード / 📚振り返り / ⚙設定）、画面下部固定ナビ。📖解説は右スライドイン式ドロワーに独立（画面を邪魔しない）

### 7. 環境認識ボード（セッション12で追加、S13で構造刷新、S14で直接編集化、S17で単一ペア選択式に再設計、S19で上位足・エントリー足プルダウン選択に変更、S20で根拠チェック項目を追加、**S51で🎯タブから完全廃止**）

⚠️ **S51でこの節の内容（🎯トレードタブのStep①UI）は削除済み。** S33〜S48で🔭一覧の根拠パネル（「12. 🔭一覧の根拠パネル」参照）が同じ5フィールド（`tfHigher`/`tfEntry`/`judge`/`checksHigher`/`checksEntry`）を書ける唯一の入力元になっていたため、UIの二重管理を解消する目的で撤去した。**データモデル（`mochipoyo_market_view_v1` の該当フィールド）・端末間マージ・CSV・統計は無変更**。ペア削除（旧「このペアのデータを削除」）は🔭一覧の各行の🗑ボタンに統合。以下は歴史的経緯として残す。

**旧設計（〜S50、セッション20時点）:** ツールバーの「通貨ペア選択」ドロップダウンで1つペアを選ぶ → カード上に「上位足」「エントリー足」の2つのプルダウンが表示され、ユーザーが自分の監視スタイル（例：日足×15分足）を選択・即保存。各時間足の下には「グランビル/RCI/MACD/ラウンドナンバー/ロールリバーサル」の根拠チェック行が並び、ユーザーが確認した内容をその場で記録できる。選択内容・チェック値はペアごとに記憶され、ペア切り替え時に復元される。

**設計の進化:**
- **S12〜13**: ペア＋時間足ごとの複数カード一覧表示（スクロール確認）
- **S14**: モーダルなしの直接クリック編集に変更
- **S17**: 複数カード一覧 → 単一ペア選択式に変更（コンパクト化）
- **S18**: アラート発生足を単一選択 → **複数時間足（1H/4H/D/W）同時トグル式**に変更、発生日時を足ごとに記録し、判定と紐付けて統計化
- **S19**: 週足/日足/4時間足「固定3行トグル」 → **「上位足プルダウン」「エントリー足プルダウン」に変更**（複数監視スタイルに対応）
- **S20**: **上位足・エントリー足ごとに根拠チェック行を追加**（グランビル/RCI短期中期長期/MACD/ラウンドナンバー/ロールリバーサル、各時間足で独立）

#### 機能詳細
- **ペア選択（S25で分類表示化）**: ツールバーの「通貨ペア選択」ドロップダウンでペアを選択。プリセット28ペアは 5 つのグループ（株価先物・仮想通貨・金銀原油・USD系・その他通貨）に分類され、プルダウンは `<optgroup>` で表示。存在しなければ自動作成、存在すれば前回選択した上位足・エントリー足が復元される。選択中のペアID は localStorage（`mochipoyo_market_view_selected`）に保存
- **カスタムペア**: 「その他（自由入力）」を選ぶと入力欄が表示。入力値を確定すると登録・選択される
- **全銘柄の自動生成（S25新規）**: 🔭一覧タブがボード選択の履歴に依存せずプリセット全28銘柄を巡回対象とするため、`loadMarket()` がストレージ読み込み時に未作成ペアを自動生成。ボード側の `selectPair()` 挙動には影響しない（ボードは従来通り1ペアずつ選択・記録）
- **上位足・エントリー足の選択（セッション19で新規）**: カード上の2つのプルダウン
  - **上位足**: 週足 / 日足 / 4時間足 / 1時間足 から選択
  - **エントリー足**: 5分足 / 15分足 / 1時間足 / 4時間足 から選択
  - 選択するとペア属性（`tfHigher` / `tfEntry`）として即保存
- **根拠チェック（セッション20で新規、S37で RCI/MACD 選択肢変更、S38でエントリーFibo追加、S46で「待ち」追加）**: 各時間足のプルダウン下に8行が並ぶ。上位足とエントリー足で独立した値を保持
  - **グランビル**: 1 / 2 / ❌
  - **RCI**: 短期/中期/長期（各：上限 / 60↑ / 60↓ / 下限 / ⏳待ち / ❌）を3本独立で記録
  - **MACD**: RD(転換) / HD(継続) / ⏳RD待ち / ⏳HD待ち / ❌（S37: 表示名変更。S46: 「待ち」を追加）
  - **ラウンドナンバー**: 有 / 無 / ❌
  - **ロールリバーサル**: 確認 / 未確認 / ❌
  - **エントリーFibo（S38）**: 23% / 38% / 50% / 61% / 78% / ❌ — エントリー足の1波のどのフィボナッチ水準でエントリーしたかを記録。小数点は省略
  - 選択値は ペア属性（`checksHigher` / `checksEntry`）として即保存。同じボタンを再タップで未選択に戻せる。`MV_TF_CHECKS` 配列に1行足すだけで新項目が両時間足に自動反映される（拡張性）
- **アラート発生足**: カード上に「1H / 4H / D / W」の4つのバッジが常時表示。それぞれ独立にON/OFFをタップ切替、ONにした瞬間の日時を記録
- **判定3択**: 「✅エントリー / ⏸保留 / ⏭スルー」のトグル。判定を新たに設定するたびに `judgeLog` に記録（統計の元データ）
- **チェックリストへのジャンプボタン**: 「📝 チェックリストへ」ボタンで🎯トレード内のチェックセクションにスムーススクロール移動
- **データ独立性**: 専用キー `mochipoyo_market_view_v1` (ペア・S19で `tfHigher` / `tfEntry` 追加・`judgeLog`) + `mochipoyo_market_view_selected` (選択中ペアID) に保存。トレード記録（`mochipoyo_trades_v1`）とCSVには一切影響しない
- **アラート発生足別の判定統計（S18で追加）**: 📚振り返りタブの統計サマリー内に「アラート発生足別の判定統計」セクションを追加。`judgeLog` を時間足ごとに集計し、各時間足で発生したアラート後の ✅エントリー/⏸保留/⏭スルー 件数を表示（同時に複数の足がONだった場合はそれぞれの足のカウントに重複加算）
- **自動マイグレーション**: 
  - S12形式（pairs が文字列配列）→ S14形式（オブジェクト配列＋id・alertTf・judge）：既存スナップショット・履歴保持、watchId 付け替え
  - S14形式（ペア＋時間足ごとの複数カード）→ S17形式（ペア単位）：同名ペアが複数あれば統合、スナップショット履歴は失われない
  - S17形式（単一 alertTf 文字列）→ S18形式（`alerts.{h1,h4,d,w}` オブジェクト）：一致する時間足があれば ON として引き継ぐ（発生日時は不明なため空）
- **拡張性**: `MV_TFS` に1行足せば記録する時間足を追加でき、カード・履歴すべてに自動反映。アラート発生足は `MV_ALERT_TFS` で管理
- ⚠️ **S12〜14で削除された機能**: 記録モーダル、時間足ごとのメモ入力欄、AI分析コピペ連携（プロンプト自動生成・コピー機能）。過去の設計は `docs/CHANGELOG_ARCHIVE.md` を参照

### 8. トレード結果の記録（旧：3分割エントリーと決済記録、セッション22で追加 → **S54で全廃・結果タグ＋円建て損益方式に完全置換**）

⚠️ **この節が扱っていた仕組み（建玉ブロック・3分割決済モーダル・RR計算・合成RR・枚目別/損切り根拠別の成績）はS54で完全に削除された。** ユーザーの実運用フローが変わり、価格ベースのRR計算ではなく結果タグ（REG/BIG/MAX/微益/建値決済/損切/その他）と円建ての損益額で記録する方式に置き換えたいという明示的な指示による。以下は歴史的経緯として残す。詳細は「3. トレード記録フォーム」の現行仕様を参照。

**旧・実運用ルール（〜S53、参考）**:
- エントリーは常に3分割（例：3万円リスクなら1万円×3）。3枚とも同一価格・同時エントリー
- 各枚の計画利確は 2:1 / 3:1 / 4:1。ただし相場次第で RCI上限・下限／直近高値／ラウンドナンバーで早期利確することもある
- 1枚目を利確したら残り2枚の損切りを建値に上げる（建値で戻されたら RR 0.00 → `breakeven` 根拠で記録）
- 損切り位置は 直近安値/高値をカバー か 直近の波のフィボナッチ161%をカバー

**旧データモデル（S54で削除）**: `entryPrice`/`slPrice`/`slBasis`/`splitCount`/`exits[]`（`slot`付き=実玉・`slot`なし=参考到達ポイント）、合成RR＝3枚のRRの平均（`tradeNetRR()`）。旧・削除済み関数：`calcRR()`/`fmtRR()`/`tradeExits()`/`isSplitTrade()`/`slotExits()`/`tradeNetRR()`/`planTargetPrices()`/`decimals()`、決済モーダル関連の `openExitModal()`/`renderExitAll/Risk/Slots/Rows()`/`exitRowInner()`/`renderExitSum()`/`fillRemainingSlots()`一式。

**S54の新方式（現行）:**
- トレードに `manualAlertAt`/`manualAlertTf`（フォーム内独立のアラート記録）、`resultTag`（結果タグ）、`resultOtherReason`（その他の理由）、`pnlAmount`（円、符号付き）、`beTouch`（建値タッチ）、`images.{entry,higher,others[]}`（Firebase StorageのURL）を追加
- `resultTag` は `''|'reg'|'big'|'max'|'small_profit'|'breakeven'|'stoploss'|'other'` の単一値。`other` のときのみ `resultOtherReason` を使う
- 画像アップロードは Firebase Storage の REST API（`POST https://firebasestorage.googleapis.com/v0/b/{bucket}/o?uploadType=media&name={path}`）へ**fetchのみ**で行う（`uploadChartImage()`）。**単一ファイル・CDN依存なしの原則を守るため Firebase JS SDK は使わない**。bucket・API Keyは⚙設定タブの「🖼 チャート画像（Firebase Storage）」セクションで入力し `mochipoyo_firebase_config_v1` に保存
- 統計タブは「合計損益」「平均損益」「勝率（`pnlAmount > 0`）」「結果タグ別の成績」「建値タッチ別の成績」に置換（詳細は「5. 統計サマリー」参照）
- 旧CSV列（`entryPrice`/`slPrice`/`slBasis`/`splitCount`/`exits`）は完全撤去し、新列（`manualAlertAt`/`manualAlertTf`/`resultTag`/`resultOtherReason`/`pnlAmount`/`beTouch`/`imgEntry`/`imgHigher`/`imgOthers`）に置換（ユーザー確認済みの完全置換方針。旧建玉・決済データは移行されない）

### 9. 🔭 トレンド一覧タブ（セッション23で追加、**S24でチャート巡回ツールに作り直し**）

**ユースケース（S24で明確化、S25で銘柄拡張）**: 手法がトレンドフォローなので、トレードの最初の工程は**「トレンドが発生している通貨を探すチャート巡回」**。監視28ペア（S25で仮想通貨6種を追加）を**1日約4回**見るが、都度長い時間はかけられない。**チャートを一目見て記録し、次へ**を最優先に設計されている。

巡回で記録するのは上位足ごとに3点：
1. **トレンド方向** — ↗上昇 / ↘下降 / ─レンジ
2. **EMA ZONE の色** — 緑 / 赤 / グレー
3. **グランビルの波** — 買①〜④ / 売①〜④

**S23からの変更点（なぜ作り直したか）**: S23 は「トレンド方向を並べて比べる」画面で、入力はアコーディオン展開が必要だった。22ペア × (展開→入力→閉じる) は1日4回の巡回に耐えず、EMAゾーンは記録すらできなかった。S24 で**アコーディオンを全廃し、行内で全項目をタップ**する形に変更。

**データモデル**:
```js
w.trend = { d: {state, zone, granville}, h4: {...}, w: {...}, mn: {...} }
// state: '' | 'up' | 'down' | 'range'
// zone:  '' | 'green' | 'red' | 'gray'      ← S24追加（EMAゾーンの色）
// granville: '' | '1'…'8'（1〜4=買い / 5〜8=売り）
w.trendAt = ISO文字列   // ← S24追加。ペア単位の最終更新時刻（鮮度表示用）
```
⚠️ **既存の `checksHigher.granville` とは完全に別データ**。ボードの「上位足」はユーザーが選んだ任意の1つ（週足〜1時間足）なのに対し、一覧は1時間足・4時間足・日足（＋任意で週足・月足）を固定枠で持つため意味が異なる。**CSV・トレード記録・統計には一切影響しない**（`CSV_HEADERS` に `trend` は入らない）。

**時間足（S24、S57で1時間足を追加）**: `MV_TREND_TFS` は1時間足・4時間足・日足・週足・月足の5本（表示順もこの並び）。うち `optional: true` の**週足・月足は既定で非表示**で、ツールバーのトグルで出す。表示設定は市場データではなく専用キー `mochipoyo_trend_tfs_v1` に持つ（端末ごとの表示設定にすぎないため）。

**並び順＝選択した時間足のアラート発生時刻順（S67、S44のエントリー圏距離順を置き換え）** — ツールバーの1H/4H/D/Wボタン（`trendSortTf`）で選んだ足の`w.alerts[tf].at`が新しい順。カード上のアラートバッジをタップした瞬間にもこの基準足へ切り替わる（`mvToggleAlert()`）。アラート未発生のペアは末尾に沈む。旧「エントリー圏までの距離」（`pairEntryDistance()`の昇順、S44でS32のGO/WAIT/NO判定を置き換えたもの）は並び順としては撤去したが、関数自体は波マップの圏内強調・🎯根拠ボタンの色分けで使い続けている。詳細は下記「11. エントリー圏で並べる」を参照（並び順以外の用途は無変更）。

⚠️ **`pairEntryDistance()`（波マップ・🎯チップが見る圏内判定）が見るのは `MV_TREND_DEFAULT_TFS`（1時間足・4時間足・日足、S57で1時間足を追加）だけ**。週足・月足は表示トグルで出入りするため、主軸に入れると「表示を切り替えただけで圏内判定が飛ぶ」。

**UI（S24、S44で判定バッジを撤去、S58で目線3択→手動GOに置換）**: 1ペア = **ヘッダ1行＋可視時間足ごとの入力行**の2バンド構成。
- **ヘッダ**: ペア名 / **🚩 GOボタン（S58）** / 鮮度チップ / アラートバッジ4つ / 🗑（巡回記録クリア）/ `→`（ボードへ）
  - ※ セッション33で判定ボタンはヘッダから根拠パネルの最下部へ移設済み
  - ※ セッション44でセットアップ判定バッジを撤去（375px で3段になっていた問題もここで解消）
  - ※ セッション58で目線3択（📈/〰/🔄）を撤去し、同じ位置に手動GOボタンを新設
- **入力行**: 足ラベル / 方向3ボタン（↗↘─）/ ゾーン3ドット（緑赤灰）/ グランビルチップ / **🎯 エントリー圏チップ兼「根拠」ボタン（S44・圏内の足にだけ出る）**

**手動GOフラグ（S58、目線3択を置換）**: ペア名の直後の `GO` ボタン1つ。**`pairEntryDistance()` の自動距離とは完全に独立した主観の優先度タグ**で、巡回中に「これは近々来る」と判断したペアへ1タップで印を付ける。ONのペアは一覧の最上位「🚩 GO」グループにまとまり、**再タップで解除するまで自動では消えない**（`mvClearTrend()` の🧹巡回記録クリアの対象外＝クリアしてもGOは残る）。書き込み口は `mvSetGo()`。ツールバーの「🚩 GOのみ」ボタンで絞り込める（`trendGoOnly`、他の絞り込みとAND条件）。
- ⚠️ **旧「目線3択」（S35、📈トレンドフォロー/〰レンジ/🔄RD逆張り）はS58で完全撤去**。S44で自動判定から切り離されて以降は絞り込みタグの役目しか無く、ユーザー要望によりGOフラグに置き換えた。旧データ（`w.mode`/`w.modeAt`）は読み書きしないだけでlocalStorageには残るが無害
- データは `w.go`（真偽値）/ `w.goAt`（端末間マージ用タイムスタンプ）。`mergePair()` で `judgeAt` と同じ形の last-write-wins マージ

**グランビル・ピッカー（S44で簡素化）**: チップをタップすると**グランビルの図を表示したモーダル**が開く。**記録経路は図のタップだけ**（「買①〜売④」の8ボタンと「売買8つすべて表示」は S44 で撤去 — タップで自動確定する今は二重入力にしかならないため）。読み取り表示は波番号ではなく**エントリー圏との距離**。方向が未記録・レンジのときはチップが `disabled`。`IMG_GRANVILLE` は**開いたときに初めて代入**して初回描画から外している。

**鮮度**: `w.trendAt` から経過時間を出し、**12h超で `.stale`（警告色）/ 36h超で `.cold`**。1日4回の巡回に対して「1回抜けた／1日抜けた」の目安。ツールバーの「🕐 未更新のみ」で絞り込める。**鮮度でソートはしない**。

⚠️ **最重要仕様1**: アラート・判定の処理本体は **`mvToggleAlert()` / `mvSetJudge()` に切り出してボードと一覧で共有**している。`mvSetJudge()` は `judgeLog` への push を含み、振り返りタブの「アラート発生足別の判定統計」（`renderMvAlertStats()`）がこれを集計する。**どの画面から判定してもこの関数を通すこと**。直接 `w.judge` を書くと統計が壊れる。

⚠️ **最重要仕様2（S24）**: トレンド3項目の書き込みは **`mvWriteTrend()` が唯一の入り口**。`trendAt` の更新と、方向が消えた／反転したときのグランビル自動クリアをここで引き受けている。直接 `w.trend` を書くと鮮度表示と整合性が壊れる。

⚠️ **最重要仕様3（S24）**: タップ時の再描画は **`trendPatchItem()` でその行だけ差し替える**。全体を作り直さないのは、22ペア×最大4足×3コントロールで毎タップ千単位のノードを捨てることになるうえ、**並べ替えが走ると2足目を記録した瞬間に行がグループ間を移動して指の下から消える**ため。グループ分けは次の全描画まで意図的に古いままにし、`setActiveTab('trend')`（タブに戻る）とフィルタ操作で最新化する。

**拡張性**: `MV_TREND_TFS` に1行足せば時間足を追加でき（`optional: true` を付ければ既定非表示）、行・移行処理・集計すべてに自動反映される。ゾーンの選択肢は `MV_TREND_ZONES`、グランビルの文言と売買区分は `MV_GRANVILLE_POSITIONS`（`side` プロパティがピッカーの絞り込みに使われる）。

### 10. データ保護と複数端末同期（セッション28で追加）

**背景**: 「記録が時間経過で初期化される」という症状の原因は2つあった。

1. **iOS Safari の7日間ストレージ削除（ITP）** — ブラウザのタブから開いただけのサイトは、最終アクセスから7日でデータを消される。**ホーム画面に追加された PWA だけが対象外**。これが主犯
2. **`loadMarket()` の catch が空データを返し、それにプリセット自動生成が走って `saveMarket()` が本物のデータを空で上書きしていた** — 一過性の読み込み失敗が恒久的な消滅に変わる構造

なお**保存の呼び忘れは1件も無かった**（調査済み）。

#### 破損検知とキルスイッチ
- `STORAGE_FAILED = { trades, market }` フラグ。破損を検知したら原文を `<key>__corrupt_<ts>` に**1回だけ**退避（`loadMarket` は1描画で十数回呼ばれるため、毎回退避するとストレージを埋め尽くす）
- ⚠️ **キルスイッチは save 側に置いてある**。全書き込み経路が `if (!saveX(data)) return;` の規約に乗っているので、`saveMarket` / `saveTrades` の先頭で止めれば全経路が自動で止まる。**`loadMarket()` の呼び出し元14箇所には一切手を入れていない**（改修箇所を増やすと新たなバグ源になるため）
- `loadMarket()` は「真の初回（`raw` が null/空）」と「保存済みの何かがある」を先に区別する。**この区別が無かったことが原因2の本体**
- 件数が減る保存の直前に `<key>__bak` へ1世代退避（`storageBackupIfShrinking`）。保存自体は止めない（ペア削除など正当な縮小があるため）。設定タブに復元ボタン
- 破損中は常時バナー `#fatalBanner` を出す（設定タブより上位。見逃すと復旧不能なため）

#### PWA 化
- `manifest.webmanifest` / `sw.js` / `assets/icon-192.png` `icon-512.png` `apple-touch-icon.png`
- ⚠️ **参照はすべて相対パス必須**。GitHub Pages は `/fx-entry-checklist/` 配下のサブパス配信なので、絶対パス `/sw.js` は404になる
- SW のキャッシュ戦略：**HTML は network-first**（cache-first にすると「直したのに反映されない」事故）、`assets/*` は cache-first。**別オリジン（Supabase）は素通し**（同期レスポンスがキャッシュから返ると古いデータで上書きされる）
- `navigator.storage.persist()` も呼ぶ（Chrome/Android 向け。iOS は未対応なので PWA 化が唯一の対策）
- 設定タブにホーム画面追加の状態と手順を表示（`renderPwaStatus()`）

#### 全データ JSON バックアップ
- 設定タブの「⬇ 全データJSONエクスポート／⬆ インポート」。**環境認識ボード・トレンド一覧を持ち出せる唯一の手段**（CSV は trades のみ）
- 対象キーは列挙せず接頭辞 `mochipoyo_` で拾う（キーを増やすたびに漏れる事故を構造的に防ぐ）。`BACKUP_SKIP` で退避用の派生キー・**ログイン情報・巻き戻しスナップショット**を除外
- インポート時は現状を `__preimport_<ts>` に自動退避してから置換

#### 端末間マージ（同期の中核）
⚠️ **全体を「後勝ち」にしてはいけない。** 3端末が別々に記録するので、まるごと上書きすると片方の巡回記録が丸ごと消える。**要素キー単位でマージする。**

| 対象 | 突き合わせキー | 比較する時刻 | 備考 |
|---|---|---|---|
| trades | `id` | `updatedAt \|\| createdAt` | 墓標が記録より新しければ削除 |
| pairs | **`pair` 名** | フィールド群ごと | ⚠️ **id は端末ごとに採番されるので使えない**（全ペアが二重になる）。id はローカルのものを保つ |
| `trend[tf]` | **時間足単位** | `trend[tf].at` | ⚠️ ペア単位 `trendAt` にすると「PCで日足・iPhoneで4H」と記録したとき片方が消える。`trendAt` はマージ後に `max(trend[*].at)` で再計算 |
| `alerts[tf]` | 時間足キー | `alerts[tf].chAt` | ⚠️ `at` は OFF で消えるので別途 `chAt` が要る（無いと消したONが復活する） |
| `checksHigher`/`Entry` | チェック項目単位 | 並行マップ `checksAt['checksHigher.granville']` | 値の側の形は変えない（CSV の `mvChecks` がその形を焼き込むため） |
| `judge` / `tfHigher`,`tfEntry` | ペア単位 | `judgeAt` / `tfAt` | |
| `judgeLog` / `snapshots` | `id` で集合和 | `at` 昇順 | 純追記ログなので競合しない。**過去の集計値は変わらない** |

- 削除は `mochipoyo_tombstones_v1`（90日で刈る）。同期は「両方にあるものを残す」方向に働くので、墓標が無いと未同期の端末から復活する
- ⚠️ **「消す」操作は必ず “空の内容＋現在時刻” として書くこと（S81）**。値を消すだけで時刻を打たないと、その要素の時刻が空（＝0）に戻り、他端末に残る古い記録が必ず勝つ。消したはずの記録が同期のたびに復活し、こちらの削除も他端末へ伝わらない。🧹巡回記録クリア（`mvClearTrend`）がこれで壊れていた
- ⚠️ **ペアの🗑削除は「行の消滅」では表現できない（S81）**。プリセット28銘柄は `loadMarket()` が必ず作り直すので、削除直後からローカルにペアが存在する＝`mergePairs` の「ローカルに無いペア」用の墓標チェックを通らない。`mergePair(a, b, dead)` に墓標を渡し、**マージ結果に `mvApplyPairTombstone()` を当てて削除より古い内容を双方から落とす**（`mergeTrades` が墓標を最終結果に当てるのと同じ考え方）。削除より新しい＝消したあとに録り直したものは残す
- ⚠️ **`STORAGE_FAILED` が立っている間は同期を完全停止**（`runSync()` 冒頭）。壊れたローカルでクラウドを潰さないための最重要の安全弁。**ここが破損検知と同期の接続点**
- 送信前に**必ず先にローカルへ確定**させる（順序が逆だと送信成功・手元未反映の食い違いが残る）
- `rev` の楽観ロック。0件更新なら他端末の割り込みとみなし最大3回リトライ

#### Supabase 側（S78で全面置換 — 下の旧記述より優先）
- **ログインなし**。fx-trade-tracker と同じ Supabase プロジェクトを共用し、`SUPABASE_URL`/`SUPABASE_KEY`（anon）を定数で持つ。設定タブの接続先入力・マジックリンク・`syncValidToken` は撤去
- 表は **`trades_checklist`**（`id text pk, data jsonb, updated_at`、RLS無効）。⚠️ **表 `trades` は fx-trade-tracker 用なので絶対に触らない**
- **1記録1行**。行 id の接頭辞：`t:<tradeId>` / `p:<ペア名>`（ペアの `id` は送らない）/ `j:<judgeLogId>` / `s:<snapshotId>` / `tomb`（墓標マップ1行）
- ⚠️ **運ぶ形だけ行に分け、マージは従来の `mergeDoc()`（要素キー単位）のまま**。ペア行を丸ごと後勝ちにすると足単位の記録が消える
- `runSync()`：`GET ?updated_at=gt.<前回取得−10分>`（**差分取得**。起動時・⟳ボタンは全件）→ 受信墓標をローカル墓標に合わせて `mergeDoc` → 変化があればローカル確定・`renderAll()` → `mochipoyo_sync_pushed_v1`（行id→ハッシュ）と比べて**変わった行だけ** `POST Prefer: resolution=merge-duplicates`。受信行のハッシュも記録してエコー送信を防ぐ
- ⚠️ **読み込み間隔は30秒未満にしない**（移植元で6秒間隔→月19.4GBで停止。共用なので fx-trade-tracker も止まる）
- ⚠️ `file://` で開いたときは同期しない（テストが本番の共用表に書き込むのを防ぐ）。テストは `window.__SYNC_ALLOW_FILE = true` で許可し Supabase を route モック（`s78-sync-test.mjs`）
- 以下は S28〜S77 の旧方式（`app_state`）の記述:
- テーブルは **1ユーザー1行の JSONB ドキュメント**（`app_state`）。マージはクライアント側で行うので Postgres 関数不要＝単一HTML方針を守れる
- 認証は**メールのマジックリンク**。anon key は HTML/localStorage に載る前提だが、**RLS があれば anon key 単体では何も読めない**（`auth.uid()` が null でポリシー不成立）
- 接続先（URL・anon key）は**設定タブから入力**して localStorage に保存する。リポジトリに鍵を置かない
- ⚠️ iPhone はメールアプリ内ブラウザで開くと PWA と別ストレージになる → ログイン画面に「Safari で開く」旨を明記済み

#### 同期のきっかけ
起動時 / `visibilitychange`（復帰時・前回から60秒以上） / 書き込み後3秒デバウンス / `online` / 手動ボタン。
`saveMarket` / `saveTrades` を**ラップして dirty を検知**している（全書き込みがこの2つを通る規約なので漏れなく拾える）。`pagehide` の `sendBeacon` は使わない（マージが要るため単純 PUT は危険）。

### 11. エントリー圏で並べる（セッション44 — S32 の GO/WAIT/NO 判定を全廃）

**ユースケース**: S32 で入れた自動判定（🟢GO / 🟡WAIT / ⚪NO / 🔄RD）は、S40〜S43 で「グランビルの図を直接タップして波の位置（`wpos`）を記録する」形が完成したことで役目を終えた。位置を残せば**買②③/売②③（＝実質のチャンス）までの近さ**が連続量で分かるので、3段階の離散ラベルは同じ情報をより粗く言い直しているだけになっていた。判定バッジがヘッダを3段に膨らませていた問題もここで解消。

**新しい軸** — `w` に一切保存しない純粋な導出関数（S32 の原則をそのまま踏襲）:
```js
function pairEntryDistance(w)      // ペアが「チャンスにどれだけ近いか」。無記録は Infinity
function tfEntryDistance(w, tfKey) // 足1本ぶん。行の 🎯 と「根拠」ボタンの出し分けに使う
function pairIsHot(w) / tfIsHot(w, tfKey)   // <= GV_ENTRY_RADIUS
```
中身は `gvEntryDistance()`（S43）＝ `SETUP_GO_WAVES` のアンカーまでの最短 % 距離。

⚠️ **最重要仕様1**: 見るのは **`MV_TREND_DEFAULT_TFS`（1時間足・4時間足・日足、S57で1時間足を追加）だけ**。週足・月足は表示トグルで出入りするので、混ぜると「表示を切り替えただけで並び順が飛ぶ」。行に出る 🎯 チップは足ごとなので週足・月足でも出るが、**ペアの順位は動かさない**。

⚠️ **最重要仕様2**: **概算（ボタンで選んだだけ＝`wpos` が空）は圏内に数えない。** 概算の座標はアンカーそのものなので距離0になり、買②③/売②③を選んだ全件が無条件で最上位に来てしまう（S43 の圏内判定とまったく同じ理由）。

⚠️ **最重要仕様3**: 方向と逆 side の波は `mvWriteTrend()` が自動で消すので、距離を測る側で side を見る必要はない。

**削除したもの**（S32〜S35 の残骸をゼロにすること）: `MV_SETUPS` / `SETUP_SIDE` / `SETUP_STATUSES` / `setupVerdict()` / `pairVerdicts()` / `pairStatus()` / `TREND_GROUPS` / `trendSetupBadgesHtml()` と CSS の `.sbadge` / `.setup-badges`。**`SETUP_GO_WAVES` だけは残す** — `gvEntryDistance()` が圏の中心として参照する唯一の定義。

**残したもの**: `checkConfidence()` / `setupConfidence()`（S38 の確度。根拠パネルの `.tp-conf` で**判定に関係なく常時表示**）、目線3択（判定から切り離した絞り込みタグ、**S58で手動GOフラグに置換され撤去**）。

**表示**:
- 入力行の末尾に **🎯 チップ兼「根拠」ボタン**（`trendEntryChipHtml()` / `.tent` / `.tent-btn`）。圏内の足にだけ出る
  - ⚠️ **チップとボタンは1要素にまとめてある**。375px の入力行に残る余白は 40px 弱しかなく、2要素にすると圏内の行だけ2段に折り返して巡回の縦スクロールが増える。狭い画面では `.tent .d`（％と「根拠」の文字）を CSS で畳み、🎯 と title だけ残す
  - ⚠️ `MV_TREND_KEY_TO_TF` が引けない足（**月足**）はボタンにせず `<span>` で出す。引けないと `tfHigher` に入れる値が無く、パネルが `marketFieldsFromBoard()` の要求を満たせない
- 🗺 波マップのドット色は**中立1色（`.gv-dot.plain`）＋圏内だけ `.hot`**。単一銘柄モードの足別色（`.tf-d` 等）は従来どおり

⚠️ **S67で撤去**: 一覧の並び順（距離の昇順）・グループ見出し「🎯 エントリー圏（n）」・上部サマリーの「🎯 圏内 n ペア」タイル・ツールバーの「🎯 エントリー圏のみ」絞り込み（`trendEntryOnly`、ボタンid `trendFilterAligned`）。並び順は「9. 🔭トレンド一覧タブ」記載のとおりアラート発生時刻順（`trendSortTf`）に置き換わった。`pairEntryDistance()`/`pairIsHot()`自体、および行の🎯チップ・波マップの圏内判定（`.hot`強調）は無変更で残る。

**拡張性**: `SETUP_GO_WAVES` に1要素足せば圏が増え、`GV_ENTRY_RADIUS` 1つで圏の広さが変わる（行の 🎯・波マップの圏の強調表示に同時に効く。S67以降、並び順・サマリー・絞り込みへの影響は無い）。

### 12. 🔭一覧の根拠パネル（セッション33で追加、S44で入口を変更）

### 12. 🔭一覧の根拠パネル（セッション33で追加）

**ユースケース**: 巡回で「このペアは GO」と分かった直後に、🎯ボードへ移って**同じ時間足をもう一度選び直し**、根拠14項目をボタン群で押す必要があった。この二度手間を消し、**環境認識を一覧タブで完結**させる。

**S44: 入口は「圏内の足の 🎯 根拠ボタン」**（S33〜S43 はセットアップバッジだった）。その足の行の下にパネルが開き、**`tfHigher` は押した足から確定**する（`MV_TREND_KEY_TO_TF[tfKey]`）。上位足を選び直させないのが要点。開閉状態は `trendPanel = { id, tfKey }`（旧 `{ id, setupKey }`）。

⚠️ **ボタンが出るのは「図の上でタップして記録した位置がエントリー圏内の足」だけ**。概算・圏外・未記録の行にはボタン自体が無い（押しても意味のないボタンを置かない ── S42-③「タップ不反応」の再演を避ける）。別の足のボタンを押せば上位足がそのまま入れ替わる。

**パネルの構成**（S62 で**縦2セクション**に変更。S33〜S61 は横並び2列だった）:
- **上位足セクション**：見出しに押した足を固定表示（`.tp-fixed`）＋ `MV_TF_CHECKS` の7行
- **エントリー足セクション**：見出しにエントリー足 `<select>`（`w.tfEntry` を初期選択＝前回値）と目線バッジ（`.tp-side`）＋ `MV_ENTRY_CHECKS` の4行
- 各行は「ラベル｜選択肢ボタン群」の2列。先頭の空ボタン `—` が「再タップで解除」に相当
- 一覧が追跡する足（週足・日足・4時間足・1時間足、S57で1時間足を追加）のグランビルは **S29 と同じ判定式で読み取り専用**（S62 以降は上位足のみ。エントリー足はグランビルを持たない）
- フッター：判定3ボタン（`trendJudgeBtnsHtml()` を再利用）＋「📝 記録フォームへ」

⚠️ **横並び2列に戻さないこと（S62）**。上位足とエントリー足は**項目そのものが違う**（7項目 vs 4項目）ので、ラベル列を共有する3列グリッドは成立しない。縦に積むと1項目が横幅をフルに使えるぶん、選択肢の折り返しはむしろ減る（375px 実測で各行1〜2段）。

⚠️ **最重要仕様1**: **スキーマ・CSV・端末間マージ・統計はすべて無変更**。フォームのデータ元 `marketFieldsFromBoard()` が要求するのは `tfHigher` / `tfEntry` / `judge` / `checksHigher` / `checksEntry` の5つだけなので、一覧からこの5つを書ければ目的を達する。書き込み先はボードとまったく同じフィールド。

⚠️ **最重要仕様2**: 「記録フォームへ」の有効化条件 `w.tfHigher && w.tfEntry && w.judge` は **`marketFieldsFromBoard()` の null 判定と同じ式を保つこと**。片方だけ変えるとボタンは押せるのにフォームの保存ボタンが disabled になる。

⚠️ **最重要仕様3**: パネル HTML の生成は **`trendItemHtml()` の中**に置く。`trendPatchItem()` が `.trend-item` を outerHTML ごと差し替えるため、`trendPanel`（開閉状態のモジュール変数）から作り直さないと、方向やゾーンを触った瞬間に入力途中のパネルが消える。**根拠チェックの書き込みでは再描画しない** — 並び順（`pairEntryDistance()`）が読むのは `w.trend` だけなので順位も 🎯 も動かず、選んだ値はボタンの `.on` を直接付け替えて示している。エントリー足の変更だけは再描画する（グランビルのセルが連動⇄選択可で入れ替わるため）。

⚠️ **最重要仕様4**: パネルは**エントリー圏から外れても自動で閉じない**。記録の途中で勝手に畳まれる方が事故なので意図的。

**共有関数化（S33）**: ボードのハンドラから **`mvSetTf()`（上位足・エントリー足）と `mvWriteCheck()`（根拠チェック1項目）を切り出し**、ボードと一覧の両方がこれを通る。`mvToggleAlert()` / `mvSetJudge()` / `mvWriteTrend()` と同じ「書き込み口を1つにする」規約。`mvWriteCheck` は同値を渡すとトグル解除する（ボードのボタン再タップ相当。一覧の空 option はここに現在値を渡して消している）。

**ボードは残す** — 同じフィールドを読み書きするので放置しても自動で整合する。一覧が主動線、ボードは詳細/編集用。※ S44 でボードの `.mv-card-head` からセットアップ判定バッジを撤去した（判定そのものが無くなったため）。

**スコープ境界**: 一覧で完結するのは**環境認識まで**。区分・エントリー日時・建玉は実際のトレード記録なので 🎯フォームに残る。

### 12b. エントリー足の3ステップ確認フロー（セッション62で追加）

**ユースケース**: エントリー足の根拠は S20 以来ずっと上位足と同じ項目（グランビル/RCI×3/MACD/ラウンドナンバー/ロールリバーサル/Fibo）だった。しかし実運用の手順は**上位足の確度が高いことを前提に、5分足で3つのステップを順に確認する**もので、上位足の環境認識項目をもう一度押しても意味を持っていなかった。

**実際の確認手順** — これをそのまま項目にしてある:

| ステップ | 確認すること | 項目（`k`） | 選択肢 |
|---|---|---|---|
| ❶ | 上位足のネックライン付近で、5分足に反転の形が出たか | `necklineForm` | ダブルボトム / 逆三尊（買）・ダブルトップ / 三尊（売）・❌ |
| ❷ | アラートが発生したか（S83） | `maBreak` | ● / ✖ |
| ❸ | グランビルの波にダマシ・ヒゲの発生が無いか（S83） | `rollReversal` | ダマシ / ヒゲ発生 |
| ❸ | 実際に入った注文方法（S82） | `entryOrderType` | 指値 / 逆指値 / ❌ |
| ❸ | 実際に入ったフィボ水準（指値のときだけ表示） | `entryFibo` | 23% / 38% / 50% / 61% / 78% / ❌ |

**S83変更**：ユーザー指摘「エントリー足の②の項目をアラートに変更、選択項目は●か✖／③をグランビルに変更し、選択項目はダマシとヒゲ発生に変更」に対応。❷（旧 `maBreak`「5分足MA抜け」、上抜け/下抜け/❌）を**「アラート」**（●/✖）に、❸（旧 `rollReversal`「ロールリバーサル」、確認/❌）を**「グランビル」**（ダマシ/ヒゲ発生）に置き換えた。キー名 `maBreak`/`rollReversal` は据え置き（CSV列名・`mvSyncEntrySideChecks()` 等のロジックへの影響を避けるため）。両項目とも新オプションに `side`（買い/売り絞り込み）を付けていない＝**方向に依存しない項目になった**ため、上位足の方向を反転・レンジ化しても値は自動で消えなくなった（❶ `necklineForm` は引き続き方向で絞られ、反転で消える）。旧オプション値（上抜け/下抜け/確認）は新しい選択肢と一致しないため画面上「未選択」相当に見えるが、既存データを書き換えるマイグレーションは行っていない（再選択・再タップで上書きされるまでそのまま残る。無害）。CSV列名・列数・確度スコアの分母は無変更（値の中身が変わるだけ）。端末間マージ・波マップ・並び順・エントリー圏の距離計算は無変更。既存s62-test.mjsを新仕様に更新（❷❸が方向非依存になった分岐を含む）し全通過。sw.js: v53→v54

**S82変更**：ユーザー指摘「FIBOの箇所にまず指値か逆指値かの選択をさせて、指値ならFIBOの選択へ、逆指値ならそのままでよい」に対応。`entryFibo` の直前に `entryOrderType`（指値/逆指値/❌）を追加し、**`entryFibo` の行は `entryOrderType === '指値'` のときだけ表示**するようにした（`trendPanelHtml()` の `rowsOf` が条件フィルタ）。逆指値はブレイクに飛び乗るだけでフィボの何%かという概念自体が無いため、選ばせない方が実態に合う。⚠️ **entryOrderType を「指値」以外（逆指値／❌／再タップでの解除）に変えると、`mvWriteCheck()` が隠れた `entryFibo` の値を自動でクリアする**（逆指値で入ったのに古いFibo値だけ残る食い違いを防ぐ）。⚠️ 根拠チェックのボタンは通常 `.on` の付け替えだけで再描画しない規約（§12最重要仕様3）だが、`entryOrderType` の変更だけは例外的にパネルを再描画する（下のFibo行の出現/消滅に影響するため。エントリー足セレクトの変更と同じ扱い）。CSV列は `en_*` が4列→5列（`en_rollReversal` の次に `en_entryOrderType` が入り、`en_entryFibo` はそのまま最後尾）。確度スコアの分母（エントリー足、均等割り）は4項目→5項目に変化。旧CSV（`en_entryOrderType` 列なし）を import しても `entryFibo` 単体は従来どおり復元される（未知の列は無視されるだけ）。端末間マージ・波マップ・並び順・エントリー圏の距離計算は無変更（`checksEntry` は「チェック項目単位」でマージされるので項目が増えても同期コードは無改修）。新設s82-test.mjs 13項目、既存s62-test.mjs/s44-test.mjsを新仕様に更新し全通過。sw.js: v52→v53

**S65変更**：❸「フィボ×ロールリバーサルの重なり」（`fiboRoll`、重なり有/重なり無/❌）を撤去し、**上位足 `MV_TF_CHECKS` にあった `rollReversal`（ロールリバーサル、確認/❌）をエントリー足のこの位置に一本化**した。上位足でのロールリバーサル確認は、実運用ではエントリー足（5分足）での確認と重複していたため、ユーザー指摘により上位足からは削除。キー名は上位足と同じ `rollReversal` を使うが `checksHigher`/`checksEntry` で保存先が分かれるため衝突しない。既存データの `checksHigher.rollReversal`（旧上位足分）・`checksEntry.fiboRoll`（旧エントリー足分）は読み書きされないだけで無害（S58 の `w.mode` と同じ扱い）。CSV列は `hi_*` が6→5列（`hi_rollReversal` 消滅）、`en_*` は4列のまま（`en_fiboRoll` → `en_rollReversal`）。確度スコアの重み配分（上位足）は RCI各15・MACD35・ラウンドナンバー10（合計90を分母として%計算、100固定ではない）。sw.js: v35→v36

定義は **`MV_ENTRY_CHECKS`**（上位足は従来どおり `MV_TF_CHECKS`）。`short` に ❶❷❸ を含めてあるので、パネルではステップ番号がそのままラベルに出る。

**方向による選択肢の絞り込み**: `opts` の要素に `side: 'buy' | 'sell'` を書くと、**上位足の方向（`w.trend[tfKey].state`）に合う選択肢だけ**が出る（`mvEntrySide()` → `trendCheckCell()`）。↗の押し目狙いで「ダブルトップ」を選べても意味がないため。方向未記録・レンジのときは両サイド出す（選べる手が無くなる方が事故）。目線は見出しの `.tp-side` バッジに常時表示する。

⚠️ **最重要仕様1**: **向きに紐づく記録の後始末は `mvSyncEntrySideChecks()` に集約し、`mvWriteTrend()` と `mvSetTf()`（`tfHigher` のとき）から呼ぶ**。方向と上位足が変わる経路はこの2つだけなので、ここに置けば「↗で記録したダブルボトムが↘に変えても残る」取りこぼしが構造的に起きない。**グランビルと `wpos` の後始末を `mvWriteTrend()` の1箇所に寄せてあるのと同じ理由**（§13 最重要仕様3）。向きを持たない❸（`rollReversal` / `entryFibo`）は方向を変えても消さない。

⚠️ **最重要仕様2**: **`entryFibo` のキー名は変えないこと**。CSV列 `en_entryFibo` と、📚振り返りの「エントリーFibo水準別の成績」（`byFibo` が `t.mvChecks.entry.entryFibo` を読む）がこの名前に依存している。S38 から続く記録がそのまま集計に乗り続ける。

⚠️ **最重要仕様3**: 項目を走査する側は必ず**区分ごとの定義**を引く。分母・列・表示がずれる箇所は5つ:
`checkConfidence(checks, list)`（分母が上位足7・エントリー足4に分かれた）/ `pairWaitCount()` / `basisItemsOf(checks, list)` / `CSV_BASIS_COLUMNS`（`MV_CHECK_SNAPSHOTS[].checks` から生成）/ `trendPanelHtml()` の `rowsOf()`。

**`opts` の書き方**: 文字列でも `{ v, side }` でも書けるが、`mvNormalizeChecks()` が宣言時に後者へ揃えるので**描画側は常にオブジェクトとして扱う**。正規化を1箇所に閉じておかないと、選択肢の形を意識する場所が増えて `side` の考慮漏れが出る。

**上位足から外したもの**: `entryFibo`（❸専用になったため、S62）・`rollReversal`（エントリー足の❸に一本化したため、S65）。CSV列 `hi_entryFibo`/`hi_rollReversal` は消えるが、旧CSVを import しても未知の列として無視されるだけで実害はない。エントリー足から外したもの: `granville` ほか上位足の項目（S62）。既存データに残る `w.checksEntry.granville` は読み書きされないだけで無害（S58 の `w.mode` と同じ扱い）。

**スコープ境界**: 端末間マージ・波マップ・エントリー圏の距離計算（`pairEntryDistance()`）・並び順は無変更。`checksEntry` は S28 の表で「チェック項目単位」でマージされるので、中の項目が入れ替わっても同期コードは1行も触らなくてよい。

### 13. 波の位置タップ記録と 🗺 波マップ（セッション40で追加）

**ユースケース**: 巡回で見ているチャートの頭の中には「買②を少し過ぎたあたり」「まだ買①の手前」という**連続的な位置**があるのに、記録は `買①〜売④` の8択（実際は方向で絞られた4択）しかなく、エントリーの早い/遅いが残らなかった。さらに全通貨を見渡して「今どこに何が溜まっているか」を掴む手段が無かった。

**やったこと**: グランビルの図をタップ面にして、**今のローソク足の位置を直接指す**。位置はそのまま残り、最寄りの公式ポジションが波番号として確定する。記録した位置は 🗺 波マップで全通貨まとめて見られる。

#### データモデル
`w.trend[tf]` に**文字列フィールド `wpos` を1つだけ**追加した。

```js
w.trend.d = { state: 'up', zone: 'green', granville: '2', wpos: '39.4,68.1', at: '...' }
```

- `wpos` = 画像に対する `"x,y"`（%・小数1桁）。**空文字＝ボタンで選んだだけの概算**、値あり＝図の上でタップした正確な位置
- `wx` / `wy` の2つではなく1つの文字列にした理由：①`mvEnsureTrend()` の「`trend[tf]` の全フィールドは文字列」という不変条件を壊さない（配列に `'wpos'` を1語足すだけで移行が済む）②x と y は必ず対で意味を持つので、片方だけ欠けた状態が構造的に作れない ③JSONバックアップでも人が読める
- アンカー座標は `MV_GRANVILLE_ANCHORS`（8点の % 座標）。ピッカーに薄い目印として重ねて出るので、図を差し替えて座標がズレたら目視で分かる

⚠️ **最重要仕様1**: **端末間マージ・CSV・統計・判定ロジックはすべて無変更**。マージは `trend[tf]` を**オブジェクトごと**差し替える（`if (B && ts(B.at) > ts(...)) out.trend[t.k] = B;`）ので、中に新フィールドを足しても同期コードは1行も触らなくてよい。`setupVerdict()` が読むのは `state / zone / granville` の3つだけなので GO/WAIT/NO も動かない。CSV は元から `w.trend` を見ない。

⚠️ **最重要仕様2**: **スナップ候補は必ずピッカーのグリッドに出ている選択肢と同じ集合**にする（`gvCandidates()`）。`mvWriteTrend()` は方向と逆 side のグランビルを自動で消すので、↗ の記録中に売りの波へスナップさせると**保存した瞬間に消える**。候補を絞ることで構造的に起こらなくしている。「売買8つすべて表示」を ON にすると候補も8つに広がり、そのときは既存仕様どおり自動クリアが効く。

⚠️ **最重要仕様3**: `wpos` の後始末は `mvWriteTrend()` の `if (!cur.granville) cur.wpos = '';` 1行だけ。granville が消える経路がそこ1箇所なので、方向の反転・レンジ化・クリアのすべてが自動で拾える。

⚠️ **最重要仕様4**: `.gv-fig` は**画像の実寸比を `aspect-ratio` に張って `object-fit` を使わない**。`object-fit: contain` のままだと「要素の箱」と「実際に絵が描かれる領域」がズレ、レターボックスの余白ぶんだけ重ね物が浮く（＝タップ座標が全部ズレる）。比率は `naturalWidth/naturalHeight` から JS が入れるので、図を差し替えても崩れない。

#### ピッカー（`💰 波?` チップから開く）
- 図の上をタップ → マーカー（リング＋十字）が出て、`買② 押し目` のようにスナップ結果を必ず言語化して表示する
- **タップしてもモーダルは閉じない**（押し直して微調整する前提）。行の差し替えは**閉じたときに1回**（`trendGvDirty`）
- 8ボタンは残す。押すと波は変わるが `wpos` は空になる（＝概算に戻る）。マーカーはアンカー位置へ移るので「ボタンで大まかに置いてから指で微調整」ができる
- 📖 ボタンで既存の解説ドロワー（`guide-granville`）へ
- **他時間足の参考重ね表示（S47）**: 図の上部に、いま開いている時間足を除いた他の時間足のトグルボタン（`trendVisibleTfs` にある足だけ・記録が無い足は disabled）が並ぶ。ONにすると、その時間足の既存の波位置を波マップと同じ足別配色（`.gv-dot.tf-d`/`tf-h4`/`tf-w`/`tf-mn`）で薄く重ねる。**タップして記録するのは開いた時間足だけ**という既存仕様は不変 — 参考表示は `gvRefEntry()`（読み取り専用の導出）と `gvRefDotHtml()` だけで完結し、`trendGvFig` の click ハンドラや `mvWriteTrend()` には一切触れていない。トグル状態 `trendGvRefOn` はモーダルを開くたびにリセットされる

#### 一覧の行
S40時点では `wpos` があるときだけ極小の波形 SVG（`gvMiniSvg()`）を前置していたが、**S45で撤去**。手描きの汎用パス（`GV_MINI_PATH`）が実アンカー座標（`MV_GRANVILLE_ANCHORS`）と一致しておらず（買③以外の7ポジションで座標が大きくズレる）、点の位置がタップした場所と無関係に見える紛らわしい表示だったため。タップ記録／ボタン選択（概算）の区別は、チップの `title` ツールチップの文言のみで示す（ラベル文字 `g.short` は従来どおりタップ座標から正しく計算されたまま）。

#### 🗺 波マップ
🔭一覧のツールバーから開く。**一覧の絞り込み（🎯エントリー圏のみ・未更新のみ・⏳待ちあり・カテゴリ・S58以降は🚩GOのみ）をそのまま適用**する（`trendApplyFilters()` を一覧から切り出して共有）。

- 時間足タブは `trendVisibleTfs`（表示トグルと一致）
- ドットは `wpos` があれば実線、無ければ**アンカー座標に中空**（＝既存記録も初日から全部載る）。色は `pairStatus()` の GO/WAIT/NO/RD
- ⚠️ **ほぼ同じ位置のものは小さな円周へ散らす**。散らさないと重なった数だけラベルが隠れ「1ペアしか居ない」ように見える（概算はアンカーの一点に必ず全部重なる）
- ⚠️ **図の下のテキスト一覧は飾りではなく本体**。ドットは必ず重なるので、波番号ごとに全件を読める形を必ず併置する。ペア名をタップすると一覧の該当行へ戻る
- ⚠️ ラベルの色は**テーマ変数に任せない**。図はテーマに関係なく白い画像なので、ダークだと明るい文字が白地に乗って読めなくなる

**拡張性**: `MV_GRANVILLE_ANCHORS` に座標を足せばポジションを増やせる。図を差し替えたら8点を測り直すこと。

**スコープ境界**: 記録するのは**波のどこにいるか**まで。エントリー/利確/損切りの価格そのものは 🎯フォームの建玉ブロック（`planTargetPrices()`）が持つ。波の位置はその判断材料であって、価格を自動計算するものではない。

### 14. 波マップの日次記録・単一銘柄モード（セッション42で追加）

**ユースケース**: S40 で波の位置が残るようになったが、波マップは「今」しか見られず**昨日・一昨日と比べて波がどう動いたか**を追えなかった。また同じ銘柄の日足と4時間足を1枚に並べる手段が無かった。

#### 日次スナップショット（`snapshots` を再利用）

```js
const WM_KIND = 'wavemap';
const WM_KEEP_DAYS = 30;
// { id: mvGenId('wm'), at: ISO, day: 'YYYY-MM-DD', kind: WM_KIND,
//   items: [{ p: 'USDJPY', t: 'd', g: '2', w: '36,62', s: 'go' }] }
```

保存先は market doc の `snapshots`（S28 のマージ表に「`id` で集合和・`at` 昇順」の行が既にある純追記ログ）。**だから同期コードは `wmPrune` の1行しか触っていない。**

- **記録の契機**: PWA はバックグラウンドで動けないので「毎日12時に自動保存」は原理的に不可。代わりに **🔭一覧タブを開くたびに今日の分を上書き**（`wmAutoSaveOnOpen()` → `setActiveTab()`）＋ 波マップの「📸 記録」ボタン
- ⚠️ **今日は常に上書き、過去日だけ凍結。** 「未保存なら保存」にすると巡回前にタブを開いた瞬間に空のレコードが今日の分として確定し、その日の記録が丸ごと残らない。`items.length === 0` なら保存しない
- ⚠️ ペアは **`id` ではなく名前 `p`** で持つ（S28 が pairs をペア名で突き合わせるのと同じ理由。id は端末ごとに採番される）
- ⚠️ **`toISOString().slice(0,10)` は使わない**（UTC なので JST では朝9時に日付が変わり、朝の巡回と夜の巡回が別日になる）。`wmDayKey()` がローカル日付で切る

⚠️ **最重要仕様1（プルーンの2つの落とし穴）**:
1. `saveMarket()` の縮小ガードのカウントから **wavemap を除外**すること。30日プルーンが1件でも削ると毎回「縮小」判定になり、`__bak` が「1秒前の状態」で焼き潰されて復元ボタンが無力化する
2. `mergeDoc()` の**マージ結果にも `wmPrune` を掛ける**こと。`mergeLog` はトゥームストーン無しの集合和なので、片方が落とした古い記録が同期でそのまま戻り、落とす→戻るを永久に繰り返す。日付は決定的なので両端末が同じ集合に収束する
3. `wmPrune` は **`kind !== WM_KIND` を無条件 keep**（既存の想定レコードを巻き込まない）

⚠️ **最重要仕様2**: `w.trend[tf]` のスキーマ・CSV・統計・GO/WAIT/NO 判定はすべて無変更。増えたのは `snapshots` の中身だけ。`BACKUP_SCHEMA` も据え置き（キー構成は変わらない）。

#### 日付比較と軌跡

`◀ / 今 / ▶` で日付を戻すと、選んだ日を実線ドット、**その前日を薄いドット（`.prev`）で重ね、動いたものだけ線で結ぶ**。

- ⚠️ **散らし処理のオフセットを前日レイヤーへ転写すること**（`wmScatter()` が返す → `wmApplyOffsets()`）。散らしは「同一セル内の件数と並び順」で角度が決まるので、当日と前日で件数が1件違うだけで**動いていないペアの点までズレ、実在しない軌跡線が引かれる**
- ⚠️ **軌跡線は SVG**（`#trendMapTrail`、`viewBox="0 0 100 100"` ＋ `preserveAspectRatio="none"`）。div + `transform: rotate()` は `.gv-fig` の `--gv-ar` 依存でリサイズごとに破綻する。**viewBox を潰して使うので `vector-effect: non-scaling-stroke` は必須**（無いと線幅が縦横で歪む）
- ⚠️ **ナビの目盛から今日を外す**（`trendMapNavDays()`）。今日の分は一覧タブを開くたびに上書きされる＝「今」とほぼ同じ内容なので、外さないと ◀ ▶ が「今」と「今日」の間で無意味に1段寄り道する。ライブ表示の前日レイヤーも同じ理由で今日を除外する
- ⚠️ **過去日では「未更新のみ」「🚩GOのみ」の絞り込みが効かない**（items が `p/t/g/w/s` しか持たず、`w.go` を運んでいないため導出できない。S58時点で `s` は既にS44から空文字専用のキーで実質未使用）。`#trendMapNote` に明記する — 黙って無視するとフィルタが壊れて見える。**🎯エントリー圏のみ**は座標（`w`）、カテゴリはペア名から導出できるので過去日でも効く
- ⚠️ 凡例の `data-map-goto` は id を要求するが過去日はペア名しか持たない。引けなければ（削除済みペア）`<button>` ではなく素のテキストで出す。押しても何も起きないボタンは S42-③ の「無反応」バグの再演

#### 単一銘柄モード

銘柄プルダウン（`#trendMapPairSelect`）で1つ選ぶと、**その銘柄の表示中の全時間足を1枚に重ねる**。

- ⚠️ **絞り込みは `trendApplyFilters()` に足してはいけない** — 🔭一覧タブと共有しているため一覧の表示まで変わる。`trendMapLiveEntries()` / `trendMapDayEntries()` の中で当てる
- ⚠️ **`pairStatus()` はペア単位なので、1銘柄では全足が同じ status 色になって用を成さない。** 色は `.gv-dot.tf-d` / `.tf-h4` / `.tf-w` / `.tf-mn` の足別に振り直し、記録の質（タップ＝実線／ボタン＝中空）は `.pt` に残す。ラベルもペア名ではなく足名にする
- 足タブは選択ではなく凡例になる（`disabled`）
- `openTrendMap()` で**日付・銘柄とも毎回リセット**（閉じて開いたら「今の全銘柄」に戻る）

**拡張性**: `WM_KEEP_DAYS` 1つで保持期間が変わる。`MV_TREND_TFS` に足を追加すれば `wmBuildItems()`・単一銘柄モード・足別の色（CSS に1行）すべてに乗る。

**スコープ境界**: 残すのは**波の位置の定点観測**まで。判定（GO/WAIT/NO）の履歴やトレード成績とは接続していない。

### 15. 🗺 波マップのエントリー圏（セッション43で追加）

**ユースケース**: エントリータイミングは **買②③ / 売②③** と決まっている。GO 判定は「その波かどうか」までは見ていたが、S40 で残るようになった **`wpos`（図のどこをタップしたか）はどこにも効いていなかった**。同じ買②でも「ドンピシャ」と「まだ手前」が区別できない。

**やったこと**: 波マップに **エントリー水準の圏**（4つ）を敷き、**圏内に入っているタップ記録だけを強調**する。

```js
const GV_ENTRY_RADIUS = 10;                  // % 座標での半径
function gvEntryDistance(x, y) { … }         // 最寄りのエントリーアンカーまでの距離
// trendMapEntryOf() が返す1件に dist / hot が乗る
```

- 圏の波は**別配列を作らず `SETUP_GO_WAVES` をそのまま参照**する。GO の波を増減したときに波マップの圏だけ取り残されると、判定と見た目が食い違う
- 半径 10% の根拠：8アンカーの最近接距離が約 19.5%（`'3'`↔`'8'`）なので、その半分が「他のどのアンカーより近い」ことが保証される上限。**定数1つでチューニングできる**

⚠️ **最重要仕様1**: **距離の測り方は `gvNearest()` と必ず同じ**（% 座標の素の距離）。画像は横長なので幾何学的には x と y で縮尺が違うが、ここで補正すると「買②と表示されているのに買③の圏内」という食い違いが起きる。**スナップ結果との一致 > 幾何学的な正しさ。**

⚠️ **最重要仕様2**: **概算（ボタンで選んだだけ＝`wpos` 空）は圏内に数えない。** 概算の座標はアンカーそのものなので距離 0 になり、買②③・売②③を選んだ全件が無条件で圏内になってしまう。見たいのは実測位置なので圏外扱いにし、その旨を `#trendMapNote` と凡例の脚注に明記する。

⚠️ **最重要仕様3**: `.gv-ezone` は **width と height を同じ「%」で指定する**＝画面上は横長の楕円になるのが正しい。`.gv-anchor` の `aspect-ratio: 1/1`（画面上の正円）と違い、`gvEntryDistance()` が測るのは % 空間の距離なので、画面上の正円にすると圏の見た目と圏内判定がズレる。

⚠️ **最重要仕様4**: `trendMapEntryOnly` の絞り込みは **`wmScatter()` より前**に当てる。散らしは同一セル内の件数と並び順で角度が決まるので、絞った後の集合で計算しないと前日レイヤーへ転写するオフセットが噛み合わず、S42 と同じ「実在しない軌跡線」が出る。

⚠️ **最重要仕様5**: `.hot` は**当日レイヤーだけ**（`trendMapDotHtml` が `' prev'` を見て外す）。過去の位置まで光ると「今どこが圏内か」が読めない。

**表示**:
- 図の上に緑の圏4つ（`#trendMapZones`。軌跡線より上・ドットより下）
- 圏内のドットは `.gv-dot.hot` でリング＋太字ラベル
- `#trendMapMeta` に `n件（うち 🎯 m件）`
- **凡例の先頭に「🎯 圏内（n）」グループ**（`.grp.entry`）。距離の近い順に `USDJPY 買② 3%`。既存の波番号別グループは従来どおり全件を再掲する
- ツールバーに **`🎯 エントリー圏のみ`**（`#trendMapEntryOnly`）。`openTrendMap()` で日付・銘柄と同様に**毎回 OFF へリセット**

⚠️ 凡例の 🎯 グループは同じペアを波番号別グループにも再掲するので、**テストで凡例の件数を数えるときは `.grp:not(.entry)` で絞る**こと（S40 テストの2件はこれで直した）。

**スコープ境界**: CSV・端末間マージ・統計はすべて無変更。

※ **S44 で `gvEntryDistance()` は 🔭一覧の主軸に昇格した**（`pairEntryDistance()` が並び順・サマリー・絞り込み・行の 🎯 に使う）。同時に GO/WAIT/NO 判定そのものが廃止されたので、上の「GO 判定」への言及は S43 時点の記述として読むこと。波マップ側の仕様（最重要仕様1〜5）はすべて現行のまま有効。

## タブ構成（セッション16で再設計、S23で4タブに、S55で3タブに再統合）

⚠️ **S55で🎯トレードタブを廃止し、下部タブバーは🔭一覧／📚振り返り／⚙設定の3タブになった。** トレード記録フォーム自体は削除しておらず、`#tradeModal`（`.modal-backdrop`）としてそのまま流用し、🔭一覧タブの根拠パネルの「📝 記録フォームへ」ボタンおよびヘッダの「→」ボタン、履歴の「✎ 編集」ボタンから**モーダルとして呼び出す**形に変更した。理由：S51で環境認識ボードを廃止して以降、🔭一覧タブが唯一の入力元として完結していたのに、結果記録のためだけに🎯タブへ画面遷移する操作が「一覧タブだけで完結できるはずなのに二度手間」というユーザー指摘による。`openTradeModal(watchId)` / `closeTradeModal()` が唯一の開閉口（背景クリック・Escape・✕ボタン・保存成功時すべてこの2関数を通る）。`doSave()` / `syncFormFromMarket()` / `pickMarketFields()` / `marketFieldsFromBoard()` / `loadTradeIntoForm()` のロジックは無変更（DOM要素idが同一のまま）。

| タブ | アイコン | 機能 |
|------|---------|------|
| 一覧 | 🔭 | **【S24改訂・S33/S40/S44/S51/S55拡張】トレンド一覧＝チャート巡回＋環境認識＋トレード記録まで完結する唯一の入力元**：監視ペア横断で上位足のトレンド方向・EMAゾーン・グランビルの波を行内タップで記録する。**S40: 波はグランビルの図を直接タップして記録し、🗺 波マップで全通貨の波の位置を1枚で見渡せる**。**S42: 波マップは日次で記録され、前日との軌跡比較と1銘柄の全時間足同時表示ができる**。**S44: GO/WAIT/NO の自動判定を全廃し、エントリー圏（買②③/売②③）までの距離順に並べる。圏内の足にだけ 🎯「根拠」ボタンが出て、根拠16項目・判定まで入れて記録フォームへ直行できる**。**S51: 環境認識ボードの廃止に伴い、通貨ペア削除（🗑）もこのタブの行に統合**。**S55: 根拠パネルの「📝 記録フォームへ」・ヘッダの「→」ボタンがタブ遷移ではなく `#tradeModal` をその場で開くモーダル方式に変更**（保存・キャンセルで一覧タブに自動で戻る）。詳細は上記「9. 🔭 トレンド一覧タブ」「11. エントリー圏で並べる」「12. 🔭一覧の根拠パネル」「13. 波の位置タップ記録と 🗺 波マップ」「14. 波マップの日次記録・単一銘柄モード」 |
| 振り返り | 📚 | **【新】統計＋履歴の統合画面**：上部に統計タイル群、下部にトレード履歴カード。区分フィルタ（全体/検証/リアル）が1つに統一され、切替すると統計と履歴が同時に絞り込まれる（セッション16で履歴・統計パネルを合併、フィルタを一元化）。**S55: 履歴の「✎ 編集」ボタンも `#tradeModal` を開く方式に変更**（旧🎯タブへの遷移は廃止） |
| 設定 | ⚙ | CSV エクスポート/インポート、その他設定 |
| トレード記録モーダル | 📝 | **【S55新設・旧🎯タブを置換】`#tradeModal`**：🔭一覧の根拠パネル「📝 記録フォームへ」／ヘッダ「→」／履歴の「✎ 編集」から開く。内容は旧🎯タブと同一（区分・エントリー日時・売買方向・アラート・結果タグ・損益・建値タッチ・チャート画像・振り返り備考）。✕ボタン・背景クリック・Escapeキー・保存成功のいずれでも閉じ、閉じた後は常に🔭一覧タブの画面に戻る |
| 解説ドロワー | 📖 | **右スライドイン式**：タブバーから独立し、右下の📖ボタンまたは**🔭一覧タブの根拠パネルのラベル（グランビル・MACD）**からスライドイン（S51で環境認識ボード廃止に伴いジャンプ元を一覧タブへ変更）。背後のスクロール位置が保たれるため、確認しながら作業を続けられる |

## カラーパレット

dataviz スキル準拠。ライト/ダーク両モード対応。

- **ダークモード**: surface #1a1a19 / accent #3987e5 / good #0ca30c / bad #e66767
- **ライトモード**: surface #fcfcfb / accent #2a78d6 / good #006300 / bad #d03b3b

## 実装状態

**✅ 本番使用可能** — 最新は S84（2026-09-22）。直近の変更点は下記「セッション履歴」表の先頭数行、詳細な機能一覧は「実装済み機能」節、S28〜S77の全文プローズ履歴は`docs/CHANGELOG_ARCHIVE.md`を参照（S80でCLAUDE.md本体を軽量化するため移設）。

**⚠️ 利用開始にあたって必須の作業**：3端末とも**ホーム画面に追加**し、以後アイコンから起動すること（iOS の7日削除を回避する唯一の方法）。同期を使うには設定タブで Supabase の Project URL / anon key を入力してログインする。

### セッション履歴（最新は S84。S28-69は`docs/CHANGELOG_ARCHIVE.md`に移設）

**S1-27 の詳細**: `memory/sessions/` 内の個別ファイルおよび `docs/SESSIONS_14_TO_18_ARCHIVE.md` / `docs/CHANGELOG_ARCHIVE.md` を参照。初期実装（S1-13）→ 環境ボード刷新（S14-18）→ 環境ボード仕様最適化（S19-21）→ 3分割エントリー・トレンド一覧追加（S22-25）→ ファイル最適化・UI改善（S26-27）

| セッション | 主な変更 | 日付 |
| 84 | 🐛✏️ **結果タグ/建値タッチのタップ反映バグ修正・日時手入力対応・その他理由の選択肢差し替え**。3件のユーザー報告に対応。①**結果タグ（REG/BIG/MAX等）・建値タッチのボタンをタップしても見た目が反映されない不具合を修正**：原因はCSSに`.result-btn.active`という汎用ルールが無く、`.entered.active`/`.hold.active`/`.demo.active`等「区分固有クラス＋active」の組み合わせにしか背景色が定義されていなかったため。結果タグ・建値タッチのボタンは`data-value`属性のみで区分クラスを持たないので、JSは`active`クラスを正しく付け外ししていたが見た目に一切反映されなかった（区分・判定トグルは元々`.demo`/`.real`等のクラスを併せ持つため無事だった）。`.result-btn.active { background: var(--accent); ... }`を追加して解消 ②**日時入力欄（エントリー日時・エグジット日時・アラート発生日時・アラート発生日時モーダル）に手入力用テキスト欄を併設**。`datetime-local`のネイティブUI（特にiOSのホイール選択）はキーボード入力ができない環境があるため、隣に`YYYY-MM-DD HH:MM`形式のテキスト欄を追加し双方向同期（`dtManualSync()`/`dtManualSyncAll()`）。ピッカーで選んでもテキスト欄に反映され、テキストで入力してもピッカーに反映される。不正な形式はトースト警告して元の値に戻す ③**結果「その他」のプルダウン選択肢を差し替え**（`RESULT_OTHER_REASONS`）：「ニュースで手仕舞い/時間切れ・撤退/ルール違反での早期決済/その他」→「RCI(上限OR下限)にて決済/ネックラインOR直近の高値・安値で決済/経済指標にて撤退/閉場前に決済/ルール違反での決済」。保存済みの旧理由文字列は表示上「未選択（保存済みの文字列のみ残る）」相当になるがデータ自体は保持され、CSV往復にも影響しない。データモデル・CSV列・端末間マージ・統計・波マップ・根拠パネルは無変更。既存全スイート（s44/s55/s59/s60/s62/s76/s77/s80/s81/s82）通過確認済み。sw.js: v54→v55 | 2026-09-22 |
| 83 | ✏️ **エントリー足❷❸の項目差し替え：MA抜け→アラート、ロールリバーサル→グランビル（ダマシ/ヒゲ発生）**。ユーザー指摘「エントリー足の②の項目をアラートに変更、選択項目は●か✖／③をグランビルに変更し、選択項目はダマシとヒゲ発生に変更」に対応。①`MV_ENTRY_CHECKS` の ❷ `maBreak`（旧「5分足MA抜け」上抜け/下抜け/❌）を**「アラート」**（●/✖）に置換 ②❸ `rollReversal`（旧「ロールリバーサル」確認/❌）を**「グランビル」**（ダマシ/ヒゲ発生）に置換 ③キー名 `maBreak`/`rollReversal` は据え置き（CSV列名・`checksEntry`保存先・確度スコアのロジックへの影響を避けるため） ④両項目とも新オプションに `side`（買い/売り絞り込み）を付けていないため、上位足の方向を反転・レンジ化しても自動では消えなくなった（`mvSyncEntrySideChecks()` は `opt.side` が無い項目をそもそも対象にしないため無改修で正しく動く。❶ `necklineForm` は引き続き方向で絞られ、反転で消える）。CSV列名・列数・確度スコアの分母は無変更（選択肢の中身が変わるだけ）。旧オプション値（上抜け/下抜け/確認）を保存済みのデータは新しい選択肢と一致せず画面上「未選択」相当になるが、マイグレーションは行っていない（無害、再選択で上書きされるまで残るだけ）。端末間マージ・波マップ・並び順・エントリー圏距離計算は無変更。既存s62-test.mjsを新仕様に更新（方向反転時に❷❸が残ることを検証する形へ変更、確度テストは❌選択肢が残る`necklineForm`を使う形へ変更）し全通過、他の既存スイートも全通過。sw.js: v53→v54 | 2026-09-22 |
| 82 | 🎯 **エントリー足のFibo選択に「指値／逆指値」の前置き選択を追加**。ユーザー要望「FIBOの箇所にまず指値か逆指値かの選択をさせて、指値ならFIBOの選択へ、逆指値ならそのままでよい」に対応。①`MV_ENTRY_CHECKS` の `entryFibo` 直前に `entryOrderType`（指値/逆指値/❌）を追加 ②`trendPanelHtml()` の `rowsOf` を条件フィルタに変更し、`entryFibo` 行は `checksEntry.entryOrderType === '指値'` のときだけ表示（逆指値・❌・未選択では隠す。逆指値はブレイクに乗るだけでフィボの水準という概念が無いため）③`mvWriteCheck()` に後始末を追加：`entryOrderType` が「指値」以外に変わったら、隠れた `entryFibo` の値を自動でクリア（古い値が食い違って残らないように）④根拠チェックのボタンは通常パネルを再描画しない規約（S33最重要仕様3）だが、`entryOrderType` の変更だけはFibo行の出現/消滅に関わるため例外的に再描画するようクリックハンドラに分岐を追加。CSV列は `en_*` が4列→5列（`en_entryOrderType` を追加）。確度スコア（エントリー足、均等割り）は分母が4項目→5項目に。端末間マージ・波マップ・並び順・エントリー圏距離計算は無変更（`checksEntry` は項目単位でマージされるため）。新設s82-test.mjs 13項目、既存s62-test.mjs（3ステップフローの項目数・保存内容・CSV列を新仕様に更新）・s44-test.mjs（パネルの行数を更新）を含め全スイート通過。sw.js: v52→v53 | 2026-09-21 |
| 81 | 🐛 **3端末で「消した記録が同期のたびに復活し、新たな記録ができない」不具合の修正**。ユーザー報告「ゴミ箱や箒のマークで入力を消した後に新しく記録しようとすると、他端末の情報を取り入れましたという同期の更新で消す前に戻ってしまう」の原因を2つ特定し修正。①**🧹巡回記録クリア**（`mvClearTrend()`）が `w.trend = {}` と空にするだけで時刻を打っていなかった。端末間マージは `trend[tf].at` の新しい方を採るので、クリアすると時刻が空（＝0）に戻り、他端末に残る古い記録が**必ず**勝つ。消した記録が同期のたびに復活し、こちらのクリアも他端末へ伝わらなかった。→ 全時間足に「空の内容＋現在時刻」を書き、クリアを1つの更新として正しく伝播させる ②**🗑ペア削除**は墓標（`markDeleted('pairs', ...)`）を立てていたが、**プリセット28銘柄は `loadMarket()` が必ず作り直す**ため削除直後からローカルにペアが存在し、`mergePairs()` の「ローカルに無いペア」用の墓標チェックを一切通らなかった。結果、他端末に残る削除前の判定・GO・上位足/エントリー足・アラート・根拠チェック・巡回記録がフィールド単位で丸ごと戻っていた。→ `mergePair(a, b, dead)` に墓標を渡し、新設 `mvApplyPairTombstone()` でマージ結果に当てて削除より古い内容を**双方から**落とす（`mergeTrades()` が墓標を最終結果に当てるのと同じ考え方）。これで削除が他端末へも伝わる。削除より新しい＝消したあとに録り直したものは残す ③クリアが「空の内容＋現在時刻」になったため、`w.trendAt` をそのまま見る鮮度表示だとクリア直後のペアが「たった今」＝巡回済みに見えてしまう。新設 `trendContentAt()` で**中身が実際に入っている足**の最新時刻から鮮度を出すよう変更（`trendAgeHours()`/`trendFreshChip()`）。マージ用の `trendAt` の意味は無変更。データモデル・CSV・統計・波マップ・並び順・根拠パネル・Supabase の行構成は無変更。新設s81-test.mjs 26項目通過、既存全スイート通過（s78-sync 17／s44 64／s62 48／s60 17／s59 28／s55 15／s80 19／s76 11／s77 7／s75 8／s74 7／s73 9／s43 35／s42 45／s40 62）。sw.js: v51→v52 | 2026-09-21 |
| 80 | 📊 **確度%帯別/根拠項目別の成績、リスク額＋R倍率、見送りの振り返り、本番同期の安全弁バグ修正**。ユーザーからの改善提案7点への対応。①📚統計に「確度%帯別の成績」（`checkConfidence()`の重み付けスコアが実際に機能しているかを検証）・「根拠項目別の成績」（`MV_CHECK_SNAPSHOTS`を走査しRCI/MACD等の値ごとの平均損益を表示）を追加 ②フォームに「リスク額（円）」（`riskAmount`）を追加し `pnlAmount/riskAmount` でR倍率を算出、履歴カード・平均R倍率タイルに表示 ③保留・スルー記録に「結果的に：獲れた／妥当だった」の振り返りボタン（`retroOutcome`）を追加、📚に集計タイル新設 ④トレード削除時にFirebase Storageの孤児画像をベストエフォートで削除（`deleteTradeImages()`） ⑤**重大発見・修正**：`runSync()`の安全弁が`file://`判定のみで、`http://localhost`で配信されるテスト（s28-test.mjs等）から本番の共用Supabaseテーブルへ実際に読み書きしていた。localhost/127.0.0.1も同じ扱いに拡張。`s78-sync-test.mjs`のモックURLも旧プロジェクトIDのままで実質何もインターセプトしていなかったため現在のURLに修正 ⑥CSV列に`riskAmount`/`retroOutcome`を追加 ⑦壊れたテスト整理：s24-test.mjs削除（S51/S55/S63等で前提UIが軒並み消滅）、s28-test.mjsの`.tab-btn[data-tab="trade"]`/`#mvPairSelect`等の廃止済みセレクタと旧`app_state`同期UIのテストを現行仕様に更新・削除、s40-test.mjsのS45で撤去された文字ラベル表示への依存を`title`属性参照に修正。データモデル・並び順・波マップ・端末間マージの中核ロジックは無変更。新設s80-test.mjs 19項目、s28-test.mjs 69項目・s78-sync-test.mjs 17項目・既存全スイート通過。sw.js: v50→v51 | 2026-09-20 |
| 79 | 🎯 **🎯チップからエントリー距離%を削除、確度%のみ表示**。ユーザー指摘「🎯マークの右横に2つパーセントが並んでいる（距離%と確度%）のは紛らわしい」に対応。`trendEntryChipHtml()`の距離%（`pct`変数）を表示から削除し確度%のみ残す。距離情報はtitle属性のツールチップに残る。sw.js: v49→v50 | 2026-09-19 |
| 78 | ☁ **端末間同期を fx-trade-tracker 方式（ログインなし・1記録1行）に置換**。マジックリンクの送信回数制限で実用にならなかったため。表 `trades_checklist` に `t:/p:/j:/s:/tomb` 行で保存、マージは既存 `mergeDoc()` を流用、差分取得＋変更行のみ送信で通信量を抑制。認証・接続先入力UI・`app_state` コードを削除。新設 `s78-sync-test.mjs` 17項目（2端末モック：足単位マージ・差分送受信・墓標削除・安全弁・`trades` 表非接触）、s44/s55/s60 通過。同セッションの Firebase 画像保存の実運用化（`s78-test.mjs`）も含む。sw.js: v47→v49 | 2026-09-14 |
| 77 | ✕ **🔭一覧の通貨名検索欄に✕クリアボタンを追加**。ユーザー要望「検索バーに入力した文字を消す際にバーの右側に小さな❌マークを設けてそこをタップすれば、入力していた文字をすぐ消せるようにしたい」に対応。①`#trendSearchInput`を`.trend-search-wrap`（`position:relative`）で包み、右端に`#trendSearchClear`ボタン（✕、`hidden`属性で既定非表示）を配置。入力欄の`padding-right`を広げてボタンと文字が重ならないようにした ②`input`イベントで値の有無に応じて`trendSearchClear.hidden`を切替 ③クリアボタンのクリックで入力値と`trendSearchQuery`をリセットし絞り込みを解除、フォーカスを入力欄へ戻す。`renderTrendList()`は`trendListEl`のみ再構築なので他のUI状態に影響しない。データモデル・CSV・端末間マージ・統計・並び順ロジック・波マップ・根拠パネル・S76の検索ロジック自体は無変更。新設s77-test.mjs 7項目通過（初期非表示・入力後表示・クリア後の値/表示/フォーカス/絞り込み解除、コンソールエラー無し）。sw.js: v46→v47。gitコミット: （未実施） | 2026-09-13 |
| 76 | 🔍 **🔭一覧ツールバーに通貨名検索機能を追加**。ユーザー要望「並び順や全てのカテゴリの箇所に、通貨の頭文字を入力したらその通貨だけ検索できる検索機能を設けて」に対応。①カテゴリ選択（`#trendCategorySelect`）の隣に検索入力欄（`#trendSearchInput`）を新設、`input`イベントで`trendSearchQuery`を更新し即時絞り込み（大文字小文字は区別しない）②`trendApplyFilters()`（一覧・波マップ共有の絞り込み合流点）に1行追加するだけで実装。GO/未更新/カテゴリと同じAND条件、🗺波マップのライブ表示（`trendMapLiveEntries()`）にもコード変更なしで自動反映 ③波マップの過去日表示（`trendMapDayEntries()`）は`items`が`p`（ペア名）のみ保持するため、カテゴリと同様に個別で検索条件を追加 ④`renderTrendList()`は`trendListEl`のみ再構築し検索`<input>`要素自体は作り直さないため、連続入力中もフォーカス・カーソル位置を保持 ⑤GO/未更新/カテゴリと同じく永続化しない（巡回中の一時的な絞り込み）。データモデル・CSV・端末間マージ・統計・並び順ロジック・根拠パネルは無変更。新設s76-test.mjs 11項目通過（検索欄存在・大小文字無視・AND条件・空該当表示・フォーカス保持・波マップ連動・コンソールエラー無し）、既存s42/s43/s44/s55/s59/s60全通過（205項目）。sw.js: v45→v46。gitコミット: （未実施） | 2026-09-13 |
| 75 | 🎯 **上位足RCIの選択肢をトレンド方向で自動絞り込み**。ユーザー指摘「上位足のRCIの選択の際、上昇トレンドの場合は下限か↓60か❌しか選択しないから記録した情報から反映して自動で選択項目を省略してほしい。下降トレンドならその逆」に対応。①`MV_TF_CHECKS`のRCI短期/中期/長期の`opts`に`side`を付与（上限・60↑→`sell`、60↓・下限→`buy`、❌は無指定）。押し目狙い（↗）ならRCIが下がっている場面しか意味を持たないため、逆サイドの選択肢は隠す方針は既存のエントリー足❶❷（S62 `MV_ENTRY_CHECKS`の`side`絞り込み）と同じ規約 ②`trendCheckCell()`の`side`算出を`field === 'tfEntry'`限定から`tfEntry || tfHigher`に拡張。`mvEntrySide(w)`は元々`w.tfHigher`が指す足の`w.trend[tfKey].state`を見て`up→buy`/`down→sell`を返す関数なので、上位足のRCIにもそのまま使い回せる ③`mvSyncEntrySideChecks()`（S62新設、`mvWriteTrend`/`mvSetTf`から呼ばれる書き込み後始末の唯一の入口）を拡張し、エントリー足に加えて上位足の`checksHigher[tfKey]`も走査。方向を反転・レンジ化したときに逆サイドで選んでいたRCI値を自動クリアする（❌・MACD・ラウンドナンバーはside無しなので影響なし）。方向未記録・レンジでは従来通り5択とも表示。新設s75-test.mjs 8項目通過、既存s42/s43/s44/s55/s59/s60/s62/s73/s74全通過（254項目）。sw.js: v44→v45。gitコミット: （未実施） | 2026-09-11 |
| 74 | 🎯 **未記録足は確度%を表示しない**。ユーザー指摘「🎯マークの横の確度%が、記録してない時間足は100%で表示されるのは誤解を招くので、記録してないものは%を表示させないでほしい」に対応。①`hasAnyHigherCheck(w, tfKey)`関数を新設し、その足の根拠チェックに1項目でも値が入っているかを判定 ②チップの確度%表示：記録済みのときだけ `<span class="d conf">61%</span>` を出力、未記録は HTML から除外 ③パネル内（`.tp-conf`）も未記録時は「🎯 確度 未記録」（グレー表示）に変更 ④このため`trendEntryChipHtml()`と`trendPanelHtml()`で`conf`変数の型を「数値 | null」に。視覚的に記録済みだけを見分けやすく。データモデル・CSV・端末間マージ・統計・グランビル・波マップ・エントリー圏距離計算・並び順は無変更。新設s74-test.mjs 7項目通過、既存s44/s62/s60/s59/s55全通過（289項目）。sw.js: v43→v44。gitコミット: （未実施） | 2026-09-10 |
| 73 | ⏱️ **GO右横にアラート発生時刻からの経過時間表示**。ユーザー要望「アラート発生時間をプルダウンに入力して保存して、その入力した時間（アラートを記録した時刻ではなく、入力されている時間からの経過時間）を読み取って現在時刻までどれくらい時間が経過しているかの時間を表示させたい」に対応。従来は🕐鮮度チップ（最終更新時刻からの経過）を表示していたが、ON の足だけ横に並べて「1H 3h15m」「4H 2d2h」のように表示。基準は S71 で手入力したアラート発生時刻（`w.alerts[tf].at`）。①新規関数 `elapsedShort()` で経過時間を「3h15m」「2d2h」「たった今」のように表示 ②`trendAlertAgeChipsHtml()`で ON の足すべてを横に並べ、タイトル属性に発生日時の全表記を追加 ③未来の時刻入力時は 0 に丸める。アラート無しのペアは従来の鮮度チップ（未記録/3h等）にフォールバック。バッジ下の発生日時表示（S69）は変更なし。データモデル・CSV・端末間マージ・統計・波マップ・根拠パネル・並び順は無変更。新設s73-test.mjs 9項目通過、既存s44/s62/s60/s59/s55全通過（280項目）。sw.js: v42→v43。gitコミット: （未実施） | 2026-09-10 |
| 72 | 🎯 **checksHigher を時間足キーごとに独立化→per-tf確度%**。バグ修正：🎯チップの確度%が全足で同じ値だった。根拠チェック（`checksHigher`）をペア共有から時間足キー（`h1`/`h4`/`d`/`w`/`mn`）ごとに独立化。①`w.checksHigher`：平坦形式 → 入れ子形式（時間足キー配下） ②旧データは`w.tfHigher`が指す足下に自動移行（`mvMigrateChecksHigherToTf()`） ③`mvWriteCheck(field='tfHigher')`は`MV_TF_TO_TREND_KEY[w.tfHigher]`で時間足を決定 ④根拠パネル・確度計算・マージロジックを時間足単位に対応 ⑤CSV出力時に平坦化（従来どおり）。テスト全通過（s62: 48項目、s44: 64項目、s60: 17項目）。データモデル・スナップショット・統計・マージロジックの高次部分は無変更。sw.js: v41→v42。gitコミット: （未実施） | 2026-09-10 |
| 71 | 📱 **アラート発生日時の手入力モーダル化**。要望：アラートが鳴った時刻を後から入力したい（値動き確認後の記録に対応）。🔭カードのアラートバッジ（1H/4H/D/W）をタップ → `#alertTimeModal`でdatetime-local入力 → 「保存」でON+時刻記録、「OFFにする」でON⇒OFF、「キャンセル」で変更なし。初期値：既存時刻をプリフィル、無ければ現在時刻。①新規関数 `mvSetAlertAt()`（モーダル「保存」「OFFにする」の共用先）、`openAlertTimeModal()`（バッジタップ時のエントリー点）、`toDatetimeLocalValue()`（datetime-local形式変換） ②モーダルの開閉・保存・OFF操作・表示反映をPlaywright smoke test で確認（コンソールエラーなし）③ボード側の旧`mvToggleAlert`（タップ時刻自動記録）は廃止、モーダル方式に一本化。並び順S67「選択した足のアラート時刻順」は維持。sw.js: v40→v41。gitコミット: （未実施） | 2026-09-10 |
| 70 | 🎯 **🎯チップの確度%を375px幅でも常時表示**。ユーザー要望「🎯マークをタップしてプルダウンを開かなくても、カードをスライドするだけで各通貨の確度が一目でわかるようにしたい」に対応。①CSS変更：`.tent .d { display: none; }` → `.tent .d:not(.conf) { display: none; }` で確度スパン（`.conf`）だけを畳みの対象から除外 ②幅を空けるため表示文言を簡略化（「確度61%」→「61%」）。スマホでも1段レイアウト維持（s44-testで検証）。確度計算ロジック・データモデル・CSV・端末間マージは無変更。sw.js: v39→v40。gitコミット: 9a22176 S70 | 2026-09-09 |

詳細ログはアーカイブを参照：
- S1-13: CLAUDE.md 内に統合済み
- S14-18: `docs/SESSIONS_14_TO_18_ARCHIVE.md` / `docs/CHANGELOG_ARCHIVE.md` で保存
- S28-69: `docs/CHANGELOG_ARCHIVE.md`（S80でCLAUDE.md本体の軽量化のため移設。本体には直近セッション（S70〜）だけを残す）
- S70以降: このすぐ上の表に記載

## 開発上の注意点

### 編集時の確認項目
- JS 構文チェック: `node -e "const fs=require('fs');const html=fs.readFileSync('index.html','utf8');const m=html.match(/<script>([\s\S]*)<\/script>/);new Function(m[1]);console.log('OK')"`
- **セッション21で検証済み**: CSVラウンドトリップ（`mvChecks` の往復、`❌`値、引用符・カンマを含むメモ）、旧CSV（`check1〜15`列）の import、旧 `checks` データのマイグレーション、編集時のボード由来項目の凍結
- **次のテスト推奨項目**:
  - 複数トレードでの統計検証（平均RR・勝率・根拠別ランキング）
  - エッジケース（空履歴、大量データ、localStorage 容量上限）
- スマホレスポンシブ確認（タブバーのセーフエリア対応、notch対応、下部パディング100px）
- 環境ボードの根拠チェック行がスマホ幅で折り返さないか確認

### Playwright テストの走らせ方（S80で全件グリーンに整理）
```bash
npm install
node s82-test.mjs      # S82: エントリー足Fiboの前に指値/逆指値選択（13項目・file:// で完結）
node s81-test.mjs      # S81: 🧹クリア・🗑削除が同期で巻き戻らない（26項目・file:// で完結）
node s80-test.mjs      # S80: 確度%帯別/根拠項目別の成績・リスク額+R倍率・見送りの振り返り・CSV往復（19項目・file:// で完結）
node s78-sync-test.mjs # S78: 端末間同期（2端末モック、17項目・file:// で完結）
node s77-test.mjs      # S77: 通貨名検索欄の✕クリアボタン（7項目・file:// で完結）
node s76-test.mjs      # S76: 🔭一覧ツールバーの通貨名検索機能（11項目・file:// で完結）
node s75-test.mjs      # S75: 上位足RCIの選択肢をトレンド方向で自動絞り込み（8項目・file:// で完結）
node s74-test.mjs      # S74: 未記録足は確度%を表示しない（7項目・file:// で完結）
node s73-test.mjs      # S73: GO右横にアラート経過時間表示（9項目・file:// で完結）
node s62-test.mjs      # S62: エントリー足の3ステップ確認フロー（48項目・file:// で完結）
node s60-test.mjs      # S60: GO/未更新のタップ箇所重複解消（17項目・file:// で完結）
node s59-test.mjs      # S59: 一覧タブ上部の説明文・サマリー・ツールバー整理（28項目・file:// で完結）
node s55-test.mjs      # S55: 🎯タブ廃止・🔭一覧からモーダルで記録フォーム（15項目・file:// で完結）
node s44-test.mjs      # S44: 判定の全廃とエントリー圏を軸にした一覧（64項目・file:// で完結）
node s43-test.mjs      # S43: 波マップのエントリー圏（35項目・file:// で完結）
node s42-test.mjs      # S42: 日次記録・単一銘柄モード・タップ不反応の修正（45項目・file:// で完結）
node s40-test.mjs      # S40: 波の位置タップ記録＋🗺波マップ（62項目・file:// で完結、S80で全項目通過に修正）
python3 -m http.server 8000 --bind 127.0.0.1 &   # S28 は Service Worker を使うので http が要る
node s28-test.mjs      # S28: 破損検知・バックアップ・PWA・タイムスタンプ・端末間マージ（69項目）
```
- ⚠️ 環境に置かれている Chromium が `package.json` の playwright と別ビルドのことがあるため、各テストは `chromium.launch()` に失敗したら `executablePath: '/opt/pw-browsers/chromium'` へ落ちるようにしてある
- ✅ **S80で全テストが全項目通過するようにした。** `s24-test.mjs`（S32〜S55で前提UIがほぼ全滅していたため削除、s44/s62等が現行仕様をカバー）を削除し、`s28-test.mjs`（`.tab-btn[data-tab="trade"]`・`#mvPairSelect`など廃止済みUIへの依存、`checksHigher`のS72入れ子化未対応、S71のアラートモーダル化未対応、旧`app_state`同期UIのテスト）と`s40-test.mjs`（S45で撤去された文字ラベル表示への依存）を現行仕様に更新した。旧・既知の失敗（S24/S28のS32-33仕様変更未追随分、S40の2件）はこのS80ですべて解消済み
- ⚠️⚠️ **重大発見（S80）：`s28-test.mjs`は`http://localhost:8000`で配信するため、`runSync()`の旧・安全弁（`location.protocol === 'file:'`のみ判定）をすり抜け、本番の共用Supabaseテーブルへ実際に読み書きしていた。** `runSync()`側でlocalhost/127.0.0.1もfile:と同じ扱いに拡張して修正済み（[index.html](index.html)の`runSync()`冒頭）。**新しく`http://`で配信するテストを書くときは、本番のSUPABASE_URLへの生アクセスにならないことを必ず確認すること**（file://を使うか、`page.route()`で明示的にモックする）
- ⚠️ **`s78-sync-test.mjs`のSupabaseモックURLは`SUPABASE_URL`定数と完全一致させること。** S78本番修正（d6ac473、プロジェクトIDを`inqvrsfzskjusmbwlimx`に変更）でURLが変わった際、このテストのモックURLだけ古いプロジェクトIDのまま残っており、実質何もインターセプトせず本番へ生の`fetch`が飛んでいた（S80で発見・修正）。新しいテストでSupabaseをモックするときは、コピペ元のURLが最新か必ず`index.html`の`SUPABASE_URL`と突き合わせること
- ⚠️ **一覧の行を指すセレクタは必ず `[data-trend-item="<id>"]` で絞ること。** `loadMarket()` がプリセット28銘柄を自動生成するので同じ属性の要素が28個あり、S44で並び順が「エントリー圏に近い順」に、S67で「選択した時間足のアラート発生時刻順」に変わっているため、いずれにせよ**seed したペアが先頭とは限らない**（`.first()` 頼みのセレクタは S44 でここで落ちた）
- ⚠️ **図（`IMG_GRANVILLE`）を差し替えたら `MV_GRANVILLE_ANCHORS` の8点を測り直すこと。** S41 で座標を更新した際、`s40-test.mjs` が旧座標を直書きしていて1件落ちた。現在はアプリ側の `MV_GRANVILLE_ANCHORS` を `page.evaluate` で引く形に直してあるので、次の差し替えでは落ちない
- ✅ **旧ツールバーボタン id（`#trendFilterAligned` 等）への依存は S62 で解消済み。** 絞り込みの起点は `[data-trend-summary-filter="go|stale"]`（`.trend-summary` 内のボタン。S63で`wait`、S67で`hot`の分岐を撤去）に統一されている。並び順の起点は `[data-trend-sort-tf="h1|h4|d|w"]`（S67新設）。新しくテストを書くときもこれらを使うこと
- ✅ **一覧の行の🎯根拠パネルを開く（判定・根拠チェックボタンを叩く前に必要）のは `[data-trend-panel-open]` を`.first()`等でクリックするだけでよい。** S44以降、判定ボタン（`.mv-judge-btn`）・根拠チェックボタン（`.mv-tfcheck-btn`）はこのパネルの中にしか無い（旧`#mvPairSelect`のボード自体がS51で撤去済み）

### CSV列構成（S83更新）
`id`, `tradeType`, `alertPair`, `alertTf`, `result`, `entryPattern`, `datetime`, `exitDatetime`, `pair`, `tfHigher`, `tfEntry`, `direction`, `exitResult`, `notes`, `manualAlertAt`, `manualAlertTf`, `resultTag`, `resultOtherReason`, `pnlAmount`, `riskAmount`, `beTouch`, `retroOutcome`, `imgEntry`, `imgHigher`, `imgOthers`, `watchId`, `hi_*` × 5, `en_*` × 5, `createdAt`

**S80変更**：`pnlAmount`の直後に`riskAmount`（円、リスク額。`pnlAmount/riskAmount`でR倍率を算出）、`beTouch`の直後に`retroOutcome`（保留・スルー記録の事後振り返り、`''|'missed'|'valid'`）を追加。旧CSV（両列なし）を import すると `riskAmount=0`/`retroOutcome=''` のデフォルト値になる。

⚠️ export は**位置配列**なので、`CSV_HEADERS` と export の並びを必ず同時に直すこと（ズレると全列が破壊される）。

根拠チェック列は `MV_CHECK_SNAPSHOTS`（`hi` = 上位足 / `en` = エントリー足）**× その区分の `checks`** から自動生成される（`CSV_BASIS_COLUMNS`）。S62 で区分ごとに定義が分かれたので、`MV_TF_CHECKS` / `MV_ENTRY_CHECKS` のどちらに項目を足してもCSV列が自動で増える。

- `hi_*` (5): `rciShort` / `rciMid` / `rciLong` / `macd` / `roundNumber`
- `en_*` (5): `necklineForm` / `maBreak` / `rollReversal` / `entryOrderType` / `entryFibo`

**S83変更**：`en_maBreak`（旧「5分足MA抜け」上抜け/下抜け/❌）は**「アラート」**（●/✖）に、`en_rollReversal`（旧「ロールリバーサル」確認/❌）は**「グランビル」**（ダマシ/ヒゲ発生）に選択肢の意味が変わった。列名・列数・列順は無変更（キー名を据え置いたため）。旧CSVをそのまま re-import すると値が新しい選択肢と一致せず「未選択」相当になるが、列自体は壊れない。詳細は「12b. エントリー足の3ステップ確認フロー」参照。sw.js: v53→v54

**S82変更**：`entryFibo` の直前に `entryOrderType`（指値/逆指値/❌）を追加。エントリー足のFibo選択の前に注文方法を選ばせ、指値のときだけFibo入力欄が出る（詳細は「12b. エントリー足の3ステップ確認フロー」参照）。CSV列は `en_*` が4列→5列。旧CSV（`en_entryOrderType` 列なし）を import すると `entryOrderType=''` になるが、`entryFibo` はそのまま復元される。sw.js: v52→v53

**S65変更**：ユーザー指摘「エントリー足の❸『重なり』項目を消してロールRvに変更したい。上位足のロールRvは消してよい」に対応。エントリー足の `fiboRoll`（フィボ×ロールリバーサルの重なり、重なり有/重なり無/❌）を撤去し、上位足 `MV_TF_CHECKS` にあった `rollReversal`（ロールリバーサル、確認/❌）をエントリー足の同じ位置（❸）に一本化。上位足からは `rollReversal` を削除（実運用で5分足での確認と重複していたため）。CSV列は `hi_*` が6→5列（`hi_rollReversal` 消滅）、`en_*` は4列のまま（`en_fiboRoll` → `en_rollReversal` に入れ替え）。確度スコアの重み（上位足）は RCI各15・MACD35・ラウンドナンバー10（分母90、100固定ではなく残った項目の重み合計を分母にする）。🔭一覧の行の🎯チップにも上位足の確度%を表示するようになった（S64）ため、この重み変更はチップ表示にもそのまま反映される。旧CSVの `hi_rollReversal`/`en_fiboRoll` 列は import しても未知の列として無視される。端末間マージ・波マップ・エントリー圏の距離計算・並び順・統計タブは無変更。sw.js: v35→v36

**S64変更**：ユーザー指摘「確度%を🔭一覧の🎯チップにも表示してほしい。エントリー足の確度・合算の表示はいらない」に対応。`trendEntryChipHtml()` の🎯チップに上位足の確度%を常時表示（title属性にも反映）。根拠パネル内の確度表示（`.tp-conf`）はエントリー足・合算を撤去し上位足のみに簡略化。`setupConfidence()` を `higherConfidence()` に置換。sw.js: v34→v35

**S63変更**：ユーザー指摘「グランビルは根拠パネルから無くしてよい（🔭一覧の行に🌊アイコンで既に出ている）」「RCI/MACDの⏳待ち、ラウンドナンバーの無、ロールリバーサルの未確認、エントリー足の⏳待ちは選択肢として不要」に対応。①`hi_granville` 列を撤去（`checksHigher.granville` への同期処理 `mvGranvilleSyncField()` も削除。実際の波記録 `w.trend[tf].granville` は無変更）②RCI短期/中期/長期・MACDから「⏳待ち」系の選択肢を、ラウンドナンバーから「無」を、ロールリバーサルから「未確認」を撤去 ③エントリー足の❶❷❸すべてから「⏳待ち」を撤去。これにより「⏳待ち」概念がアプリ全体から無くなったため、`pairWaitCount()`／`trendWaitBadgeHtml()`／サマリーの「⏳待ち」タイル／ツールバーの「待ちあり」絞り込みを削除（常に0件で機能しない死んだコードになるため）。`isUnmetCheck()` は❌のみを見る単純な判定に戻した ④各項目の左端にあった空欄（`—`、未記録時にデフォルト選択されて見えるボタン）を撤去。解除は選択済みボタンの再タップ（`mvWriteCheck` のトグル）で行う（従来から可能だった操作で変更なし）⑤確度スコア（`checkConfidence`）に項目ごとの重み `weight` を導入。上位足はRCI各15・MACD35・ラウンドナンバー10・ロールリバーサル10（合計100）で、環境認識の核となるRCI/MACDを厚く、補助的なラウンドナンバー/ロールリバーサルを薄くした。エントリー足は`weight`を持たせず従来通り均等割り（4項目×25%）。`weight`を持たない項目は自動的に1として扱われる。CSV列は `hi_*`×6／`en_*`×4 に変化（`hi_granville`が消滅、旧CSVを import しても未知の列として無視される）。端末間マージ・波マップ・エントリー圏の距離計算（`pairEntryDistance()`）・並び順・統計タブは無変更。

**S62変更**：エントリー足を3ステップ確認フローに置き換えたため、`en_*` 列が `en_granville`/`en_rci*`/`en_macd`/`en_roundNumber`/`en_rollReversal` から `en_necklineForm`/`en_maBreak`/`en_fiboRoll` に入れ替わった（`en_entryFibo` は据え置き ── 📚の Fibo別成績が見ているため）。上位足からは `hi_entryFibo` が外れた。旧CSVを import すると入れ替わった列は未知の列として無視される（値は空のまま＝「未選択」扱いなので集計は壊れない）。

**S54変更**：`entryPrice`/`slPrice`/`slBasis`/`splitCount`/`exits` 列を撤去し、`manualAlertAt`/`manualAlertTf`/`resultTag`/`resultOtherReason`/`pnlAmount`/`beTouch`/`imgEntry`/`imgHigher`/`imgOthers` 列に置換（ユーザー確認済みの完全置換方針。旧CSVの建玉・決済データは import しても復帰しない）

互換性：
- S54変更：旧CSV（新列なし）を import すると `manualAlertAt=''`/`manualAlertTf=''`/`resultTag=''`/`resultOtherReason=''`/`pnlAmount=0`/`beTouch=''`/`images={entry:'',higher:'',others:[]}` のデフォルト値になる。旧建玉・決済データ（`entryPrice`/`slPrice`/`slBasis`/`splitCount`/`exits`）は列ごと撤去されたため import しても読み込まれない
- セッション38追加：`entryFibo` 根拠チェック項目を追加。旧CSV（新列なし）を import すると `hi_entryFibo=''` / `en_entryFibo=''` となるが、既存の集計に影響しない（新項目は ❌ と同じ「未選択」扱い）
- セッション37変更：RCI/MACD の選択肢表示名を変更（`'60↑'` → `'60↑'`、`'RD'` → `'RD(転換)'`など）。CSV には値の形式は変わらず、旧CSV との互換性維持
- セッション21変更：`check1〜check15` 列を廃止。旧CSVを import しても未知の列として無視される（`checks` は復活しない）。`hi_*`/`en_*` 列が1つも無いCSVは `mvChecks = null` となり、履歴で根拠行を表示しない
- セッション3以降の形式（`tradeType`/`alertPair` なし）：import 時に `tradeType='real'` を自動設定
- セッション2以前の形式（削除済みフィールド）：不明な列を自動スキップするため可能だが、削除済み情報は復帰しません
- セッション13変更（`result` 値）：'entered', 'hold', 'through' を許容。旧CSV（'hold' なし）も正常に import 可能

セッション12（環境認識ボード新規実装）・セッション13（判定3択化・構造刷新）の詳細は `docs/CHANGELOG_ARCHIVE.md` を参照。**環境認識ボードの現行仕様は本ファイル冒頭「7. 環境認識ボード」の記載が最新**（セッション14で刷新済み）。

### 実装済み機能（セッション84完了時点、S52-S58は`docs/CHANGELOG_ARCHIVE.md`に移設済み）
- ✅ **結果タグ/建値タッチのタップ反映バグ修正・日時手入力対応・その他理由の選択肢差し替え**（S84）— `.result-btn.active`のCSS欠落で結果タグ・建値タッチのタップが見た目に反映されなかった不具合を修正。日時入力欄に手入力用テキスト欄を併設（`datetime-local`のホイール選択しか無い環境向け）。結果「その他」の理由をRCI/ネックライン/経済指標/閉場前/ルール違反の5択に差し替え
- ✅ **エントリー足❷❸の項目差し替え（MA抜け→アラート、ロールリバーサル→グランビル）**（S83）— ❷はアラート発生（●/✖）、❸はグランビルのダマシ/ヒゲ発生に選択肢を変更。キー名・CSV列は据え置き、方向非依存の項目になったため反転しても自動で消えなくなった
- ✅ **エントリー足のFibo選択に指値/逆指値の前置き選択を追加**（S82）— entryFibo行はentryOrderTypeが「指値」のときだけ表示。逆指値に変えると隠れたFibo値を自動クリア
- ✅ **消した記録が同期で復活する不具合の修正**（S81）— 🧹クリアが時刻を打たず必ず同期で負けていた問題、🗑削除の墓標がプリセット自動再生成で無視されていた問題を修正。削除操作が3端末すべてに正しく伝わるように
- ✅ **確度%帯別/根拠項目別の成績、リスク額＋R倍率、見送りの振り返り**（S80）— 📚統計に確度スコア・各根拠項目の値ごとの成績検証、フォームに「リスク額」入力とR倍率表示、保留/スルー記録への事後振り返りボタンを追加。トレード削除時のFirebase孤児画像も削除。あわせて本番Supabaseへ書き込んでいたテストの安全弁バグを修正
- ✅ **🎯チップからエントリー距離%を削除**（S79）— 距離%と確度%が並んで紛らわしかったため確度%のみ表示に
- ✅ **🔭一覧ツールバーに通貨名検索機能**（S76）— カテゴリ選択の隣に検索入力欄を新設。通貨名の部分一致（大小文字区別なし）で絞り込み、GO/未更新/カテゴリと同じAND条件。`trendApplyFilters()`への1行追加で🗺波マップにも自動反映
- ✅ **上位足RCIの選択肢をトレンド方向で自動絞り込み**（S75）— 上昇トレンドなら下限/60↓/❌、下降トレンドなら上限/60↑/❌のみ表示。S62のエントリー足side絞り込みと同じ規約。方向反転時は逆サイドのRCI値を自動クリア（mvSyncEntrySideChecksを拡張）
- ✅ **未記録足は確度%を表示しない**（S74）— 🎯チップの確度%は記録済みだけ表示。未記録時は「確度 未記録」（グレー）に。誤解を招く「未記録なのに100%」を排除
- ✅ **GO右横にアラート発生時刻からの経過時間表示**（S73）— 「1H 3h15m」のように、手入力アラート時刻から現在までの経過を表示
- ✅ **checksHigher を時間足キーごとに独立化→per-tf確度%**（S72）— 🎯確度%が全足で同じ値だったバグ修正
- ✅ **アラート発生日時の手入力モーダル化**（S71）— アラートバッジをタップしてdatetime-local入力可能に
- ✅ **🎯チップの確度%を375px幅でも常時表示**（S70）— スマホ1段レイアウト維持しながら確度を常時表示
- ✅ **アラート発生日時をカードに常時表示、アラート〜エントリー経過時間の統計追加**（S69）— 🔭カードのアラート下に発生日時表示、📚統計タブに経過時間タイル追加
- ✅ **🔭一覧の並び順をアラート発生時刻順に置換・カード連動・圏内/巡回タイル撤去**（S67）— ツールバーの1H/4H/D/Wボタンで選んだ足のアラート発生日時が新しい順に一覧を並べる（`trendSortTf`）。カード上のアラートバッジをタップすると基準足もその場で切り替わる。サマリーの「🎯圏内」「巡回n/mペア」タイルとエントリー圏のみ絞り込み・グループ見出しは撤去（GO・未更新の2タイルに整理）。`pairEntryDistance()`等の圏内判定関数自体は波マップ・🎯根拠ボタンの色分けのため存続

### 今後の拡張候補（セッション52以降）

詳細は `memory/ARCHIVE_SESSION_ROADMAP.md` を参照。優先度順（S51完了時点）：

**即実装可能（ブロッカーなし）:**
- **0.** 🎯 `GV_ENTRY_RADIUS` の実運用チューニング

**要計画・影響範囲大:**
- **4.** 📈 他のインジケーター画像追加
- **5.** 📊 根拠別の成績詳細分析
- **6.** 🧠 localStorage 容量対策（ユーザーがデータ溜まってから）

**低優先度（既存機能で対応可能）:**
- 📊 期間フィルター
- 🏷 タグ・カテゴリ

### 既知の制限
- ブラウザの localStorage 容量上限（通常 5-10MB、数千件のトレード記録で注意）
- 環境認識ボードはCSVエクスポート非対応（トレード記録とは独立したデータストア）

## URL

| 環境 | URL |
|------|-----|
| jsDelivr CDN（推奨） | https://cdn.jsdelivr.net/gh/jiangchengban-art/fx-entry-checklist@master/index.html |
| GitHub Pages | https://jiangchengban-art.github.io/fx-entry-checklist/ |
| GitHub 本体 | https://github.com/jiangchengban-art/fx-entry-checklist |

## ローカル実行

```bash
cd fx-entry-checklist
python -m http.server 8000
# http://localhost:8000 で開く
```
