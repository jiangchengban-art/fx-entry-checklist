# fx-entry-checklist プロジェクト

FX Entry Checklist（FXエントリーチェックリスト＆トレード記録ツール）

## プロジェクト概要

- **ファイル**: `index.html` （単一ファイル、CDN依存なし、完全オフライン動作）
- **技術スタック**: HTML5 + CSS3 + Vanilla JavaScript
- **データ永続化**: localStorage (キー: `mochipoyo_trades_v1`, `mochipoyo_market_view_v1`, `mochipoyo_theme`)
- **独立性**: FX Trade Tracker とは完全に分離（別の localStorage キーを使用）

## ユースケース

FX トレーダーが環境認識ボードで通貨ペア・時間足・根拠チェック（グランビル/RCI/MACD/ラウンドナンバー/ロールリバーサル）を記録 → 判定（エントリー/保留/スルー）を決定 → トレード記録フォームで建玉（エントリー価格・損切り価格・損切り根拠）を設定して計画ターゲットを確認 → エントリー後に決済モーダルで各枚の決済を記録（3分割なら2:1/3:1/4:1） → 振り返りタブで合成RR・枚目別成績・損切り根拠別成績から実力を分析

## 機能リスト

### 1. ~~エントリーチェックリスト（15項目）~~ → **セッション21で廃止**
環境認識ボードに根拠チェック（グランビル/RCI/MACD/ラウンドナンバー/ロールリバーサル）を実装した結果内容が重複したため、**セクションごと削除**。関連する進捗バー・NG警告モーダル・「チェック遵守率」統計タイル・履歴のNGバッジ・CSV `check1〜15` 列もすべて撤去した。既存記録の `checks` は `loadTrades()` のマイグレーションで破棄される。

📖解説ドロワーへのジャンプリンク（グランビル・MACD）はチェックリスト内にあったため、**環境認識ボードの該当チェック行のラベルへ移設**（`MV_TF_CHECKS` の `link` プロパティ）。

### 2. エントリーパターン（セッション21で自動導出に変更）
固定4択の入力欄は廃止。環境認識ボードの `tfEntry + '×' + tfHigher` から自動生成される（例：「15分足×日足」）。ボードは上位足4種×エントリー足4種=16通りを表現できるため、旧4択より表現力が高い。CSV の `entryPattern` 列は互換のため維持。

### 3. トレード記録フォーム（セッション21で大幅スリム化、S22で建玉ブロック追加）
**現在の入力項目:**
- **必須**: 区分（検証/リアル）、エントリー日時
- **条件付き必須**: 売買方向（ボードの判定が「エントリー」のときのみ表示・必須）
- **判定=エントリーのときのみ表示（任意）**: 建玉ブロック — エントリー価格 / 損切り価格 / **損切り根拠**（直近高値・安値をカバー / フィボ161%をカバー / その他）。入力すると **2:1 / 3:1 / 4:1 の計画ターゲット価格**が `#planTargets` にライブ表示される（`planTargetPrices()`）。ショートは符号が反転する
- **任意**: エグジット日時、振り返り・備考

**環境認識ボードから自動で引き継がれる項目（フォームに入力欄なし）:**
通貨ペア / アラート発生通貨ペア / アラート発生時間足（複数足を「 / 」区切りで結合）/ 上位足 / エントリー足 / エントリーパターン / 判定 / 根拠チェックのスナップショット（`mvChecks`）/ `watchId`

フォーム冒頭の `#formMarketSummary` にこれらが読み取り専用で表示される。ボード未選択や上位足・エントリー足・判定が未設定の場合は警告を出して保存ボタンを `disabled` にする。

⚠️ **編集時はボード由来の項目を保存済み記録から復元し、当時の値を凍結する**（`pickMarketFields()`）。今日のボード状態で過去の記録が上書きされるのを防ぐための最重要仕様。そのため編集で判定やペアは変更できない。

- ※ セッション3変更：スタイル・エントリー価格・決済価格・損切り価格・pips は削除
- ※ セッション6追加：区分、アラート発生フィールド、エントリー/スルー判定
- ※ セッション13追加：判定に「保留」を追加（エントリー/保留/スルーの3択）
- ※ セッション21変更：重複7項目を削除し、環境認識ボードを唯一の入力元に統一

### 4. 履歴一覧（セッション11で編集機能追加）
- 新しい順のカード表示、方向別色分け
- ※ セッション21変更：NGバッジ・チェック数バッジを削除し、代わりに**根拠サマリー行**（`.record-basis`）を追加。`mvChecks` から `❌`・未選択を除いた項目だけを上位足/エントリー足に分けて表示する
- ※ セッション6追加：区分フィルタ（全体/検証/リアル）、区分バッジ、判定バッジ、アラート情報表示
- ※ セッション10追加：**「💰 決済」ボタン**（S22で「💰 利確」から改称。エントリー記録にのみ表示）で後日の決済を記録
- ※ セッション10追加：**RR表示**（実決済ポイントに◎付きで「根拠 RR +X.XX」形式で表示）
- ※ セッション22追加：分割記録は **「決済 n/3枚 合成RR +X.XX」** を先頭に、枚順（◎=決済済 / △=未決済）→ 参考ポイントの順で表示。旧記録は従来表示のまま。損切り根拠の行も追加
- ※ セッション11追加：**「✎ 編集」ボタン**で記録フォーム全フィールドを復元・編集・更新保存可能

### 5. 統計サマリー（タブ化、セッション10更新）
- 下部タブバー「📊統計」として独立
- トレード数、エントリー数、保留数、スルー数を表示（※セッション21で「チェック遵守率」タイルを削除。現在は全7タイル）
- ※ セッション3変更：勝率・合計pips・平均pips は削除（pips入力欄廃止に伴い）
- ※ セッション6変更：区分フィルタ（全体/検証/リアル）を追加、エントリー数/スルー数タイルを追加
- ※ セッション10追加：**平均RR**、**勝率**（RR > 0 の割合）、**最大到達RR**の3タイルを追加
- ※ セッション10追加：**利確根拠別の平均RRランキング**（どの利確ルールが最も期待値高いか比較可能）
- ※ セッション13追加：**保留数**タイルを追加
- ※ セッション22変更：「平均RR」→ **「平均合成RR」**（3枚の平均。3/3決済したトレードのみ母数）、「勝率」も合成RR基準に。「最大到達RR」→ 「最大到達RR（参考含む）」に改称。**「決済待ち」タイル**（進行中の件数）を追加 → 全8タイル
- ※ セッション22追加：**「枚目別の成績」**（1/2/3枚目ごとの決済枚数・計画到達率・平均RR）→ *4:1 まで引っ張る価値があるかの判断材料*
- ※ セッション22追加：**「損切り根拠別の成績」**（直近高安カバー vs フィボ161%カバー の件数・全損率・平均合成RR）

### 6. 追加機能
- CSV エクスポート/インポート（BOM付きUTF-8、id重複検出、互換性あり）
- ダーク/ライト切替（localStorage保存）
- タブベースUI（3タブ：🎯トレード / 📚振り返り / ⚙設定）、画面下部固定ナビ。📖解説は右スライドイン式ドロワーに独立（画面を邪魔しない）

### 7. 環境認識ボード（セッション12で追加、S13で構造刷新、S14で直接編集化、S17で単一ペア選択式に再設計、**S19で上位足・エントリー足プルダウン選択に変更、S20で根拠チェック項目を追加**）

**現在の設計（セッション20時点）:** ツールバーの「通貨ペア選択」ドロップダウンで1つペアを選ぶ → カード上に「上位足」「エントリー足」の2つのプルダウンが表示され、ユーザーが自分の監視スタイル（例：日足×15分足）を選択・即保存。各時間足の下には「グランビル/RCI/MACD/ラウンドナンバー/ロールリバーサル」の根拠チェック行が並び、ユーザーが確認した内容をその場で記録できる。選択内容・チェック値はペアごとに記憶され、ペア切り替え時に復元される。

**設計の進化:**
- **S12〜13**: ペア＋時間足ごとの複数カード一覧表示（スクロール確認）
- **S14**: モーダルなしの直接クリック編集に変更
- **S17**: 複数カード一覧 → 単一ペア選択式に変更（コンパクト化）
- **S18**: アラート発生足を単一選択 → **複数時間足（1H/4H/D/W）同時トグル式**に変更、発生日時を足ごとに記録し、判定と紐付けて統計化
- **S19**: 週足/日足/4時間足「固定3行トグル」 → **「上位足プルダウン」「エントリー足プルダウン」に変更**（複数監視スタイルに対応）
- **S20**: **上位足・エントリー足ごとに根拠チェック行を追加**（グランビル/RCI短期中期長期/MACD/ラウンドナンバー/ロールリバーサル、各時間足で独立）

#### 機能詳細
- **ペア選択**: ツールバーの「通貨ペア選択」ドロップダウンでペアを選択。存在しなければ自動作成、存在すれば前回選択した上位足・エントリー足が復元される。選択中のペアID は localStorage（`mochipoyo_market_view_selected`）に保存
- **カスタムペア**: 「その他（自由入力）」を選ぶと入力欄が表示。入力値を確定すると登録・選択される
- **上位足・エントリー足の選択（セッション19で新規）**: カード上の2つのプルダウン
  - **上位足**: 週足 / 日足 / 4時間足 / 1時間足 から選択
  - **エントリー足**: 5分足 / 15分足 / 1時間足 / 4時間足 から選択
  - 選択するとペア属性（`tfHigher` / `tfEntry`）として即保存
- **根拠チェック（セッション20で新規）**: 各時間足のプルダウン下に7行が並ぶ。上位足とエントリー足で独立した値を保持
  - **グランビル**: 1 / 2 / ❌
  - **RCI**: 短期（上限/下限/❌）、中期（上限/下限/❌）、長期（上限/下限/❌）を3本独立で記録
  - **MACD**: レギュラーダイバージェンス / ヒドゥンダイバージェンス / ❌
  - **ラウンドナンバー**: 有 / 無 / ❌
  - **ロールリバーサル**: 確認 / 未確認 / ❌
  - 選択値は ペア属性（`checksHigher` / `checksEntry`）として即保存。同じボタンを再タップで未選択に戻せる。`MV_TF_CHECKS` 配列に1行足すだけで新項目が両時間足に自動反映される（拡張性）
- **アラート発生足**: カード上に「1H / 4H / D / W」の4つのバッジが常時表示。それぞれ独立にON/OFFをタップ切替、ONにした瞬間の日時を記録
- **判定3択**: 「✅エントリー / ⏸保留 / ⏭スルー」のトグル。判定を新たに設定するたびに `judgeLog` に記録（統計の元データ）
- **チェックリストへのジャンプボタン**: 「📝 チェックリストへ」ボタンで🎯トレード内のチェックセクションにスムーススクロール移動
- **データ独立性**: 専用キー `mochipoyo_market_view_v1` (ペア・S19で `tfHigher` / `tfEntry` 追加・`judgeLog`) + `mochipoyo_market_view_selected` (選択中ペアID) に保存。トレード記録（`mochipoyo_trades_v1`）とCSVには一切影響しない
- **アラート発生足別の判定統計（S18で追加）**: 📚振り返りタブの統計サマリー内に「アラート発生足別の判定統計」セクションを追加。`judgeLog` を時間足ごとに集計し、各時間足で発生したアラート後の ✅エントリー/⏸保留/⏭スルー 件数を表示（同時に複数の足がONだった場合はそれぞれの足のカウントに重複加算）
- **自動マイグレーション**: 
  - S12形式（pairs が文字列配列）→ S14形式（オブジェクト配列＋id・alertTf・judge）：既存スナップショット・履歴保持、watchId 付け替え
  - S14形式（ペア＋時間足ごとの複数カード）→ S17形式（ペア単位）：同名ペアが複数あれば統合、スナップショット履歴は失われない
  - S17形式（単一 alertTf 文字列）→ S18形式（`alerts.{h1,h4,d,w}` オブジェクト）：一致する時間足があれば ON として引き継ぐ（発生日時は不明なため空）
- **拡張性**: `MV_TFS` に1行足せば記録する時間足を追加でき、カード・履歴すべてに自動反映。アラート発生足は `MV_ALERT_TFS` で管理
- ⚠️ **S12〜14で削除された機能**: 記録モーダル、時間足ごとのメモ入力欄、AI分析コピペ連携（プロンプト自動生成・コピー機能）。過去の設計は `docs/CHANGELOG_ARCHIVE.md` を参照

### 8. 3分割エントリーと決済記録（セッション22で追加）

**実運用ルール**（ユーザーの手法）:
- エントリーは**常に3分割**（例：3万円リスクなら1万円×3）。3枚とも**同一価格・同時**エントリー
- 各枚の計画利確は **2:1 / 3:1 / 4:1**。ただし相場次第で RCI上限・下限／直近高値／ラウンドナンバーで早期利確することもある
- **1枚目を利確したら残り2枚の損切りを建値に上げる**（建値で戻されたら RR 0.00 → 既存の `breakeven` 根拠で記録）
- 損切り位置は **直近安値/高値をカバー** か **直近の波のフィボナッチ161%をカバー**

**データモデル:**
- トレードに `slBasis`（損切り根拠）と `splitCount`（新規は3、旧記録は `undefined`）を追加
- `exits[]` の行に `slot`（1〜3）を追加。**slot 付き = 実玉、slot なし = 参考到達ポイント**
- `actual` は **radio → checkbox** になり、実玉では最大3つ同時に true になる
- **合成RR = 3枚のRRの平均**（`tradeNetRR()`）。3枚すべて決済し終えたものだけ `net` を返し、進行中は `null`

⚠️ **レガシー互換が最重要仕様**：判定は `splitCount` ではなく **slot の有無**（`isSplitTrade()`）で行う。`doSave()` は編集時にトレードを丸ごと作り直すため、`splitCount: existing ? existing.splitCount : SPLIT_COUNT` として**旧記録を編集しただけで分割モデル扱いにしない**。`tradeNetRR()` のレガシー経路は現行の `exits.find(x => x.actual)` と同一式を保つ（`filter` の平均に変えると数値が動く）。旧記録は利確モーダルでも**従来の行リストUIのまま**開く。

**決済モーダル（`💰 決済`）:**
- 上部に **合成RR と「決済済 n/3枚」**をライブ表示（`#exitSum`・sticky）
- **1枚目/2枚目/3枚目の固定3ブロック**。各ブロックに 決済済チェック / 計画価格のヒント / **「計画通り」ボタン**（計画価格を流し込む）/ 根拠 / 価格 / RR
- ショートカット：**「残りを建値決済」**（未決済枚に breakeven + エントリー価格）、**「全損」**（未決済枚に stoploss + 損切り価格）
- 下部に従来どおりの **参考到達ポイント**（自由追加・削除可）
- `exitDraft` は**1本の配列**で index 0〜2 が必ず slot 1〜3、index 3 以降が参考行。この並びを守ることで行削除しても `data-i` がズレない
- **フォーカス保持**：価格の打鍵中は再描画せず、同行の `.rr` と `#exitSum` だけ差し替える

**拡張性**：`SPLIT_TARGETS = [2, 3, 4]` の配列長がそのまま分割数（`SPLIT_COUNT`）になるので、ここを変えれば枚数も計画RRも変わる。

## タブ構成（セッション16で再設計）

| タブ | アイコン | 機能 |
|------|---------|------|
| トレード | 🎯 | **2ステップフロー画面**（セッション21でチェックリスト廃止により3→2に）：① 環境認識ボード → ② トレード記録フォーム。「📝 記録フォームへ」ボタンでステップ間をスムーススクロール移動。背後のスクロール位置はそのまま保持される |
| 振り返り | 📚 | **【新】統計＋履歴の統合画面**：上部に統計タイル群、下部にトレード履歴カード。区分フィルタ（全体/検証/リアル）が1つに統一され、切替すると統計と履歴が同時に絞り込まれる（セッション16で履歴・統計パネルを合併、フィルタを一元化） |
| 設定 | ⚙ | CSV エクスポート/インポート、その他設定 |
| 解説ドロワー | 📖 | **右スライドイン式**：タブバーから独立し、右下の📖ボタンまたは**環境認識ボードの根拠チェック行のラベル（グランビル・MACD）**からスライドイン（セッション21でジャンプ元をチェックリストからボードへ移設）。背後のスクロール位置が保たれるため、確認しながら作業を続けられる |

## カラーパレット

dataviz スキル準拠。ライト/ダーク両モード対応。

- **ダークモード**: surface #1a1a19 / accent #3987e5 / good #0ca30c / bad #e66767
- **ライトモード**: surface #fcfcfb / accent #2a78d6 / good #006300 / bad #d03b3b

## 実装状態

**✅ 本番使用可能** — セッション22で3分割エントリー対応を完了。Playwright による E2E 3スイート（機能18項目 / レガシー互換・CSV往復28項目 / レスポンシブ・フォーカス・テーマ21項目）すべて通過、JSエラーなし。純ロジックの単体検証13項目も通過（合成RR・計画ターゲット・レガシー同値性）。

### セッション履歴

| セッション | 主な変更 | 日付 |
|-----------|--------|------|
| 1-13 | 初期実装～解説タブ、RR分析、判定3択化 | 2026-07-xx〜08-13 |
| 14-15 | 環境ボード直接編集化、UI調整 | 2026-08-07〜13 |
| 16 | タブ6→3統合、ステップフロー、ドロワー化 | 2026-08-13 |
| 17 | 環境ボード単一ペア選択式、ペアごとデータ復元 | 2026-08-14 |
| 18 | 複数時間足アラート、判定統計、銘柄22拡張、プロジェクト名刷新 | 2026-08-14〜15 |
| 19 | 環境ボード仕様変更（上位足・エントリー足プルダウン選択）、EMA画像追加 | 2026-08-20 |
| 20 | 環境ボード内に根拠チェック項目を追加（グランビル/RCI/MACD/ラウンドナンバー/ロールリバーサル） | 2026-08-22 |
| 21 | チェックリスト15項目を全廃、フォームの重複7項目を削除しボード連動化、mvChecks スナップショット導入、CSV刷新 | 2026-08-23 |
| 22 | 3分割エントリー対応（建玉ブロック・計画ターゲット自動計算・決済モーダル3枚スロット化・合成RR・枚目別/損切り根拠別の成績分析） | 2026-08-23 |

詳細ログは `docs/SESSIONS_14_TO_18_ARCHIVE.md` および `docs/CHANGELOG_ARCHIVE.md` を参照してください。セッション19以降は `memory/sessions/` 内に個別ファイルで記録。

## 開発上の注意点

### 編集時の確認項目
- JS 構文チェック: `node -e "const fs=require('fs');const html=fs.readFileSync('index.html','utf8');const m=html.match(/<script>([\s\S]*)<\/script>/);new Function(m[1]);console.log('OK')"`
- **セッション21で検証済み**: CSVラウンドトリップ（`mvChecks` の往復、`❌`値、引用符・カンマを含むメモ）、旧CSV（`check1〜15`列）の import、旧 `checks` データのマイグレーション、編集時のボード由来項目の凍結
- **次のテスト推奨項目**:
  - 複数トレードでの統計検証（平均RR・勝率・根拠別ランキング）
  - エッジケース（空履歴、大量データ、localStorage 容量上限）
- スマホレスポンシブ確認（タブバーのセーフエリア対応、notch対応、下部パディング100px）
- 環境ボードの根拠チェック行がスマホ幅で折り返さないか確認

### CSV列構成（セッション21更新）
`id`, `tradeType`, `alertPair`, `alertTf`, `result`, `entryPattern`, `datetime`, `exitDatetime`, `pair`, `tfHigher`, `tfEntry`, `direction`, `exitResult`, `notes`, `entryPrice`, `slPrice`, `slBasis`, `splitCount`, `exits`, `watchId`, `hi_*` × 7, `en_*` × 7, `createdAt`

⚠️ export は**位置配列**なので、`CSV_HEADERS` と export の並びを必ず同時に直すこと（ズレると全列が破壊される）。

根拠チェック列は `MV_CHECK_SNAPSHOTS`（`hi` = 上位足 / `en` = エントリー足）× `MV_TF_CHECKS` から自動生成される（`CSV_BASIS_COLUMNS`）。`MV_TF_CHECKS` に項目を1行足せばCSV列も自動で増える。

互換性：
- セッション22追加：`slBasis` / `splitCount` 列を追加。`exits` の JSON に `slot` が入る。旧CSV（新列なし）を import すると `slBasis=''` / `splitCount=undefined` となりレガシー集計に落ちる。`parseExitsCsv()` は `slot` が 1〜3 のときだけキーを付ける（`null` を入れると実玉扱いになるため）
- セッション21変更：`check1〜check15` 列を廃止。旧CSVを import しても未知の列として無視される（`checks` は復活しない）。`hi_*`/`en_*` 列が1つも無いCSVは `mvChecks = null` となり、履歴で根拠行を表示しない
- セッション9以前の形式（`entryPrice`/`slPrice`/`exits` なし）：import 時に `entryPrice=''`, `slPrice=''`, `exits=[]` を自動設定
- セッション3以降の形式（`tradeType`/`alertPair` なし）：import 時に `tradeType='real'` を自動設定
- セッション2以前の形式（削除済みフィールド）：不明な列を自動スキップするため可能だが、削除済み情報は復帰しません
- セッション13変更（`result` 値）：'entered', 'hold', 'through' を許容。旧CSV（'hold' なし）も正常に import 可能

セッション12（環境認識ボード新規実装）・セッション13（判定3択化・構造刷新）の詳細は `docs/CHANGELOG_ARCHIVE.md` を参照。**環境認識ボードの現行仕様は本ファイル冒頭「7. 環境認識ボード」の記載が最新**（セッション14で刷新済み）。

### 実装済み機能（セッション21終了時点）
- ✅ グランビルの法則 / MACD（画像埋め込み＋📖ジャンプリンク、ジャンプ元は環境ボードの根拠チェック行）
- ✅ 利確ポイント複数記録＆RR自動計算
- ✅ 根拠別RR比率分析（統計タブでランキング表示）
- ✅ CSV完全対応（RRデータ・`mvChecks` の往復、hold値対応、旧CSV互換）
- ✅ 記録の後日編集機能（ボード由来項目は当時の値で凍結）
- ✅ 判定の3択化（エントリー/保留/スルー）— 環境認識ボードが唯一の入力元
- ✅ 環境認識ボード（ペア単位の管理、上位足・エントリー足プルダウン、根拠チェック7項目×2、複数足アラート、判定3択）
- ✅ ボード → 記録フォームのデータ連動（`syncFormFromMarket()` / `marketFieldsFromBoard()`）

### 今後の拡張候補（優先度順）
- 🖼 **ファイルサイズ最適化**（優先度：中） → TinyPNG、WebP化、SVG化など。既存画像を圧縮（現在1.82MB → 1.5MB程度に短縮可能）。インジケーター画像追加前の前提条件として推奨
- 📈 **他のインジケーターへの画像＆ジャンプリンク追加**（優先度：中） → RCI、上位足MACD、トレンド/レンジ認識など。最適化後なら余裕あり
- 🔗 **記録タブと環境認識の連携**（優先度：低） → エントリー時に環境ボード見立てをワンタップで引用
- 📊 **期間フィルター**（優先度：低） → 統計画面で日付範囲指定して再計算
- 🏷 **タグ・カテゴリ**（優先度：低） → 手法ごとの分類集計
- 📱 **PWA化**（優先度：低） → Web App Manifest 追加、Service Worker 強化

### 既知の制限
- ブラウザの localStorage 容量上限（通常 5-10MB、数千件のトレード記録で注意）
- 環境認識ボードはCSVエクスポート非対応（トレード記録とは独立したデータストア）

## URL

| 環境 | URL |
|------|-----|
| jsDelivr CDN（推奨） | https://cdn.jsdelivr.net/gh/jiangchengban-art/fx-entry-checklist@master/index.html |
| GitHub Pages | https://jiangchengban-art.github.io/fx-entry-checklist/ |
| GitHub 本体 | https://github.com/jiangchengban-art/fx-entry-checklist |

## ローカル実行

```bash
cd fx-entry-checklist
python -m http.server 8000
# http://localhost:8000 で開く
```
