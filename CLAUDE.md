# fx-entry-checklist プロジェクト

もちぽよアラート式 FXエントリーチェックリスト＆トレード記録ツール

## プロジェクト概要

- **ファイル**: `index.html` （単一ファイル、CDN依存なし、完全オフライン動作）
- **技術スタック**: HTML5 + CSS3 + Vanilla JavaScript
- **データ永続化**: localStorage (キー: `mochipoyo_trades_v1`, `mochipoyo_market_view_v1`, `mochipoyo_theme`)
- **独立性**: FX Trade Tracker とは完全に分離（別の localStorage キーを使用）

## ユースケース

FX トレーダーがエントリー前に15項目のチェックリストで根拠を確認（OK/NG選択） → トレード記録フォームで詳細を入力 → 過去の履歴を参照して統計分析

## 機能リスト

### 1. エントリーチェックリスト（15項目、OK/NG選択式、セッション7で順序変更、セッション8でグランビルを追加、セッション9でMACD画像を追加）
グループ別アコーディオン表示（セッション7でグループ順序を変更、デフォルト状態を「閉じた」に変更）

**エントリー足（6項目）** ⭐ セッション7で最上位に移行：
- EMA確認 / **グランビルの法則** (セッション8で改名、ジャンプリンク付き📖) / **MACD確認** (セッション9で画像＆ジャンプリンク追加📖) / RCI確認 / ロールリバーサル / ラウンドナンバー

**上位足（7項目）:**
- 環境認識 / ボラティリティ / 上位足トレンド / 上位足値動き / 上位足RCI / 上位足MACD / 上位足高安ライン

**その他（2項目）:**
- 損切り位置 / マインド

選択方式：各項目に「OK」「NG」ボタン（トグル式、未選択も可能）
- 保存前警告：NG項目がある場合のみ表示
- 進捗表示：「OK n/合計（NG n）」の形式
- チェック状態をスナップショットとして記録に含める

### 2. エントリーパターン
トレード記録フォーム最初の必須項目。4パターン選択：
- 15分足×4時間足
- 1時間足×日足
- 4時間足(8時間足)×週足
- 日足×週足(月足)

### 3. トレード記録フォーム
**セッション6での構成（新フィールド）:**
- **必須**: 区分（検証/リアル）、アラート発生通貨ペア、アラート発生時間足、判定（エントリー/保留/スルー、セッション13で保留追加）
- **必須**: エントリーパターン、エントリー日時、通貨ペア（主要8ペア + 自由入力）、上位足、エントリー足
- **条件付き必須**: 売買方向（エントリー時のみ必須、保留・スルーの場合は不要）
- **任意**: エグジット日時、振り返り・備考
- ※ セッション3変更：スタイル・エントリー価格・決済価格・損切り価格・pips は削除
- ※ セッション6追加：区分、アラート発生フィールド、エントリー/スルー判定
- ※ セッション13追加：判定に「保留」を追加（エントリー/保留/スルーの3択）

### 4. 履歴一覧（セッション11で編集機能追加）
- 新しい順のカード表示、方向別色分け、NG項目をバッジで表示
- ※ セッション6追加：区分フィルタ（全体/検証/リアル）、区分バッジ、判定バッジ、アラート情報表示
- ※ セッション10追加：**「💰 利確」ボタン**（エントリー記録にのみ表示）で後日利確ポイントを記録
- ※ セッション10追加：**RR表示**（実決済ポイントに◎付きで「根拠 RR +X.XX」形式で表示）
- ※ セッション11追加：**「✎ 編集」ボタン**で記録フォーム全フィールドを復元・編集・更新保存可能

### 5. 統計サマリー（タブ化、セッション10更新）
- 下部タブバー「📊統計」として独立
- トレード数、エントリー数、保留数、スルー数、チェック遵守率を表示
- ※ セッション3変更：勝率・合計pips・平均pips は削除（pips入力欄廃止に伴い）
- ※ セッション6変更：区分フィルタ（全体/検証/リアル）を追加、エントリー数/スルー数タイルを追加
- ※ セッション10追加：**平均RR**、**勝率**（RR > 0 の割合）、**最大到達RR**の3タイルを追加
- ※ セッション10追加：**利確根拠別の平均RRランキング**（どの利確ルールが最も期待値高いか比較可能）
- ※ セッション13追加：**保留数**タイルを追加

### 6. 追加機能
- CSV エクスポート/インポート（BOM付きUTF-8、id重複検出、互換性あり）
- ダーク/ライト切替（localStorage保存）
- タブベースUI（6タブ：🌐環境 / ✅チェック / 📝記録する / 📚履歴 / 📊統計 / 📖解説）、画面下部固定ナビ

### 7. 環境認識ボード（セッション12で追加、セッション13で完全リニューアル）

#### 設計の転換（セッション13）
**旧設計（S12）:** 監視「通貨ペア」に週足/日足/4時間足の見立てを記録
**新設計（S13）:** 監視「項目」（通貨ペア＋時間足の組み合わせ）ごとに「判定」（エントリー/保留/スルー）を記録

**背景:** アラート発生は通貨ペア・時間足の組み合わせごとに発生するため、各アラート単位に判定を記録する方が実務的。

#### 機能詳細
- **監視項目管理**: 通貨ペア（プルダウン+カスタム） + 時間足（プルダウン：主要6足）で登録。同じペアでも時間足が異なれば別項目として追加可
- **判定3択**: 各カードに「✅エントリー / ⏸保留 / ⏭スルー」のトグル（再クリックで未選択に戻せる）。トレード記録フォームの判定と独立
- **状態選択**: モーダルで各時間足に「▲上昇 / ▬レンジ / ▼下降」の3択トグル（再クリックで未選択に戻せる）＋任意メモ
- **履歴保持**: 「記録する」のたびにスナップショットを追加保存。最新をカード表示し、過去分は `<details>` で折りたたみ表示（個別削除可）
- **前回値の引き継ぎ**: モーダルを開くと前回の見立てが初期値として入るため、変わった項目だけ編集すればよい
- **AI分析（コピペ連携方式）**:
  - 「📋 分析プロンプトをコピー」で、**自分の見立てを織り込んだ依頼文**を自動生成しクリップボードへ
  - プロンプトは AI に追認ではなく「私の見立てと異なる点の指摘」を求める設計
  - チャート画像を添えて Claude / ChatGPT に渡し、返答をテキストエリアに貼り付けて保存
  - ⚠️ **API直結は採用しない**：APIキーのブラウザ保存が必要になり「CDN依存なし・完全オフライン」方針と衝突するため
- **データ独立性**: 専用キー `mochipoyo_market_view_v1` に保存。トレード記録（`mochipoyo_trades_v1`）とCSVには一切影響しない
- **旧形式自動移行**: セッション12形式（pairs が文字列配列）を検出したら、自動的に新形式（オブジェクト配列＋id・alertTf・judge）に変換。既存スナップショット・履歴も保持
- **拡張性**: `MV_TFS` に1行足せば記録する時間足を追加でき、入力欄・表示・履歴すべてに自動反映

## タブ構成

| タブ | アイコン | 機能 |
|------|---------|------|
| 環境 | 🌐 | 環境認識ボード（監視ペアの週足/日足/4時間足のトレンド・レンジ記録、履歴保持、AI分析コピペ連携） |
| チェック | ✅ | 15項目チェックリスト（グループアコーディオン） |
| 記録する | 📝 | トレード記録フォーム |
| 履歴 | 📚 | 過去のトレード記録一覧 |
| 統計 | 📊 | 統計サマリー |
| 解説 | 📖 | インジケーター・手法の解説（`INDICATOR_INFO` データ駆動、種類別アコーディオン。各項目「この手法での確認ポイント」→「基礎知識」の二段表示。セッション8でグランビル、セッション9でMACD画像を追加、12項目に＆ジャンプリンク対応） |

## カラーパレット

dataviz スキル準拠。ライト/ダーク両モード対応。

- **ダークモード**: surface #1a1a19 / accent #3987e5 / good #0ca30c / bad #e66767
- **ライトモード**: surface #fcfcfb / accent #2a78d6 / good #006300 / bad #d03b3b

## 実装状態
**✅ 本番使用可能** | セッション4で全機能検証済み、セッション6で検証/リアル区分機能追加、セッション7でUI調整（グループ順序・アコーディオン状態変更）、セッション8でグランビルの法則実装（画像埋め込み＋ジャンプリンク機能）、セッション9でMACD画像＆ジャンプリンク実装完了、セッション10で利確ポイント記録＆RR比率分析機能実装完了、セッション11で記録編集機能実装完了、セッション12で環境認識ボード実装完了、**セッション13で判定3択化・環境認識ボード完全リニューアル実装完了**

### セッション3の機能変更・追加（2026-07-27～28）
- 🔄 記録フォーム変更：スタイル削除、価格フィールド削除、エグジット日時追加、上位足/エントリー足をプルダウン化
- ✨ 解説タブ追加：11項目のインジケーター・手法説明（INDICATOR_INFO データ駆動、4グループ別アコーディオン）

### セッション4の検証（2026-07-28）
- ✅ ブラウザ実動作確認（edge-case-test.mjs, browser-test.mjs）
- ✅ 解説タブ内容確認（11項目すべて正確）
- ✅ エッジケーステスト（空履歴、全NG、大量データ）
- ✅ 全機能が完全に動作

### セッション6の機能追加（2026-07-29）
- ✨ **アラート発生フィールド**: 通貨ペア + 時間足（フォーム最上部）
- ✨ **エントリー/スルー判定トグル**: ✅エントリー / ⏭スルー （売買方向を条件付き必須化）
- ✨ **検証/リアル区分機能**: 🔬検証 / 💰リアル トグルを追加
  - フォーム冒頭に配置（必須）
  - 履歴タブに区分フィルタ追加（全体/検証/リアル）
  - 統計タブに区分フィルタ追加、エントリー数/スルー数タイルを追加
- 🔄 CSV に `tradeType` 列を追加、互換性維持
- JS構文チェック完了、フィルタ機能検証完了

### セッション7の UI 調整（2026-08-02）
- 🔄 **チェックリストグループ順序**: 「エントリー足」を最上位に配置
  - 変更前：上位足 → エントリー足 → その他
  - 変更後：エントリー足 → 上位足 → その他
  - 理由：FX トレーディングの実務流では、まずエントリー足の確認が優先
- 🔄 **アコーディオンデフォルト状態**: すべてのグループをデフォルトで閉じた状態に変更
  - 理由：ユーザーが自分の確認順序に応じて必要なグループだけを展開できるように改善

### セッション8のグランビルの法則実装（2026-08-02）
- ✨ **グランビルの法則の追加**
  - チェック項目「エントリー足トレンド勢い」を「グランビルの法則」に名称変更
  - 解説タブに図解（買①〜④・売①〜④のパターン図、base64画像埋め込み）と解説文を追加
  - チェックタブの項目名クリック → 解説タブの該当セクションへ自動ジャンプ＆スクロール（グループを自動展開）
  - 解説タブに「← チェックに戻る」ボタンを追加
  - 画像は `assets/granville.png` を元に base64 化し `IMG_GRANVILLE` 定数として index.html 内に埋め込み（CDN依存なしの方針を維持）
  - ⚠️ index.html のファイルサイズが 65KB → 約1.6MB に増加（画像embedded分）
  - ブラウザ検証完了（Playwright で全機能テスト済み）

### セッション9のMACD画像＆ジャンプリンク実装（2026-08-03）
- ✨ **MACDにダイバージェンス解説画像を追加**
  - 画像: `assets/S__31744006.jpg`（レギュラー・ダイバージェンス vs ヒドゥン・ダイバージェンス比較図）
    - 左側：トレンド転換の予兆（価格が高値更新 ⇔ オシレーター切り下がり等）
    - 右側：トレンド継続の根拠（価格の押し目・戻り ⇔ オシレーター逆行）
  - base64 化して `IMG_MACD_DIVERGENCE` 定数として index.html 内に埋め込み
  - 解説文を図の内容に合わせて拡充（上昇/下降トレンドでの高値/安値注目ポイントを明記）
- ✨ **チェックタブ「MACD確認」のジャンプリンク化**
  - 「MACD確認」がクリック可能なリンクボタン化（📖アイコン付き）
  - グランビルと同じパターンで実装：CHECK_ITEMS に `link: 'macd'`、INDICATOR_INFO に `id: 'macd'` を追加
  - 解説タブへの自動切替＆スクロール＆グループ展開
  - 「← チェックに戻る」ボタンで戻り可能
- ⚠️ index.html のファイルサイズが 約1.6MB → 約1.86MB に増加（MACD画像追加分、184KB）
- JS構文チェック完了

### セッション10の利確ポイント記録＆RR比率分析実装（2026-08-03～04 前半）
- ✨ **利確記録モーダル（新規）**
  - トリガー: 履歴カード右の「💰 利確」ボタン（result === 'entered' のみ表示）
  - 入力: エントリー価格 + 損切り価格 → リスク幅自動表示
  - 利確ポイント複数追加可能（根拠プルダウン + 価格テキスト）
  - 価格変更時にRRが即座に自動計算・表示
  - `◎` ラジオで「実際に決済したポイント」を1つ指定
  - 保存時に価格未入力行は自動削除
- ✨ **RRの自動計算（符号付き）**
  - `RR = (到達価格 − エントリー価格) × 向き ÷ |エントリー − 損切り|`
  - ショートは符号反転：損切り=-1.00 / 建値=0.00 / 利確=+X.XX
  - `calcRR(entryPrice, slPrice, exitPrice, direction)` 関数で実装
- ✨ **EXIT_BASIS 定数**（利確根拠10選択肢）
  - RCI上限/下限到達、直近高値/安値到達、ラウンドナンバー、EMAタッチ、MACDクロス、建値決済、損切り、その他
  - 将来の根拠追加は1行足すだけで選択肢・統計集計に自動反映
- ✨ **統計タブ拡張**
  - **新規タイル3つ**: 平均RR（実決済ポイントの平均）、勝率（RR > 0 の割合 %）、最大到達RR（1トレード内での最大の平均）
  - **新規セクション**: 利確根拠別の平均RRランキング（根拠ごとに平均RRを算出、期待値高い順ソート）
- ✨ **履歴カード更新**
  - 利確表示: 「◎ RCI上限到達 RR +1.00 ／ 直近高値到達 RR +2.00」形式
  - 正RR → 緑色（good）、負RR → 赤色（bad）で色分け
  - 実決済フラグ関係なく全ポイント到達距離で根拠別平均を計算 → 「持っていたらどこまで伸びたか」を根拠別に比較可能
- ✨ **CSV 完全対応**
  - 新規列: `entryPrice`, `slPrice`, `exits`（JSON形式で1列格納）
  - エクスポート: `JSON.stringify()` で配列をシリアライズ
  - インポート: `parseExitsCsv()` で復元（型チェック・エラーハンドリング付き）
  - 後方互換性: セッション9以前のCSV/データもそのままインポート可能（exits は `[]` で初期化）
- 🔧 **ソート順序改善** 
  - 同分記録での順序不定問題を解決（`createdAt` でフォールバック）
- ⚠️ index.html のファイルサイズが 約1.86MB → 約1.87MB に増加（JS追加分、±10KB、画像なし）
- ✅ **ブラウザテスト**: Playwright で17項目全てPASS（JS構文チェック: OK）

### セッション11の記録編集機能実装（2026-08-04）
- ✨ **「✎ 編集」ボタン追加**：履歴カード右側に配置、すべての記録で編集可能
- ✨ **フォーム全フィールド復元**：editingId で判定、既存記録データをすべてのフィールドに復元
  - チェックリスト状態（OK/NG）も復元（`applyCheckStates()` 関数）
  - カスタム入力フィールド（アラート通貨ペア・通貨ペア）にも対応
- ✨ **編集モード表示**：見出しに「✎ 編集中」バッジ、保存ボタン「💾 更新を保存」、「編集をキャンセル」ボタン
- ✨ **データ保全性**：既存記録の id, createdAt, entryPrice, slPrice, exits（利確データ）を上書きしない
- ✨ **キャンセル処理**：「編集をキャンセル」でフォームリセット、編集モード終了
- ⚠️ index.html のファイルサイズが 約1.87MB → 約1.875MB に増加（JS追加分、±5KB、画像なし）
- ✅ **ブラウザテスト**: Playwright で7項目全てPASS（記録保存→編集→更新→反映確認含む）

## 開発上の注意点

### 編集時の確認項目
- JS 構文チェック: `node -e "const fs=require('fs');const html=fs.readFileSync('index.html','utf8');const m=html.match(/<script>([\s\S]*)<\/script>/);new Function(m[1]);console.log('OK')"`
- **次のテスト推奨項目**（メモリ参照）:
  - CSV ラウンドトリップテスト（OK/NG選択値の往復確認）
  - 複数トレードでの統計検証
  - エッジケース（空履歴、全NG、大量データ）
- スマホレスポンシブ確認（タブバーのセーフエリア対応、notch対応、下部パディング100px）
- OK/NG状態の視覚的な区別確認（緑/赤背景）

### CSV列構成（セッション13更新）
`id`, `tradeType`, `alertPair`, `alertTf`, `result`, `entryPattern`, `datetime`, `exitDatetime`, `pair`, `tfHigher`, `tfEntry`, `direction`, `exitResult`, `notes`, `entryPrice`, `slPrice`, `exits`, `check1～check15` (ok/ng/空欄), `createdAt`

互換性：
- セッション9以前の形式（`entryPrice`/`slPrice`/`exits` なし）：import 時に `entryPrice=''`, `slPrice=''`, `exits=[]` を自動設定
- セッション3以降の形式（`tradeType`/`alertPair` なし）：import 時に `tradeType='real'` を自動設定
- セッション2以前の形式（削除済みフィールド）：不明な列を自動スキップするため可能だが、削除済み情報は復帰しません
- セッション13変更（`result` 値）：'entered', 'hold', 'through' を許容。旧CSV（'hold' なし）も正常に import 可能

### セッション12の環境認識ボード実装（2026-08-05）
- ✨ **環境認識ボード（🌐環境タブ）**: 監視ペアごとに週足/日足/4時間足のトレンド・レンジを記録
  - 監視ペア管理（主要8ペア＋自由入力、監視解除は履歴ごと削除）
  - 各時間足に「▲上昇 / ▬レンジ / ▼下降」の3択トグル（再クリックで未選択に戻せる）＋メモ
  - 更新のたびスナップショット保存、過去の見立てを履歴として保持
  - モーダル再度開くと前回の見立てが初期値として引き継がれる
- ✨ **AI分析コピペ連携**：自分の見立てを織り込んだプロンプト自動生成、clipboard API対応
- ✨ **データ独立性**：`mochipoyo_market_view_v1` キーでトレード記録と分離、CSV対象外
- ✨ **拡張設計**：`MV_TFS` に1行足すで時間足追加可能、全UI自動対応
- ⚠️ index.html のファイルサイズが 約1.875MB → 約1.887MB に増加（JS追加分、±12KB、画像なし）
- ✅ **ブラウザテスト**: Playwright で36項目全てPASS

### セッション13の判定3択化・環境認識ボード完全リニューアル（2026-08-05）
- ✨ **判定の3択化**
  - トレード記録フォーム：「✅エントリー / ⏸保留 / ⏭スルー」の3択トグル
  - 保留時は売買方向・利確欄が非表示・任意扱い（エントリー時のみ必須）
  - CSV import で 'hold' 値を認識・復元
- ✨ **統計タブに「保留数」タイル追加**
- ✨ **環境認識ボード完全リニューアル**（設計の転換）
  - **旧設計**：監視「通貨ペア」に週足/日足/4時間足の見立てを記録
  - **新設計**：監視「項目」（通貨ペア＋時間足の組み合わせ）ごとに「判定」（エントリー/保留/スルー）を記録
  - **入力**: 通貨ペア＋時間足を選択して「追加する」。同じペアでも時間足が異なれば別項目として追加可
  - **判定3択**: 各カードに「✅エントリー / ⏸保留 / ⏭スルー」のトグル（再クリックで未選択に戻す）
  - **旧形式自動移行**: セッション12形式（pairs が文字列配列）を自動検出・変換、既存スナップショット・履歴も保持
- ⚠️ index.html のファイルサイズが 約1.887MB → 約1.89MB に増加（JS追加分、±3KB、画像なし）
- ✅ **ブラウザテスト**: 追加/重複チェック/判定トグル/記録保存/削除/旧形式移行 全PASS

### 拡張計画（セッション1～13で実装完了した機能）
実装済み（セッション13終了時点）：
- ✅ グランビルの法則（画像埋め込み＋ジャンプリンク）
- ✅ MACD（ダイバージェンス画像埋め込み＋ジャンプリンク）
- ✅ チェック→解説タブ間のジャンプナビゲーション（2項目対応）
- ✅ 利確ポイント複数記録＆RR自動計算
- ✅ 根拠別RR比率分析（統計タブでランキング表示）
- ✅ CSV完全対応（RRデータの往復、hold値対応）
- ✅ 記録の後日編集機能（全フィールド編集・保存）
- ✅ 判定の3択化（エントリー/保留/スルー）
- ✅ **環境認識ボード完全リニューアル**（アラート単位の管理、判定3択記録、旧形式自動移行）

今後の拡張候補（優先度順）：
- 🖼 **ファイルサイズ最適化**（優先度：中） → TinyPNG、WebP化、SVG化など。既存画像を圧縮（現在1.89MB → 1.5MB程度に短縮可能）。インジケーター画像追加前の前提条件として推奨
- 📈 **他のインジケーターへの画像＆ジャンプリンク追加**（優先度：中） → RCI、上位足MACD、トレンド/レンジ認識など。最適化後なら余裕あり
- 🔗 **記録タブと環境認識の連携**（優先度：低） → エントリー時に環境ボード見立てをワンタップで引用
- 📊 **期間フィルター**（優先度：低） → 統計画面で日付範囲指定して再計算
- 🏷 **タグ・カテゴリ**（優先度：低） → 手法ごとの分類集計
- 📱 **PWA化**（優先度：低） → Web App Manifest 追加、Service Worker 強化

### 既知の制限
- ブラウザの localStorage 容量上限（通常 5-10MB、数千件のトレード記録で注意）
- 環境認識ボードはCSVエクスポート非対応（トレード記録とは独立したデータストア）

## URL

| 環境 | URL |
|------|-----|
| jsDelivr CDN（推奨） | https://cdn.jsdelivr.net/gh/jiangchengban-art/fx-entry-checklist@master/index.html |
| GitHub Pages | https://jiangchengban-art.github.io/fx-entry-checklist/ |
| GitHub 本体 | https://github.com/jiangchengban-art/fx-entry-checklist |

## ローカル実行

```bash
cd fx-entry-checklist
python -m http.server 8000
# http://localhost:8000 で開く
```
