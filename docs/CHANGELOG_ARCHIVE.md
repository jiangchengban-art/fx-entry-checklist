# セッション詳細ログ・アーカイブ

`CLAUDE.md` を簡潔に保つため、セッションごとの詳細な変更ログ（実装内容・検証結果・ファイルサイズ推移など）をここに移行しています。**削除ではなく移行**です。現在の実装状態のサマリーは `CLAUDE.md` を参照してください。

対応する詳細メモ（設計判断の背景・却下案など）は `~/.claude/projects/.../memory/sessions/` にも別途保存されています。

---

## セッション3の機能変更・追加（2026-07-27～28）
- 🔄 記録フォーム変更：スタイル削除、価格フィールド削除、エグジット日時追加、上位足/エントリー足をプルダウン化
- ✨ 解説タブ追加：11項目のインジケーター・手法説明（INDICATOR_INFO データ駆動、4グループ別アコーディオン）

## セッション4の検証（2026-07-28）
- ✅ ブラウザ実動作確認（edge-case-test.mjs, browser-test.mjs）
- ✅ 解説タブ内容確認（11項目すべて正確）
- ✅ エッジケーステスト（空履歴、全NG、大量データ）
- ✅ 全機能が完全に動作

## セッション6の機能追加（2026-07-29）
- ✨ **アラート発生フィールド**: 通貨ペア + 時間足（フォーム最上部）
- ✨ **エントリー/スルー判定トグル**: ✅エントリー / ⏭スルー （売買方向を条件付き必須化）
- ✨ **検証/リアル区分機能**: 🔬検証 / 💰リアル トグルを追加
  - フォーム冒頭に配置（必須）
  - 履歴タブに区分フィルタ追加（全体/検証/リアル）
  - 統計タブに区分フィルタ追加、エントリー数/スルー数タイルを追加
- 🔄 CSV に `tradeType` 列を追加、互換性維持
- JS構文チェック完了、フィルタ機能検証完了

## セッション7の UI 調整（2026-08-02）
- 🔄 **チェックリストグループ順序**: 「エントリー足」を最上位に配置
  - 変更前：上位足 → エントリー足 → その他
  - 変更後：エントリー足 → 上位足 → その他
  - 理由：FX トレーディングの実務流では、まずエントリー足の確認が優先
- 🔄 **アコーディオンデフォルト状態**: すべてのグループをデフォルトで閉じた状態に変更
  - 理由：ユーザーが自分の確認順序に応じて必要なグループだけを展開できるように改善

## セッション8のグランビルの法則実装（2026-08-02）
- ✨ **グランビルの法則の追加**
  - チェック項目「エントリー足トレンド勢い」を「グランビルの法則」に名称変更
  - 解説タブに図解（買①〜④・売①〜④のパターン図、base64画像埋め込み）と解説文を追加
  - チェックタブの項目名クリック → 解説タブの該当セクションへ自動ジャンプ＆スクロール（グループを自動展開）
  - 解説タブに「← チェックに戻る」ボタンを追加
  - 画像は `assets/granville.png` を元に base64 化し `IMG_GRANVILLE` 定数として index.html 内に埋め込み（CDN依存なしの方針を維持）
  - ⚠️ index.html のファイルサイズが 65KB → 約1.6MB に増加（画像embedded分）
  - ブラウザ検証完了（Playwright で全機能テスト済み）

## セッション9のMACD画像＆ジャンプリンク実装（2026-08-03）
- ✨ **MACDにダイバージェンス解説画像を追加**
  - 画像: `assets/S__31744006.jpg`（レギュラー・ダイバージェンス vs ヒドゥン・ダイバージェンス比較図）
    - 左側：トレンド転換の予兆（価格が高値更新 ⇔ オシレーター切り下がり等）
    - 右側：トレンド継続の根拠（価格の押し目・戻り ⇔ オシレーター逆行）
  - base64 化して `IMG_MACD_DIVERGENCE` 定数として index.html 内に埋め込み
  - 解説文を図の内容に合わせて拡充（上昇/下降トレンドでの高値/安値注目ポイントを明記）
- ✨ **チェックタブ「MACD確認」のジャンプリンク化**
  - 「MACD確認」がクリック可能なリンクボタン化（📖アイコン付き）
  - グランビルと同じパターンで実装：CHECK_ITEMS に `link: 'macd'`、INDICATOR_INFO に `id: 'macd'` を追加
  - 解説タブへの自動切替＆スクロール＆グループ展開
  - 「← チェックに戻る」ボタンで戻り可能
- ⚠️ index.html のファイルサイズが 約1.6MB → 約1.86MB に増加（MACD画像追加分、184KB）
- JS構文チェック完了

## セッション10の利確ポイント記録＆RR比率分析実装（2026-08-03～04 前半）
- ✨ **利確記録モーダル（新規）**
  - トリガー: 履歴カード右の「💰 利確」ボタン（result === 'entered' のみ表示）
  - 入力: エントリー価格 + 損切り価格 → リスク幅自動表示
  - 利確ポイント複数追加可能（根拠プルダウン + 価格テキスト）
  - 価格変更時にRRが即座に自動計算・表示
  - `◎` ラジオで「実際に決済したポイント」を1つ指定
  - 保存時に価格未入力行は自動削除
- ✨ **RRの自動計算（符号付き）**
  - `RR = (到達価格 − エントリー価格) × 向き ÷ |エントリー − 損切り|`
  - ショートは符号反転：損切り=-1.00 / 建値=0.00 / 利確=+X.XX
  - `calcRR(entryPrice, slPrice, exitPrice, direction)` 関数で実装
- ✨ **EXIT_BASIS 定数**（利確根拠10選択肢）
  - RCI上限/下限到達、直近高値/安値到達、ラウンドナンバー、EMAタッチ、MACDクロス、建値決済、損切り、その他
  - 将来の根拠追加は1行足すだけで選択肢・統計集計に自動反映
- ✨ **統計タブ拡張**
  - **新規タイル3つ**: 平均RR（実決済ポイントの平均）、勝率（RR > 0 の割合 %）、最大到達RR（1トレード内での最大の平均）
  - **新規セクション**: 利確根拠別の平均RRランキング（根拠ごとに平均RRを算出、期待値高い順ソート）
- ✨ **履歴カード更新**
  - 利確表示: 「◎ RCI上限到達 RR +1.00 ／ 直近高値到達 RR +2.00」形式
  - 正RR → 緑色（good）、負RR → 赤色（bad）で色分け
  - 実決済フラグ関係なく全ポイント到達距離で根拠別平均を計算 → 「持っていたらどこまで伸びたか」を根拠別に比較可能
- ✨ **CSV 完全対応**
  - 新規列: `entryPrice`, `slPrice`, `exits`（JSON形式で1列格納）
  - エクスポート: `JSON.stringify()` で配列をシリアライズ
  - インポート: `parseExitsCsv()` で復元（型チェック・エラーハンドリング付き）
  - 後方互換性: セッション9以前のCSV/データもそのままインポート可能（exits は `[]` で初期化）
- 🔧 **ソート順序改善**
  - 同分記録での順序不定問題を解決（`createdAt` でフォールバック）
- ⚠️ index.html のファイルサイズが 約1.86MB → 約1.87MB に増加（JS追加分、±10KB、画像なし）
- ✅ **ブラウザテスト**: Playwright で17項目全てPASS（JS構文チェック: OK）

## セッション11の記録編集機能実装（2026-08-04）
- ✨ **「✎ 編集」ボタン追加**：履歴カード右側に配置、すべての記録で編集可能
- ✨ **フォーム全フィールド復元**：editingId で判定、既存記録データをすべてのフィールドに復元
  - チェックリスト状態（OK/NG）も復元（`applyCheckStates()` 関数）
  - カスタム入力フィールド（アラート通貨ペア・通貨ペア）にも対応
- ✨ **編集モード表示**：見出しに「✎ 編集中」バッジ、保存ボタン「💾 更新を保存」、「編集をキャンセル」ボタン
- ✨ **データ保全性**：既存記録の id, createdAt, entryPrice, slPrice, exits（利確データ）を上書きしない
- ✨ **キャンセル処理**：「編集をキャンセル」でフォームリセット、編集モード終了
- ⚠️ index.html のファイルサイズが 約1.87MB → 約1.875MB に増加（JS追加分、±5KB、画像なし）
- ✅ **ブラウザテスト**: Playwright で7項目全てPASS（記録保存→編集→更新→反映確認含む）

## セッション12の環境認識ボード実装（2026-08-05）
- ✨ **環境認識ボード（🌐環境タブ）**: 監視ペアごとに週足/日足/4時間足のトレンド・レンジを記録
  - 監視ペア管理（主要8ペア＋自由入力、監視解除は履歴ごと削除）
  - 各時間足に「▲上昇 / ▬レンジ / ▼下降」の3択トグル（再クリックで未選択に戻せる）＋メモ
  - 更新のたびスナップショット保存、過去の見立てを履歴として保持
  - モーダル再度開くと前回の見立てが初期値として引き継がれる
- ✨ **AI分析コピペ連携**：自分の見立てを織り込んだプロンプト自動生成、clipboard API対応
- ✨ **データ独立性**：`mochipoyo_market_view_v1` キーでトレード記録と分離、CSV対象外
- ✨ **拡張設計**：`MV_TFS` に1行足すで時間足追加可能、全UI自動対応
- ⚠️ index.html のファイルサイズが 約1.875MB → 約1.887MB に増加（JS追加分、±12KB、画像なし）
- ✅ **ブラウザテスト**: Playwright で36項目全てPASS

> ⚠️ **この設計はセッション13・14で大きく変わっています。** モーダルによる記録方式・AI分析コピペ連携は**セッション14で廃止**されました。当時の設計として歴史的に記録しています。

## セッション13の判定3択化・環境認識ボード完全リニューアル（2026-08-05）
- ✨ **判定の3択化**
  - トレード記録フォーム：「✅エントリー / ⏸保留 / ⏭スルー」の3択トグル
  - 保留時は売買方向・利確欄が非表示・任意扱い（エントリー時のみ必須）
  - CSV import で 'hold' 値を認識・復元
- ✨ **統計タブに「保留数」タイル追加**
- ✨ **環境認識ボード完全リニューアル**（設計の転換）
  - **旧設計**：監視「通貨ペア」に週足/日足/4時間足の見立てを記録
  - **新設計**：監視「項目」（通貨ペア＋時間足の組み合わせ）ごとに「判定」（エントリー/保留/スルー）を記録
  - **入力**: 通貨ペア＋時間足を選択して「追加する」。同じペアでも時間足が異なれば別項目として追加可
  - **判定3択**: 各カードに「✅エントリー / ⏸保留 / ⏭スルー」のトグル（再クリックで未選択に戻す）
  - **旧形式自動移行**: セッション12形式（pairs が文字列配列）を自動検出・変換、既存スナップショット・履歴も保持
- ⚠️ index.html のファイルサイズが 約1.887MB → 約1.89MB に増加（JS追加分、±3KB、画像なし）
- ✅ **ブラウザテスト**: 追加/重複チェック/判定トグル/記録保存/削除/旧形式移行 全PASS

> ⚠️ **記録モーダル方式（判定はそのまま）はセッション14で置き換えられました。** 詳細はセッション14の項目を参照。

## セッション14: アラート発生足の記録機能（試作→撤回）と、環境認識ボードの直接編集化（2026-08-07〜08）

このセッションは2段階で進みました。

### 第1段階（試作・後に撤回）
週足/日足/4時間足の状態記録に加えて、「🔔 発生足」という新しい行（日足〜1分足の6時間足を複数選択でON/OFF、記録モーダル内でトグル→スナップショットに保存→カードに点灯表示）を実装。Playwrightで13項目PASSし、いったん完成させた。

### 第2段階（最終形）
実際の画面をユーザーに見せたところ、「もっとシンプルでいい」という判断で設計を転換：

- ✨ **記録モーダルを廃止**し、週足/日足/4時間足の状態ボタン（▲上昇/▬レンジ/▼下降）を**カード上に直接表示**。クリックすると即座に新しいスナップショットとして保存される（トグル式、再クリックで未選択に戻せる）
- ✨ 監視項目追加時の時間足プルダウンを「時間足を選択…」→「**アラート発生足を選択…**」に改名（既存の `alertTf` フィールドの表示名変更のみ、データ構造は無変更）
- 🗑 **削除された機能**（第1段階の🔔発生足トグル、およびセッション12〜13から存在した機能）：
  - 記録モーダル本体（`mvModal`）
  - 時間足ごとのメモ入力欄
  - AI分析コピペ連携機能一式（`buildAiPrompt`、「📋 分析プロンプトをコピー」、AIチャート分析欄）
  - 🔔発生足の複数選択・点灯表示・履歴併記
- ✅ **ブラウザテスト**: Playwright 16/16 PASS（直接クリック編集、即保存、再クリックで解除、モーダル・発生足行・メモ・AI欄の不在確認、履歴/判定/監視解除の継続動作、新旧データ後方互換、375px幅で横スクロールなし）
- ファイルサイズ: 約1.89MB → **約1.824MB**（機能削減により減少）

**学び**: AI分析コピペ連携（セッション12設計）は当時「良い設計」として作り込んだが、実際の画面を見せた結果ユーザーには不要と判断された。次に大きな機能を追加する時は、作り込む前にまず最小構成で確認してもらう方が手戻りが少ない可能性がある。

## セッション28-69の変更履歴（元CLAUDE.mdの表から移設、S80でCLAUDE.md本体を軽量化）

| セッション | 主な変更 | 日付 |
| 69 | 📊 **アラート発生日時をカードに常時表示、アラート〜エントリー経過時間の統計追加**。ユーザー要望「アラートが鳴ってから実際にエントリーするまでの時間を記録として平均値で出したい」「アラートが何月何日の何時に発生したか確認できるようにしたい」に対応。①🔭カードのアラートバッジ下に発生日時を常時表示（例：1H 09/09 21:30）。新規関数 `fmtDatetimeShort()` / `trendAlertTimesHtml()` ②📚統計タブに「アラート〜エントリーの経過時間」タイル。手入力の `manualAlertAt` と `datetime` の差分を自動計算。平均・最短〜最長を表示。母数は判定=エントリーかつ両方入力済み。データモデル・CSV・端末間マージは無変更。sw.js: v38→v39。gitコミット: 2ef1541 S69 | 2026-09-09 |
| 68 | 🧪 **S68準備: s62-test.mjs を S63-S65 の定義変更に対応させる**。セッション63・65での根拠定義変更（上位足の granville/rollReversal/entryFibo 撤去、待ち系撤去、MV_ENTRY_CHECKS を3ステップフロー専用に置換）に対応。①テストの fiboRoll → rollReversal 置換、値「重なり有」→「確認」に修正 ②期待値を現在の実装定義に合致させる：上位足5項目（RCI3+MACD+ラウンド）・エントリー足4項目（❶反転形・❷MA抜け・❸ロールRv・❸Fibo）・パネル行数9行（上位足5+エントリー足4）・確度スコア（weight90ベース、MACD失点で61%）・CSV列5→5の入れ替え ③選択肢の空欄（未選択ラベル）撤去、④待ち系削除確認（レンジテストの期待値から⏳待ち削除）⑤履歴サマリー（`basisSummaryHtml`）の項目名置換（granville→MACD・ラウンド）。新設s62-test.mjs全48項目通過、既存s44/s43/s42全192項目通過。データモデル・CSV・端末間マージ・統計・波マップ・グランビル・根拠パネル・並び順は無変更。sw.js版番号据え置き（テスト修正のみで本体機能変更なし）。gitコミット: 78e82b6 S68準備 | 2026-09-09 |
| 67 | 🔀 **🔭一覧の並び順をエントリー圏距離順→アラート発生時刻順に置換、カード連動、圏内・巡回タイル撤去**。ユーザー要望「1Hと4Hと日足と週足のソートを作成して。通貨カードのアラート鳴った足のタップと連動させる。そして、圏内と巡回は消して」に対応。①ツールバーに並び順切替ボタン（1H/4H/D/W、`MV_ALERT_TFS`を流用、`renderTrendSortToggles()`）を新設。`trendSortTf`（既定`h1`）で選んだ足の`w.alerts[tf].at`が新しい順に並ぶ（`alertSortValue()`、アラート未発生は`-Infinity`で末尾）。旧`pairEntryDistance()`によるソートは撤去（関数自体は②の理由で残す）②カード上のアラートバッジをタップすると`mvToggleAlert()`内で`trendSortTf`もタップした足に切り替わり、ON/OFFのトグルと同時に一覧の並び替え基準も変わる（ツールバーのボタンとカードのバッジは同じ状態を共有）③サマリーの「🎯圏内」「巡回n/mペア」タイル、絞り込み`trendEntryOnly`、グループ見出し「🎯エントリー圏（n）」を撤去し、GO／未更新の2タイル＋GO最上位グループのみに整理 ④`pairEntryDistance()`/`tfEntryDistance()`/`pairIsHot()`/`tfIsHot()`は削除せず維持（波マップの`.hot`強調・🎯根拠ボタンの色分け`trendEntryChipHtml()`が使い続けるため）。データモデル・CSV・端末間マージ・統計・波マップ・根拠パネル・グランビルピッカーは無変更。既存テストのうち圏内・巡回に依存していた箇所を更新：s44-test.mjs（①③ブロックをアラート時刻順＋カード連動の検証に書き換え、64項目通過）、s59-test.mjs（サマリー2タイル前提に更新、28項目通過）、s60-test.mjs（圏内/待ちボタン不在の確認に更新、17項目通過）、s40-test.mjs（一覧サマリーの圏内フィルタ依存箇所をGOのみ絞り込みに置換、60/62項目通過・既知の無関係な2件除く）。s43/s62は無関係と確認（s62はS65時点で既に不整合が存在、今回のタスク範囲外）。sw.js: v37→v38 | 2026-09-09 |
| 66 | 🏷️ **根拠パネルの判定ボタンから絵文字マークを撤去**。ユーザー要望「記録フォームのボタンの左横の✅⏸️⏩のマークは消してよい」に対応。🔭一覧の根拠パネル footer で「📝 記録フォームへ」ボタンの左に並ぶ判定3ボタン（`trendJudgeBtnsHtml()`）は、S33以来 `j.label.slice(0, 1)` で絵文字1文字（✅/⏸/⏭）だけをボタン表示にしていたが、これを撤去し文言本体（エントリー/保留/スルー）を表示するように変更。`j.label.replace(/^\S+\s*/, '')` で先頭の絵文字＋空白を取り除くだけの描画層の変更で、`MV_JUDGES.label`（履歴カードのバッジ・フォームの判定表示・振り返り統計などで使う絵文字付きの完全な文言）・`mvSetJudge()`（判定保存ロジック）・`judgeLog`・データモデル・CSV・端末間マージ・統計はすべて無変更。Playwrightで判定ボタンのテキストとクリック後の`.on`クラス付与を確認（コンソールエラーなし）。sw.js: v36→v37 | 2026-09-09 |
| 65 | 🔁 **エントリー足❸の「重なり」をロールリバーサル確認に置換**。ユーザー要望「選択項目のエントリー足の重なりの項目を消して、ロールRvに変更したい。上位足のロールRvは消してよい」に対応。①エントリー足 `MV_ENTRY_CHECKS` の❸ `fiboRoll`（フィボ×ロールリバーサルの重なり、重なり有/重なり無/❌）を撤去し、上位足 `MV_TF_CHECKS` にあった `rollReversal`（確認/❌、short:❸ロールRv）をこの位置にそのまま移設 ②上位足からは `rollReversal` の行を削除（実運用では5分足＝エントリー足での確認と重複していたため、ユーザー指摘で不要と判断）。上位足は RCI×3・MACD・ラウンドナンバーの5項目に ③キー名 `rollReversal` は上位足・エントリー足どちらでも同じだが、保存先が `checksHigher`/`checksEntry` で分かれるため衝突しない。既存データ（旧上位足の `checksHigher.rollReversal`・旧エントリー足の `checksEntry.fiboRoll`）は読み書きされないだけで無害（S58 の `w.mode` と同じ扱い）④確度スコア（`checkConfidence`）は分母が残った項目の重み合計を使う設計なので変更不要（上位足は RCI45+MACD35+ラウンド10=90 を分母に計算）。CSV列は `hi_*` が6→5列（`hi_rollReversal` 消滅）、`en_*` は4列のまま（`en_fiboRoll`→`en_rollReversal`）。端末間マージ・波マップ・エントリー圏の距離計算（`pairEntryDistance()`）・並び順・統計タブは無変更。Playwrightスモークテストで新definitions・パネルHTML（「重なり」文言の不在・「❸ロールRv」の表示・ボタン操作）を確認（コンソールエラーなし）。sw.js: v35→v36 | 2026-09-09 |
| 64 | 🎯 **確度%を🔭一覧の🎯チップに表示、根拠パネルのエントリー足・合算表示を撤去**。ユーザー要望「確度のパーセントは通貨カードの🎯の横にも表示させて。エントリー足にはいらないから合算の表示もいらない」に対応。①`trendEntryChipHtml()` に上位足の確度%（`higherConfidence(w)`）を追加し、行の🎯チップ本体とtitle属性の両方に表示（375px以下では従来通り`.d`クラスが畳まれ🎯アイコンのみ、確度はtitleで読める）②根拠パネルの確度表示（`.tp-conf`）をエントリー足・合算を撤去し上位足のみの表示に簡略化 ③`setupConfidence(w)`（higher/entry/combinedを返す）を `higherConfidence(w)`（上位足の値だけを返す）に置換し、entry/combinedの計算自体を削除。データモデル・CSV・端末間マージ・統計・並び順ロジックは無変更。Playwrightスモークテストでチップ・パネルの表示・コンソールエラー無しを確認。sw.js: v34→v35 | 2026-09-09 |
| 63 | 🎚️ **根拠パネルの選択肢整理と確度スコアの重み付け**。ユーザー要望「根拠をタップ後の選択項目の変更。グランビルは無くしてよい。RCI待ち・MACD待ち・ラウンドナンバーの無・ロールの未確認をなくす。エントリー足の全項目の待ちの選択肢もなくす。全項目の左側にある横棒のみのタップ箇所（デフォルト選択に見える箇所）もなくす。上位足の確度はRCIとMACDを大きく、ラウンドナンバーとロールリバーサルは少なく」に対応。①`MV_TF_CHECKS`から`granville`行を撤去（🔭一覧の行に🌊アイコンで既出のため根拠パネルでの重複表示だった）。同期処理`mvGranvilleSyncField()`と書き込み先`checksHigher.granville`も削除、実際の波記録`w.trend[tf].granville`は無変更 ②RCI短期/中期/長期・MACDから「⏳待ち」系選択肢、ラウンドナンバーから「無」、ロールリバーサルから「未確認」、エントリー足❶❷❸から「⏳待ち」をすべて撤去。「待ち」概念がアプリ全体から消滅したため、常に0件で機能しなくなる`pairWaitCount()`／`trendWaitBadgeHtml()`／サマリー「⏳待ち」タイル／ツールバー「待ちあり」絞り込みを削除。`isUnmetCheck()`は❌のみを見る判定に単純化 ③`trendCheckCell()`の空欄ボタン（`btn('', '—')`、値が空文字で未記録時にデフォルト選択されて見えていた）を撤去。解除は選択済みボタンの再タップ（`mvWriteCheck`のトグル）で従来通り可能。連動して`.tp-synced`／`.tp-none`のCSSも削除 ④`checkConfidence()`に項目単位の`weight`プロパティを導入。上位足はRCI各15・MACD35・ラウンドナンバー10・ロールリバーサル10（合計100）に配分し、環境認識の核であるRCI/MACDを厚く、補助的な2項目を薄くした。`weight`を持たない項目は1として扱われるため、エントリー足は従来通り4項目均等割り（25%ずつ）。CSV列は`hi_*`×7→6に変化（`hi_granville`消滅、旧CSVは未知列として無視）。端末間マージ・波マップ・エントリー圏の距離計算（`pairEntryDistance()`）・並び順・統計タブは無変更。Playwrightスモークテストで新definitions・重み計算（MACD失点で65%、ラウンドナンバー失点で90%）・パネルHTMLからのgranville行/空欄ボタン不在を確認（コンソールエラーなし）。sw.js: v33→v34 | 2026-09-09 |
| 62 | 🪜 **エントリー足の根拠を3ステップ確認フローに全面置換**。ユーザー要望「エントリー足の選択項目は既存の項目を使わない。上位足の確度が高いことが前提で、❶上位足のネックライン付近で5分足にダブルトップ/三尊/ダブルボトム/逆三尊が出るのを確認 → ❷5分足のMAを明確に上抜けor下抜け（1波と仮定）→ ❸1波のフィボナッチとロールリバーサルが重なる箇所でエントリー（フィボの%を記録）、という流れで記録しやすい項目に」に対応。①エントリー足専用の `MV_ENTRY_CHECKS`（`necklineForm`／`maBreak`／`fiboRoll`／`entryFibo`）を新設。上位足 `MV_TF_CHECKS` からは `entryFibo` を外し7項目に。エントリー足からはグランビル/RCI×3/MACD/ラウンド/ロールRvが外れた ②`opts` は文字列でも `{v, side}` でも書けるようにし、`mvNormalizeChecks()` が宣言時に後者へ揃える。`side` があると上位足の方向に合う選択肢だけが出る（`mvEntrySide()` → `trendCheckCell()`）。方向未記録・レンジでは両サイド ③`mvSyncEntrySideChecks()` を新設し `mvWriteTrend()` と `mvSetTf('tfHigher')` から呼ぶ。方向反転・レンジ化で❶❷（向きに紐づく）だけ落とし、❸は残す ④パネルを縦2セクションに再構成（`.tp-sec` 見出し＋`.tp-row` は「ラベル｜選択肢」の2列に。旧 `.tp-head` の3列グリッドは廃止）。エントリー足の見出しには足の `<select>` と目線バッジ `.tp-side` を置く ⑤区分ごとの定義に追随させた5箇所：`checkConfidence(checks, list)`／`pairWaitCount()`／`basisItemsOf(checks, list)`／`CSV_BASIS_COLUMNS`（`MV_CHECK_SNAPSHOTS[].checks`）／`trendPanelHtml()` の `rowsOf()` ⑥`mvGranvilleSyncField()` は `tfHigher` のみに限定（エントリー足がグランビルを持たなくなったため）。既存データの `w.checksEntry.granville` は読み書きされないだけで無害 ⑦`entryFibo` のキー名据え置きにより📚の「エントリーFibo水準別の成績」とCSV `en_entryFibo` はそのまま動く。CSV列は `hi_*`×7／`en_*`×4 に入れ替え（旧列は import 時に無視）。端末間マージ・波マップ・`pairEntryDistance()`・並び順・統計タブは無変更。新設`s62-test.mjs` 48項目通過。併せて S60 で撤去済みの旧ツールバーid（`#trendFilterAligned`/`#trendFilterGo`）を参照して中断していた `s44-test.mjs`／`s40-test.mjs` を `[data-trend-summary-filter="hot|go"]` へ移行し、s44 は最後まで走って64項目通過（パネル行数の期待値も新仕様へ更新）。sw.js: v32→v33 | 2026-09-08 |
| 60 | 🎯 **GO/圏内/待ち/未更新のタップ箇所重複を解消**。ユーザーがスクリーンショットで指摘：サマリータイル（表示専用）とツールバーボタン（フィルタ用）に同じラベルの要素が2箇所並び、どちらを押せばいいか分からない状態だった。①サマリーの4タイル（🚩GO/🎯圏内/⏳待ち/🕐未更新）を`<span>`から`<button>`に変更し、`data-trend-summary-filter="go|hot|wait|stale"`でタップ即フィルタを兼務させた ②ツールバー1行目にあった同名4ボタン（`#trendFilterGo`/`#trendFilterAligned`/`#trendFilterWait`/`#trendFilterStale`）をDOMごと削除。S59で分離していたツールバー2行を「カテゴリ選択・表示する足・🗺波マップ」の1行に再統合 ③選択中の視覚表現はCSS `.trend-summary button.item.on { outline: 2px solid currentColor; }` を追加し、既存の色分け（`.item.go/.hot/.wait/.stale`）はそのまま活かして枠だけ強調 ④クリック処理を`trendSummaryEl`への1つのイベント委任に統合、旧4つの`addEventListener`と`renderTrendList()`内の`classList.toggle('on', ...)`4行を削除（`on`状態は`renderTrendSummary()`が直接クラスに出す）。巡回タイルはフィルタ手段が無いため`<span>`のまま維持。データモデル・CSV・端末間マージ・統計・並び順ロジック（`pairEntryDistance()`）はすべて無変更。新設`s60-test.mjs`17項目通過、`s59-test.mjs`のツールバー2行前提の④⑤⑥をS60仕様に更新し31項目通過、s55 15項目通過。sw.js: v32のまま（DOM/CSSのみの変更でキャッシュ無効化不要） | 2026-09-08 |
| 59 | 🧹 **🔭一覧タブ上部（説明文・サマリー・ツールバー）を巡回の実運用に合わせて整理**。ユーザー要望「一覧タブの上部のトレンド一覧の下の文言とソート用の項目を今に最適な形で整理したい」に対応。並び順のロジック自体（GO群→エントリー圏の距離順→同値は名前順）は変更せず、表示の整理のみ。①説明文はS24〜S58の継ぎ足しで5話題・306文字（375pxで12行=230px）に肥大していたのを常時2行に圧縮し、波タップ手順・GOの仕様・波マップへの言及は`.trend-help`（`<details>`）の折りたたみへ退避（初回だけ読む使い方を毎回のスクロールから外す）②サマリーを6タイル（🎯圏内=ペア単位／↗上昇・↘下降・─レンジ・未記録=足単位／🕐未更新=ペア単位、単位混在で誤読の元）から**5タイル全てペア単位**（🚩GO／🎯圏内／⏳待ち／🕐未更新／巡回n/mペア）に統一。足単位の方向カウントは各行のトグル色で常時見えており集計の価値が薄いため撤去、`w.trendAt`を持つペア数で「今日どこまで見終えたか」の進捗タイルに置換 ③【バグ修正】`renderTrendSummary()`がS44で出すクラス名を`.item.hot`/`.item.stale`に変えて以降、CSS側は旧`.item.rd`（S58で撤去済みの目線3択の残骸）のままで追随しておらず、圏内・未更新タイルが無色の灰色ピルで表示されていた。`.item.hot`/`.item.stale`をCSSに追加し`.rd`を削除 ④ツールバーを「絞り込み1行目（🚩GOのみ→🎯エントリー圏のみ→⏳待ちあり→🕐未更新のみ→カテゴリ、サマリーと同順）」と「表示設定・別画面2行目（表示する足の週足/月足トグル＋🗺波マップ）」に役割分離。`trendApplyFilters()`の絞り込み適用順もツールバー表示順に揃えた（ANDなので結果は不変、🗺波マップと共有しているため波マップ側の挙動も同一）。DOM id（`#trendFilterAligned`/`#trendFilterStale`/`#trendFilterWait`/`#trendCategorySelect`/`#trendFilterGo`/`#trendMapOpen`/`#trendTfToggles`/`#trendSummary`）・データモデル・CSV・端末間マージ・統計・並び順ロジック（`pairEntryDistance()`）はすべて無変更。s42/s43/s44/s55 全159項目通過、新設`s59-test.mjs`35項目通過（説明文圧縮・折りたたみの開閉・5タイルの単位統一・配色バグ修正の確認・ツールバー2行分割・絞り込み順入替後のAND結果一致）。sw.js: v31→v32 | 2026-09-08 |
| 57 | 🕐 **🔭一覧の巡回対象に1時間足（1H）を追加**。ユーザー要望「グランビル・ゾーン・波形の記入箇所に1Hを追加し、1H/4H/日足の順で表示」に対応。①`MV_TREND_TFS` の先頭に `{ k: 'h1', label: '1時間足', short: '1H' }` を追加し、並びを1H→4H→日足→週足（任意）→月足（任意）に変更 ②1Hは週足・月足と異なり `optional` を付けず常時表示（トグルなし）とし、`MV_TREND_DEFAULT_TFS`（並び順・🎯圏内サマリー・根拠ボタンの表示条件の主軸）にも自動的に含まれる＝ `pairEntryDistance()` は1H・4H・日足の3本の最短距離で判定するように拡張 ③`MV_TF_TO_TREND_KEY`/`MV_TREND_KEY_TO_TF` に `'1時間足': 'h1'` を追加し、上位足に1時間足を選んだ根拠パネル・ボードのグランビルが🔭一覧のh1記録と自動連動するように ④`.gv-dot.tf-h1`/`.gv-ref-btn.on.tf-h1`（色 `#c2255c`）を追加し、波マップ単一銘柄モード・グランビルピッカーの他足参考重ね表示にも1Hが自動で乗る。`mvEnsureTrend()`・端末間マージ（`mergeDoc()`）は`MV_TREND_TFS`をforEachする既存実装のままで新フィールドを自動吸収（既存ペアのマイグレーションも自動）。スキーマ変更は`w.trend.h1`が生えるのみでCSV・トレード記録・統計には無関係。**波マップの既定表示タブが「表示中の先頭の足」＝1Hに変わった**ため、s40/s42/s43/s44テストの `#trendMapOpen` 直後に `[data-map-tf="d"]` を明示クリックする箇所を追加し、🎯チップ数の期待値（2→3）を更新。s44 61項目・s43 35項目・s42 45項目・s40 60項目（既知2件の失敗はS47以前からの無関係な既知問題）通過。sw.js: v29→v30 | 2026-09-07 |
| 58 | 🚩 **🔭一覧カードの「目線」3択（📈トレンドフォロー/〰レンジ/🔄RD逆張り）を撤去し、手動GOフラグに置換**。ユーザー要望「近々で監視が必要な最もエントリーに近い通貨をソートして表示させたい」に対応。目線3択はS44で自動判定から切り離されて以降、絞り込みタグの役目しか無かったため、同じ位置（ペア名直後）に**GOボタン**を新設。①データモデルは `w.go`（真偽値）/ `w.goAt`（マージ用タイムスタンプ）を追加、`w.mode`/`w.modeAt`は読み書きしないだけで既存データはlocalStorageに残る（無害）②書き込み口 `mvSetGo()` はトグル式（`mvSetJudge`/`mvWriteTrend`と同じ「書き込み口を1つにする」規約）。`pairEntryDistance()`（エントリー圏の自動距離）とは完全に独立した主観の優先度タグで、自動解除は無い＝再タップするまで残り続ける ③`mvClearTrend()`（🧹巡回記録クリア）の対象外に明示（GOは巡回記録のやり直しとは無関係の「見張ると決めた印」なので消さない）④`renderTrendList()` に最上位「🚩 GO（n）」グループを新設（GO内部は従来通りエントリー圏距離順）。GOにすると即座にグループ移動が要るため、他の行内タップ操作（S24最重要仕様3）と異なりここだけは`trendPatchItem()`ではなく`renderTrendList()`で全体を作り直す ⑤ツールバーの目線フィルタ用`<select id="trendModeSelect">`を`🚩 GOのみ`ボタン（`#trendFilterGo`、`trendGoOnly`）に置換、`trendApplyFilters()`に統合（波マップと共有・AND条件）⑥端末間マージは`mergePair()`の`judgeAt`と同じ形で`goAt`のlast-write-wins ⑦波マップの過去日レコード（`items`が`p/t/g/w/s`のみ）はGOを運ばないため過去日では「🚩GOのみ」が効かない旨を`#trendMapNote`に明記（既存の「未更新のみ」と同じ制約）。CSV・トレード記録・統計・エントリー圏距離計算・波マップの色分けは無変更。s44-test.mjsの目線フィルタ検証をGOフラグの検証に置換（3箇所）。s44 64項目・s43 35項目・s42 45項目・s40 60項目（既知2件は無関係）・s55 15項目（併せてs55-test.mjsの他マシンパス直書きバグを修正）全通過。sw.js: v30→v31 | 2026-09-08 |
| 56 | 🛡️ **iOS起床トリガー拡充・容量超過リーンモード・復旧関数追加**で複数端末同期のロバスト性強化。①`visibilitychange`に加えて`pageshow`/`focus`/`touchstart`（4秒スロットル）/30秒ポーリング（前面表示時のみ）を追加し、iPhone開きっぱなしでもPC側更新が届くように（移植元プロジェクトで実証済み、課金上限枠考慮で30秒間隔）②`saveMarket()`の`QuotaExceededError`キャッチで、容量超過時に波マップ（wavemap）の保持を直近7日に圧縮して再保存を試行。ペア・チェック・判定・トレードは削らない（「記録本体 > 使い捨て履歴」の優先順位）。`saveTrades()`は中身を推測削除せず、エクスポート案内メッセージに変更③コンソール専用の`resetCloudToLocal()`復旧関数追加。端末間マージで直らない異常時（クラウド側doc破損など）に、ローカルを正としてクラウド側を丸ごと作り直す最終手段。UIには出さず、確認ダイアログ付き。既存機能に影響なし（マージロジック・認証・破損検知は維持）。s55-test 15項目通過・既存 s44/s43/s42 全141項目通過。sw.js: v28→v29 | 2026-09-07 |
| 55 | 🧹 **🎯トレードタブを廃止し、下部タブバーを3タブ（🔭一覧／📚振り返り／⚙設定）に再統合**。ユーザー指摘「🔭一覧タブだけで環境認識から結果記録まで完結できるはずなのに、わざわざ🎯タブへ画面遷移する流れは不要」による設計変更。①`#tabPanel-trade`（`.tab-panel`）を`#tradeModal`（`.modal-backdrop` + `.modal.wide.trade-modal`）へ包み替え。フォーム内部のDOM構造・id（`fDatetime`/`resultTagToggle`/`fPnl`等）は完全に維持 ②`openTradeModal(watchId)`/`closeTradeModal()`を新設し、根拠パネルの「📝 記録フォームへ」（`data-trend-goto-form`）・ヘッダの「→」（`data-trend-goto-board`）・履歴の「✎ 編集」（`loadTradeIntoForm()`）の3つの入口すべてをここに統一。✕ボタン・背景クリック・Escapeキー・`doSave()`保存成功時の4経路すべてが`closeTradeModal()`を通る ③`setActiveTab()`の既定値・起動時タブを`'trend'`に変更 ④`tabPanels`オブジェクトから`trade`キーを削除。`doSave()`/`syncFormFromMarket()`/`pickMarketFields()`/`marketFieldsFromBoard()`のロジックは1行も変更していない（DOM idが同一なのでモーダル内でもそのまま動作）。s42-test/s44-testの`[data-tab="trade"]`参照2箇所を更新（s44は「🎯タブ自体が存在しない」検証に置換、s42は`review`タブへのトグルに置換）。新設`s55-test.mjs`（15項目：タブ数・モーダル開閉4経路・保存後の自動遷移・編集モード）。データモデル・CSV・端末間マージ・統計は無変更。s42/s43/s44 全141項目通過。sw.js: v27→v28 | 2026-09-06 |
| 48 | 🎯 **根拠ボタン常時表示化**。S47 まで🎯「根拠ボタン」は圏内の足にだけ出ていたが、巡回中に「圏外のうちからRCI/MACDなどを記録したい」ユースケースに対応。実装は `trendEntryChipHtml()` で distance が圏外・null でも常時出力＆CSS `.tent.muted`（グレー）で圏外・未記録時は中立色表示。圏内は従来通り緑で強調、パネル開くと `.open` が優先で緑になる。条件が揃うほど確度スコア（`checkConfidence()`、パネル内に％表示）が上がっていく運用フローが実現。スキーマ・CSV・端末間マージ・統計・判定は無変更。s44-test 61/61 / s43-test 35/35 / s42-test 45/45 / s40-test 60/62（既知外）通過。sw.js: v22→v23 | 2026-09-04 |
| 47 | 🗺 グランビル・ピッカーに他時間足の参考重ね表示を追加。「タップして記録するのは開いた1時間足だけ」という既存仕様は維持しつつ、上部のトグルボタン（`trendVisibleTfs` にある他の時間足だけ・記録が無い足は disabled）を押すとその足の既存の波位置（実測 or 概算）を波マップと同じ足別配色で図に重ねられる。①`gvRefEntry(w, tfKey)` は書き込みに一切関与しない読み取り専用の導出（座標・圏内判定は `trendMapEntryOf()` と同じ `gvEntryDistance()` を通す）②`gvRefDotHtml()` で波マップと同じ `.gv-dot.tf-*` を描画 ③トグル状態 `trendGvRefOn` はモーダルを開くたびにリセット（`openTrendGv()`）④現在の時間足への書き込み経路（`trendGvFig` の click ハンドラ・`mvWriteTrend()`）は無変更、Playwright で他足の granville が影響を受けないことを確認。ボード・一覧どちらの入口も `trendGvModal` を共有するので実装は1箇所で両方に反映。スキーマ・CSV・端末間マージ・統計・判定は無変更。s44-test 59項目・s43-test 35項目・s42-test 45項目通過（s40-test の2件の既失敗はこの変更と無関係、変更前から発生）。sw.js: v21→v22 | 2026-09-04 |
| 46 | ⏳ 根拠チェックに「待ち」の選択肢を追加。RCI短期/中期/長期に「⏳待ち」、MACDに「⏳RD待ち」「⏳HD待ち」を追加（`MV_TF_CHECKS` の `opts` 編集のみ、定義元1箇所でボード・根拠パネル・CSV・履歴サマリーに自動反映）。`isUnmetCheck()` を新設し `checkConfidence()` は❌と同じ全減点扱いに統一。`pairWaitCount()`（w に保存しない純粋な導出関数）で一覧の行ヘッダに⏳バッジ、ツールバーに `⏳ 待ちあり` 絞り込みを追加（`trendApplyFilters()` に統合、波マップと共有）。並び順（`pairEntryDistance()`）には混ぜない。スキーマ・CSV列構成・端末間マージ・統計は無変更。s44-test 59項目・s43-test 35項目・s42-test 45項目通過、新規ロジックはPlaywright手動スポットチェックで確認。sw.js: v20→v21。リモート最新（gvMiniSvg撤去後）の上に積み直し | 2026-09-04 |
| 45 | 🧹 グランビル「ミニ波形」表示の撤去。行チップの `gvMiniSvg()` が、タップ記録した実座標を実アンカー座標（`MV_GRANVILLE_ANCHORS`）と無関係な手描きの汎用パス（`GV_MINI_PATH`）へそのまま重ねていたため、点の位置がタップ場所と食い違って見えるバグをユーザー報告のスクリーンショットで確認（買③以外の7ポジションで座標が大きくズレることを数値比較で特定）。`gvMiniSvg()` 関数と `GV_MINI_H`/`GV_MINI_POINTS`/`GV_MINI_PATH` 定数、`.gv-mini` CSS を削除。ラベル文字（`g.short`）とタップ/概算の区別（title属性）はそのまま残す。s40-test.mjs の関連アサーションを更新（ミニ波形が出ないことを確認する内容に変更）。スキーマ・CSV・端末間マージ・統計は無変更。s44 59項目・s43 35項目・s42 45項目・s40 62項目通過。sw.js: v18→v19 | 2026-09-03 |
| 44 | 🧹 **GO / WAIT / NO / RD 判定の全廃**とエントリー圏を軸にした 🔭一覧。①`setupVerdict()` / `pairStatus()` / `MV_SETUPS` / `SETUP_STATUSES` / `SETUP_SIDE` / `TREND_GROUPS` / `trendSetupBadgesHtml()` と `.sbadge` CSS を削除（`SETUP_GO_WAVES` だけは `gvEntryDistance()` の圏の中心として残す）②並び順・サマリー・絞り込みを `pairEntryDistance()`（＝買②③/売②③までの最短 % 距離）に置き換え。見るのは日足・4Hだけ／概算（`wpos` 空）は数えない ③根拠パネルの入口をセットアップバッジから**圏内の足の 🎯「根拠」ボタン**へ（`trendPanel = { id, tfKey }`、押した足がそのまま `tfHigher`）④ピッカーの8ボタンと「売買8つすべて表示」を撤去し、読み取り表示を圏との距離に。記録経路は図のタップだけ ⑤波マップのドット色を中立1色＋圏内 `.hot` に、`wmBuildItems()` の `s` は空（キーは互換のため残す）。スキーマ・CSV・端末間マージ・統計は無変更。s44 59項目・s43 35項目・s42 45項目・s40 63項目通過。sw.js: v17→v18 | 2026-09-03 |
| 28 | 🛡️ データ消失対策と複数端末同期。①破損検知キルスイッチ（読み込み失敗が本物のデータを空で上書きしていたバグの修正）②全データJSONバックアップ ③PWA化（iOS の7日削除回避）④端末間マージ用タイムスタンプ ⑤Supabase 同期（要素キー単位マージ・楽観ロック） | 2026-08-25 |
| 29 | 🔗 グランビル連動＆双方向移行ボタン。グランビルの選択肢 3択→8択統一、トレンド一覧記録をボードへ自動反映（対応足のみ読み取り専用化）、ボード↔一覧の移行ボタン新設（ペア単位＆足単位）、`MV_TF_TO_TREND_KEY` と `mvGranvilleSyncField()` で連動制御 | 2026-08-26 |
| 30 | 🎯 環境認識ボード通貨選択UI改善。通貨自由入力廃止（プリセット28銘柄のみ）。2段カスケードプルダウン化（グループ選択→銘柄選択）でスマホ操作効率化。既存自由入力ペアは継続表示・保持 | 2026-08-26 |
| 31 | 🔍 ストレージ診断パネル実装。ユーザー報告「1時間で消える」問題の原因特定（複数オリジンからのアクセス）。設定タブに診断パネル追加：現在のURL・データサイズ・最終書き込み時刻・write-through verify・ガイダンス | 2026-08-26 |
| 32 | 🎯 セットアップ自動判定（🟢GO / 🟡WAIT / ⚪NO）。旧 `trendRank()`（🔥/👀/💤）を完全置換。上位足に日足を使う場合と4時間足を使う場合で**別々に判定**（`MV_SETUPS`）、WAIT には「あと何を見れば GO か」の理由を表示。判定は `w` に保存しない純粋な導出関数なので localStorage スキーマ・CSV・端末間マージ・統計は無変更 | 2026-08-27 |
| 33 | 🔭 一覧タブで環境認識を完結。①ヘッダの判定3ボタンを削除（巡回中は不要）②**セットアップバッジをタップすると根拠パネルが開く** — 上位足がバッジから確定し、根拠14項目をプルダウンで入力、判定まで入れて「📝記録フォームへ」で直行。③ボードのハンドラから `mvSetTf()` / `mvWriteCheck()` を共有関数として切り出し。書き込み先はボードと同じ5フィールドなのでスキーマ・CSV・端末間マージ・統計は無変更 | 2026-08-28 |
| 34 | 🎯 根拠パネルのボタングループUI統一。根拠14項目を `<select>` からボード側と同じ `mv-tfcheck-btn` ボタン群に変更。375px でも2列折り返し表示。ボタン内でのみ `.on` クラスを切り替え、パネル全体再描画なし（位置・フォーカス保持） | 2026-08-28 |
| 35 | 🔭 一覧カードに「目線」3択を追加。📈 トレンドフォロー / 〰 レンジ / 🔄 RD逆張り でその土俵を最初に振り分け。レンジは自動で NO に、RD は最上位グループに。判定ロジックは `setupVerdict()` の冒頭2行で短絡。`mvSetMode()` で書き込み、`trendPatchItem()` で行だけ差し替え（並べ替えなし）。スキーマ・CSV・同期・統計は無変更 | 2026-08-30 |
| 36 | 📱 目線フィルタ用プルダウン。ツールバーに `#trendModeSelect` を追加。📈/〰/🔄 で絞り込み可能に。既存フィルタ（GO のみ・未更新のみ・カテゴリ）と AND 条件で併用。`renderTrendModeOptions()`・`trendModeFilter` 変数・`renderTrendList()` 内の絞り込み条件・change イベントリスナーを実装。sw.js: v8→v9 | 2026-08-31 |
| 37 | 🎯 一覧タブの根拠チェック選択肢調整。RCI 短期/中期/長期を「上限/60↑/60↓/下限/❌」の5択に変更。MACD を「RD(転換)/HD(継続)/❌」に表示名変更。`MV_TF_CHECKS` の `opts` 配列を更新するだけで環境ボード・パネル両方に反映。sw.js: v9→v10 | 2026-08-31 |
| 38 | 🟢 GO 確度スコア＆エントリーフィボナッチ記録。①GO バッジに根拠の揃い具合を%表示（上位足/エントリー足/合算）。`MV_TF_CHECKS.length` を分母に ❌ の数だけ減点。パネル内にも確度の内訳を表示。②`MV_TF_CHECKS` に `entryFibo` 項目（23/38/50/61/78%）を追加。巡回で「この1波の何%で入った」を記録可能に。sw.js: v10→v12 | 2026-08-31 |
| 39 | 📚 エントリーFibo水準別の成績分析＆🔭巡回記録クリア。①📚振り返りタブに `#fiboStats` で「エントリーFio水準別の成績」タイル追加（FIBO_ORDER で 23→78% 順、件数・勝率・平均RRを比較）。集計元は `mvChecks.entry.entryFibo` で、決済済みトレード（`tradeNetRR(t).net != null`）のみ母数。②🔭一覧カードに🗑ボタン（`mvClearTrend(watchId)`）で巡回記録（`w.trend` / `w.trendAt` / `w.mode`）のみクリア、ボード側データ保護。確認ダイアログ搭載。Playwright smoke test 完全通過、コンソールエラーなし。sw.js: v12→v13 | 2026-09-01 |
| 43 | 🎯 波マップのエントリー圏。買②③/売②③（`SETUP_GO_WAVES`）のアンカー ± `GV_ENTRY_RADIUS`(=10%) を圏として図に敷き、**図の上でタップして記録した位置が圏内のものだけ強調**。凡例の先頭に「🎯 圏内」グループ（距離の近い順）、ツールバーに `🎯 エントリー圏のみ` トグル。⚠️ 距離は `gvNearest()` と同じ素の % 距離／概算（`wpos` 空）は圏内に数えない／絞り込みは `wmScatter()` より前／`.hot` は当日レイヤーだけ。判定・CSV・統計・端末間マージは無変更。s43-test 35項目・s40-test 65項目・s42-test 46項目通過。sw.js: v16→v17 | 2026-09-03 |
| 42 | 🗺 波マップの日次記録・単一銘柄モード・タップ不反応の修正。①🔭一覧を開くたびに今日の波マップを `snapshots` へ上書き保存（`WM_KIND` / `WM_KEEP_DAYS`）。日付を戻すと前日を薄く重ねて**動いたものだけ軌跡線**を引く。⚠️ 縮小ガードのカウントから wavemap を除外／`mergeDoc` にも `wmPrune` を掛けないと `__bak` 焼き潰しと「落とす→同期で戻る」ループが起きる ②銘柄プルダウンで1銘柄の日足と4Hを同時表示（色は足別） ③**バグ修正**：「売買8つすべて表示」ON のとき逆 side が `mvWriteTrend()` に消され図の広範囲が無反応だった → スナップ候補は常に方向側に固定し、逆側ボタンは書かずに理由を出す。スキーマ・CSV・統計・判定は無変更。s42-test 46項目／s40-test 65項目通過。sw.js: v15→v16 | 2026-09-02 |
| 41 | 🖼 グランビルの波位置記録用画像を巡回向けのシンプルな図に差し替え、`MV_GRANVILLE_ANCHORS` の8点を測り直し。sw.js: v14→v15 | 2026-09-02 |
| 40 | 🎯 波の位置をタップで記録＋🗺 波マップ。①グランビルの図がタップ面になり、今のローソク足の位置を直接指すと `w.trend[tf].wpos`（`"x,y"` %）に残り、最寄りの公式ポジション（`MV_GRANVILLE_ANCHORS` / `gvNearest()`）が波番号として確定する。スナップ候補はピッカーの選択肢と同じ集合に限る（方向と逆 side を選ぶと `mvWriteTrend()` が自動で消すため）②一覧の行に極小の波形 SVG（`gvMiniSvg()`）を出し、タップ記録と概算（ボタン選択）を見分けられるようにした③🗺 波マップで全通貨の波の位置を1枚に重ねる（`trendApplyFilters()` を一覧から切り出して絞り込みを共有／重なりは小円周へ散らす／図の下に波番号別の全件テキスト）。`trend[tf]` は丸ごとマージされるので端末間同期は無変更、判定・CSV・統計も無変更。Playwright 62項目通過。sw.js: v13→v14 | 2026-09-01 |
| 41 | 🎯 グランビルの波位置記録用画像を統合。教科書的な複雑図→シンプルなモノクロ図（360×360、白背景・グレー波・黒マーク・テキスト最小限）に置き換え。Python で8点座標を自動検出（PIL + scipy）→ % 座標に変換。WebP圧縮（7.3KB）。`IMG_GRANVILLE` data URI 更新、`MV_GRANVILLE_ANCHORS` 新座標に更新。タップ面の精度向上・波マップの見渡し精度向上・巡回効率向上。sw.js: v14→v15。判定・CSV・端末間マージ・統計は無変更 | 2026-09-02 |
| 49 | 🗺 **波マップに前日比較の表示ON/OFFトグルを追加**。ツールバーに「🕓 前日比較」ボタン（既定ON）を追加。OFFにすると薄いドット・軌跡線が消え、注記も「非表示中です」に切り替わる。`trendMapShowPrev` フラグで `prevDay` を `null` に固定して前日レイヤー計算をスキップ。既存の「今日は比較対象から外す」「絞り込みは散らしより前」等の仕様は不変。開き直すと毎回 ON にリセット（日付/銘柄/圏内フィルタと同じ）。s42/s43/s44 全通過（計141項目）、コンソールエラーなし。sw.js: v23→v24 | 2026-09-04 |
| 50 | 🗺 **波マップの過去日訂正機能を追加**。誤った位置・波番号を記録してしまった場合、過去日を表示中に「✏️ 記録を訂正」ボタンでその日の記録を後から修正・削除できる。設計：過去日のレコードを直接書き換えるのではなく、その日の「新しい記録」を新しい id で追記する方式。mergeLog が同一 id で先勝ちのため id 使い回しは他端末に伝播しない。wmByDay が day ごとに at が最新のものを表示採用する既存仕様をそのまま利用するので、既存の同期・保持ロジック（wmPrune・mergeLog）はすべて無変更。s42/s43/s44 全通過、Playwright スポットチェック完了。sw.js: v24→v25 | 2026-09-04 |
| 51 | 🧹 **🎯タブの環境認識ボード（Step①）を完全廃止**。S33〜S48で🔭一覧の根拠パネルが `tfHigher`/`tfEntry`/`judge`/`checksHigher`/`checksEntry` を書ける唯一の入力元として成熟しており、旧ボードは同じ内容を二重編集させるだけのUIになっていた。①`renderMarketBoard()`・`selectPair()`・ペア選択カスケードプルダウン・ステップナビを削除 ②ペア削除（旧「このペアのデータを削除」）は🔭一覧の行に🗑ボタンとして統合、既存の🧹（巡回記録クリア）とは別操作に分離 ③記録フォームの「⚠未設定」警告・「🔭一覧タブへ」ボタンは選択中ペアの行までスクロール＆ハイライトする（旧ボードの同機能を踏襲）④`mvSetTf`/`mvWriteCheck`/`mvToggleAlert`/`mvSetJudge` 等の共有書き込み関数はそのまま温存（🔭一覧が引き続き使用）。データモデル・CSV・端末間マージ・統計は無変更。s44 61項目・s43 35項目・s42 45項目通過、実際のUIフロー（🎯根拠ボタン→判定→「→」で記録フォームへ）をPlaywrightで実地確認。sw.js: v25→v26 | 2026-09-05 |
| 54 | 🧹 **トレード記録フォームを建玉ブロック・決済モーダル・RR計算方式から結果タグ・円建て損益方式に全面置換**（ユーザーの明示的な指示による完全置換、確認済み）。①フォームに「アラート発生日時」「アラート発生時間足」（`manualAlertAt`/`manualAlertTf`、🔭一覧のボード由来アラートとは別名で独立）を新設 ②建玉ブロック（エントリー価格/損切り価格/損切り根拠・計画ターゲット表示）と3分割決済モーダル（`#exitModal`）を丸ごと削除し、代わりに結果タグ（REG/BIG/MAX/微益/建値決済/損切/その他、既存の `.result-toggle`/`.result-btn` パターンを再利用）・円建て損益（符号切替ボタン付き）・建値タッチ（3択）・チャート画像添付（エントリー足/上位足/その他）を追加 ③画像は Firebase Storage の REST API へ**fetchのみ**でアップロード（`uploadChartImage()`、SDK不使用、単一ファイル原則を維持）。bucket・API Keyは⚙設定タブの新セクションで入力・保存 ④削除した関数：`calcRR`/`fmtRR`/`tradeExits`/`isSplitTrade`/`slotExits`/`tradeNetRR`/`planTargetPrices`/`decimals`、決済モーダル一式（`openExitModal`/`renderExitAll/Risk/Slots/Rows`/`exitRowInner`/`renderExitSum`/`fillRemainingSlots`ほか）、`exitSummaryHtml`/`rrCls` ⑤CSV列を `entryPrice`/`slPrice`/`slBasis`/`splitCount`/`exits` から `manualAlertAt`/`manualAlertTf`/`resultTag`/`resultOtherReason`/`pnlAmount`/`beTouch`/`imgEntry`/`imgHigher`/`imgOthers` に置換（旧建玉・決済データは import しても復帰しない）⑥統計タブを合計損益/平均損益/勝率（pnlAmount基準）・結果タグ別成績・建値タッチ別成績に刷新、`fiboStats`（エントリーFibo別成績）は損益ベースに更新した上で維持、`mvAlertStats`（アラート発生足別の判定統計）は無変更 ⑦履歴カードは決済ボタン・RR表示・損切り根拠行を撤去し、アラート発生日時/結果タグバッジ/損益（色分け）/建値タッチ/画像サムネイルに置換。データモデル以外（🔭一覧・端末間マージ・波マップ等）は無変更。構文チェック OK。sw.js: v26→v27 | 2026-09-06 |

## 実装済み機能サマリー（S28-S58、元CLAUDE.mdの箇条書きから移設、S80で本体を軽量化）

- ✅ **GO/圏内/待ち/未更新のタップ箇所重複を解消**（S60）— サマリーの4タイルを`<button>`化して`data-trend-summary-filter`でフィルタを兼務させ、ツールバーの同名4ボタンをDOMごと削除。ツールバーは「カテゴリ選択・表示する足・波マップ」の1行に再統合。選択中はCSS `outline` で強調。データモデル・CSV・端末間マージ・統計・並び順ロジックは無変更
- ✅ **🔭一覧タブ上部の整理**（S59）— 説明文を常時2行＋`<details>`折りたたみに圧縮、サマリーを5タイル全てペア単位（🚩GO/🎯圏内/⏳待ち/🕐未更新/巡回進捗）に統一（足単位の方向カウントを撤去）、ツールバーを絞り込み1行目・表示設定2行目に分離。サマリー配色クラスが`.item.hot`/`.item.stale`にCSSが追随していなかったバグも修正。並び順ロジック・DOM id・データモデル・CSV・端末間マージ・統計は無変更
- ✅ **iOS起床トリガー拡充・容量超過リーンモード・復旧関数**（S56）— `visibilitychange`に加えて`pageshow`/`focus`/`touchstart`/30秒ポーリングを追加。iPhone開きっぱなしでもPC側更新が届く。`saveMarket()`は容量超過時に波マップを直近7日に圧縮。`resetCloudToLocal()`コンソール関数で端末間の食い違いを最終手段で修復
- ✅ **🎯トレードタブ廃止・🔭一覧タブへの完全統合**（S55）— 下部タブバーは🔭一覧／📚振り返り／⚙設定の3タブに。トレード記録フォームは`#tradeModal`としてモーダル化し、根拠パネル「📝 記録フォームへ」・ヘッダ「→」・履歴「✎ 編集」の3入口から開く。保存・キャンセルで自動的に一覧タブへ戻り、画面遷移なしで巡回→記録が完結する
- ✅ グランビルの法則 / MACD（画像埋め込み＋📖ジャンプリンク、ジャンプ元は🔭一覧の根拠パネルの根拠チェック行）
- ✅ **結果タグ＋円建て損益方式によるトレード結果記録**（S54）— アラート発生日時・時間足、結果タグ（REG/BIG/MAX/微益/建値決済/損切/その他）、円建て損益、建値タッチ、Firebase Storageへのチャート画像添付。旧・建玉ブロック/3分割決済モーダル/RR自動計算は全廃
- ✅ 🔭トレンド一覧タブ（全ペア横断で上位足トレンド比較、アラート/判定を一覧上で操作、全28銘柄常時表示、環境認識・ペア削除まで完結する唯一の入力元）
- ✅ 結果タグ別・建値タッチ別の成績分析（統計タブでランキング表示、S54）
- ✅ CSV完全対応（`mvChecks` / 結果タグ・損益・建値タッチ・画像URLの往復、hold値対応、旧CSV互換）
- ✅ 記録の後日編集機能（🔭一覧由来項目は当時の値で凍結）
- ✅ 判定の3択化（エントリー/保留/スルー）— 🔭一覧タブが唯一の入力元（必ず `mvSetJudge()` 経由で `judgeLog` に記録）
- ✅ 🎯タブ → 記録フォームのデータ連動（`syncFormFromMarket()` / `marketFieldsFromBoard()`、S51で🔭一覧の根拠パネルからの読み取りに一本化）
- ✅ 🔭一覧タブ内のアラート・判定操作（共有関数 `mvToggleAlert()` / `mvSetJudge()`）
- ✅ 全銘柄自動生成（`loadMarket()` でプリセット28ペアを自動生成、一覧タブで常時表示可能に）
- ✅ **ファイルサイズ最適化**（S26）— 画像3種をWebP化。2.04MB → **0.41MB（80%削減）**。新インジケーター追加に向けて容量確保完了
- ✅ **カテゴリ絞り込みプルダウン**（S27）— 一覧タブに `MV_PAIR_GROUPS` ベースの絞り込み機能を追加。既存フィルタ（揃っているペアのみ・未更新のみ）と並列処理で AND 条件対応
- ✅ **データ消失対策＋複数端末同期**（S28）— 破損検知キルスイッチ、JSON バックアップ、PWA化、端末間マージ用タイムスタンプ、Supabase同期
- ✅ **グランビル自動連動＆双方向移行ボタン**（S29）— グランビル 3択→8択統一、トレンド一覧の記録をボードへ自動反映（対応足のみ読み取り専用化）、ボード↔一覧の移行ボタン新設（ペア単位＋足単位）
- ✅ **🔭一覧の根拠パネル**（S33-34）— セットアップバッジ＝入口。上位足がバッジから確定し、根拠14項目をボタングループ（上位足×エントリー足の2列、`—` + 8選択肢）で入力、判定まで入れて記録フォームへ直行。ボード側と統一UI。ボタン内で状態切り替え、パネル全体再描画なし。`mvSetTf()` / `mvWriteCheck()` をボードと共有
- ✅ **🔭一覧「目線」3択**（S35、**S58で撤去**）— ペア名の直後に 📈 トレンドフォロー / 〰 レンジ / 🔄 RD逆張り。巡回で最初に土俵を振り分け、レンジは自動で NO に落ち、RD は最上位グループに上げる。目線を押しても行は飛ばない（`trendPatchItem` で行だけ差し替え）。再タップで解除しても方向・ゾーン・波の記録は消えない
- ✅ **🔭一覧「目線」フィルタ**（S36、**S58で撤去**）— ツールバーに目線フィルタ用プルダウンを追加。📈/〰/🔄 で絞り込み可能に。既存フィルタ（GO のみ・未更新のみ・カテゴリ）と AND 条件で併用
- ✅ **RCI/MACD 選択肢調整**（S37）— 表示名を「上限/60↑/60↓/下限」「RD(転換)/HD(継続)」に変更。ユーザーに直感的で、トレード記録・CSV・統計は無変更
- ✅ **GO 確度スコア＆エントリーフィボナッチ記録**（S38）— ①GO バッジに根拠の揃い具合を%表示（上位足/エントリー足/合算）。❌ の数だけ減点。パネル内にも内訳表示。②`MV_TF_CHECKS` に `entryFibo` 項目（23/38/50/61/78%）を追加。巡回で「この1波の何%で入った」を記録可能に。エントリー実現性の分析向上
- ✅ **エントリーFio水準別の成績分析＆巡回記録クリア**（S39）— 📚振り返りタブに「エントリーFio水準別の成績」タイル追加（23%/38%/50%/61%/78%の件数・勝率・平均RR）＆🔭一覧カードに🗑クリアボタン（巡回記録のみリセット、ボード側データ保護）。Playwright smoke test 完全通過
- ✅ **波の位置タップ記録＆🗺 波マップ**（S40）— グランビルの図をタップして「今のローソク足がどこにいるか」を `wpos`（画像に対する % 座標）として記録。最寄りの公式ポジションが波番号として自動確定するので、GO/WAIT/NO 判定・CSV・端末間同期・統計はすべて無変更。一覧の行には極小の波形 SVG が出てタップ記録と概算を見分けられ、🗺 波マップでは全通貨の波の位置を1枚に重ねて「今どこに何が溜まっているか」を掴める。Playwright 62項目通過（`s40-test.mjs`）
- ✅ **グランビル波位置記録用画像の統合**（S41）— S40 完成後、タップ面用に教科書的な複雑図をシンプルなモノクロ図に置き換え。Python で8点座標を自動検出（PIL + scipy）し、% 座標に変換。WebP 圧縮（7.3KB）。タップ精度・波マップの見渡し精度・巡回効率が向上。スキーマ・判定・CSV・端末間マージ・統計は無変更
- ✅ **🗺 波マップの日次スナップショット＆軌跡線**（S42①）— 一覧タブを開くたびに自動保存または「📸 記録」ボタンで手動保存。日付切替バーで前日・前々日と比較。軌跡線（SVG）で波の移動を視覚化。散らし処理で前日レイヤーに同じオフセットを適用し、偽の軌跡を排除。30日で圧縮・端末間マージ。`snapshots` 再利用で既存スキーマ無変更
- ✅ **🗺 波マップの単一銘柄モード**（S42②）— 銘柄プルダウンで1ペアの全時間足（日足・4H等）を同時表示。ドット・ラベルは足ごとに色分け。モーダル出入りで自動リセット。タブ機能は ON/OFF 選択に転換
- ✅ **グランビル・ピッカーのタップ不反応バグ修正**（S42③）— `gvCandidates()` は常に現在の方向側のみ返す。逆 side を書き込ませない設計に統一。図をタップしても反応しない領域がゼロに（※ S44 で選択ボタン自体を撤去したため、`.opp` の参考表示も併せて廃止）
- ✅ **🎯 波マップのエントリー圏**（S43）— 買②③/売②③のアンカー ± `GV_ENTRY_RADIUS`(=10%) を圏として図に敷き、タップ記録が圏内のものだけを強調。凡例の先頭に「🎯 圏内」グループ（距離の近い順）、ツールバーに「🎯 エントリー圏のみ」トグル。距離は素の % 距離で測る（スナップ結果との一致優先）、概算は圏内に数えない（距離0で全件圏内になる）、絞り込みは散らしより前に置く。判定・CSV・統計は無変更
- ✅ **判定の全廃とエントリー圏を軸にした 🔭一覧**（S44）— GO/WAIT/NO/RD を `pairEntryDistance()`（買②③/売②③までの最短 % 距離）に置き換え。並び順・サマリー・絞り込み・行の 🎯・波マップの色すべてがこの1つの連続量に乗る。根拠パネルの入口は「圏内の足の 🎯 根拠ボタン」になり、押した足がそのまま上位足に。ピッカーは図のタップだけの1経路に。スキーマ・CSV・端末間マージ・統計は無変更。s44-test 59項目通過
- ✅ **グランビル表示からテキストラベルを削除**（S45）— 一覧タブのグランビルチップから「買②」などのテキストを削除、波形アイコン🌊のみ表示。位置は title 属性で読める
- ✅ **根拠チェックに「⏳待ち」選択肢を追加**（S46）— RCI短期/中期/長期に「⏳待ち」、MACDに「⏳RD待ち」「⏳HD待ち」を追加。他の条件が揃っていてもRCI到達待ち・MACD発生待ち時の確度をスコアで反映。`pairWaitCount()` で一覧行に⏳バッジ、ツールバーに「⏳ 待ちあり」絞り込みを追加。並び順には混ぜない
- ✅ **グランビル・ピッカーの他時間足参考重ね表示**（S47）— 開いた時間足だけをタップで記録する既存仕様はそのまま、上部のトグルボタンで他の時間足（表示中のもの・記録があるものだけ）の既存の波位置を波マップと同じ足別配色で参考として重ねられる。読み取り専用の導出（`gvRefEntry()`）なので記録経路・スキーマ・CSV・端末間マージ・統計・判定は無変更
- ✅ **🎯 根拠ボタン常時表示化**（S48）— 圏内は緑で強調、圏外・未記録は中立色（グレー）で「記録可能」を示す。圏外のうちからRCI/MACDなどを事前記録でき、条件が揃うほど確度スコア（%表示）が上がっていく運用が実現。表示層のみなのでスキーマ・CSV・端末間マージ・統計・判定は無変更
- ✅ **🗺 波マップに前日比較の表示ON/OFFトグルを追加**（S49）— ツールバーに「🕓 前日比較」ボタン（既定ON）を追加。OFFにすると薄いドット・軌跡線が消え、注記も切り替わる。`trendMapShowPrev` フラグで `prevDay` を `null` に固定して前日レイヤー計算をスキップ。既存の「今日は比較対象から外す」「絞り込みは散らしより前」等の仕様は不変
- ✅ **🗺 波マップの過去日訂正機能**（S50）— 誤った位置・波番号を記録してしまった場合、過去日を表示中に「✏️ 記録を訂正」ボタンでその日の記録を後から修正・削除できる。設計は過去日のレコードを直接書き換えないで、その日の「新しい記録」を新しい id で追記する方式（mergeLog が同一 id で先勝ちのため、id 使い回しは他端末に伝播しない）。wmByDay が day ごとに at が最新のものを表示採用する既存仕様をそのまま利用するので、既存の同期・保持ロジック（wmPrune・mergeLog）はすべて無変更。s42/s43/s44 全通過、Playwright スポットチェック完了
- ✅ **🎯タブの環境認識ボード（Step①）を完全廃止**（S51）— 🔭一覧の根拠パネルが同じ5フィールドを書ける唯一の入力元として成熟していたため、UI二重管理だった旧ボードを撤去。ペア削除は🔭一覧の行の🗑ボタンへ統合。データモデル・CSV・端末間マージ・統計は無変更。s44/s43/s42 全141項目通過、実UIフローをPlaywrightで実地確認
- ✅ **🔭一覧の手動GOフラグ**（S58）— 目線3択（📈/〰/🔄）を撤去し、同じ位置にGOボタンを新設。エントリー圏の自動距離（`pairEntryDistance()`）とは独立に、巡回中の主観で「近々来る」と判断したペアへ1タップで印を付けられる。ONのペアは一覧最上位「🚩 GO」グループにまとまり、再タップで解除するまで自動では消えない（🧹巡回記録クリアの対象外）。ツールバーの目線フィルタは「🚩 GOのみ」ボタンに置換。データ（`w.go`/`w.goAt`）は端末間マージ対応。CSV・トレード記録・統計・エントリー圏距離計算は無変更

## 実装状態プローズ履歴（S28-S77、元CLAUDE.mdの「実装状態」節から移設、S80で本体を軽量化）

**✅ 本番使用可能** — セッション77で**🔭一覧の通貨名検索欄に✕クリアボタンを追加**。ユーザー要望「検索バーに入力した文字を消す際にバーの右側に小さな❌マークを設けてそこをタップすれば、入力していた文字をすぐ消せるようにしたい」に対応。①`#trendSearchInput`を`.trend-search-wrap`（`position: relative`）で包み、右端に`#trendSearchClear`ボタン（✕、`hidden`属性で既定非表示）を配置。CSSで入力欄の`padding-right`を空けてボタンと文字が重ならないようにした ②`input`イベントで検索語が空かどうかに応じて`trendSearchClear.hidden`を切り替え（1文字でも入力されたら表示）③クリアボタンのクリックで入力値・`trendSearchQuery`をリセットし、一覧の絞り込みを解除、フォーカスを入力欄へ戻す（続けて別の語句を打てるように）。`renderTrendList()`は`trendListEl`のみ再構築なので、この操作でも他のUI状態は影響を受けない。データモデル・CSV・端末間マージ・統計・並び順ロジック・波マップ・根拠パネルは無変更（S76の検索ロジック自体は無改修）。新設`s77-test.mjs`7項目通過（初期非表示・入力後表示・クリア後の値/表示/フォーカス/絞り込み解除、コンソールエラー無し）。sw.js: v46→v47。

**S76時点**：セッション76で**🔭一覧ツールバーに通貨名の部分一致検索を追加**。ユーザー要望「並び順や全てのカテゴリの箇所に、通貨の頭文字を入力したらその通貨だけ検索できる検索機能を設けて」に対応。①ツールバーのカテゴリ選択（`#trendCategorySelect`）の隣に検索入力欄（`#trendSearchInput`）を新設。`input`イベントで`trendSearchQuery`（大文字小文字を区別しないよう`.toUpperCase()`で保持）を更新し即時絞り込み ②書き込み先は既存の絞り込みの合流点`trendApplyFilters()`に1行追加しただけ（`w.pair`の部分一致）。GO/未更新/カテゴリと同じAND条件で効き、🗺波マップ（`trendMapLiveEntries()`が`trendApplyFilters()`を共有）にもコード変更なしで自動反映される ③波マップの過去日エントリー（`trendMapDayEntries()`）は`items`が`p`（ペア名）しか持たないため、カテゴリ同様に個別で検索条件を追加（`it.p`の部分一致、S28マージ表の設計に合わせた対応）④`renderTrendList()`は`trendListEl`のみを再構築し検索`<input>`自体は作り直さないため、連続入力中もフォーカス・カーソル位置が保たれる ⑤永続化しない（GO・未更新・カテゴリと同じく巡回中の一時的な絞り込み状態）。データモデル・CSV・端末間マージ・統計・並び順ロジック・根拠パネルは無変更。新設`s76-test.mjs`11項目通過（検索欄の存在・大小文字無視・AND条件・空該当時の表示・フォーカス保持・波マップ連動・コンソールエラー無し）、既存s42/s43/s44/s55/s59/s60全通過（205項目）。sw.js: v45→v46。

**S67時点**：セッション67で**🔭一覧の並び順をエントリー圏距離順からアラート発生時刻順に置換、カード連動、圏内・巡回タイルを撤去**。ユーザー指摘「1Hと4Hと日足と週足のソートを作成して。通貨カードのアラート鳴った足のタップと連動させる。そして、圏内と巡回は消して」に対応。①ツールバーに並び順切替ボタン（1H/4H/D/W、`MV_ALERT_TFS`を流用）を新設し、`trendSortTf`（既定`h1`）で選んだ足の`w.alerts[tf].at`が新しい順に一覧を並べる（`alertSortValue()`、アラート無しは末尾）。旧「エントリー圏に近い順」（`pairEntryDistance()`によるソート）は撤去 ②カード上のアラートバッジ（1H/4H/D/W）をタップすると、既存のON/OFFトグル（`mvToggleAlert()`）に加えて`trendSortTf`もタップした足に切り替わり、一覧がその場で並び替わる（ツールバーのボタンとカードのバッジは同じ状態を共有） ③サマリーの「🎯圏内」（エントリー圏の並び順が無くなったため）と「巡回n/mペア」（進捗表示、見る意味が薄れたため）の2タイルを撤去し、GO／未更新の2タイルに整理。一覧のグループ見出し「🎯エントリー圏（n）」・絞り込み「エントリー圏のみ」（`trendEntryOnly`）も撤去。GOグループ・GOのみ絞り込み・未更新絞り込みは無変更 ④`pairEntryDistance()`/`tfEntryDistance()`/`pairIsHot()`/`tfIsHot()`自体は削除せず維持（波マップの圏内強調・🎯根拠ボタンの色分け`trendEntryChipHtml()`で使い続けるため）。データモデル・CSV・端末間マージ・統計・波マップ・根拠パネルは無変更。既存テスト（s44/s59/s60-test.mjs）を現行仕様に更新し全項目通過、s40-test.mjsの波マップ連動テストも「GOのみ」絞り込みに置き換えて通過。sw.js: v37→v38。

**S66時点**：セッション66で**根拠パネルの判定ボタンから絵文字マークを撤去**。ユーザー指摘「記録フォームのボタンの左横の✅⏸️⏩のマークは消してよい」に対応。🔭一覧の根拠パネル footer で「📝 記録フォームへ」ボタンの左に並ぶ判定3ボタン（`trendJudgeBtnsHtml()`）は、S33以来 `j.label.slice(0, 1)` で絵文字1文字（✅/⏸/⏭）だけを表示していたが、絵文字を取り除き文言本体（エントリー/保留/スルー）を表示するように変更。`MV_JUDGES.label` 自体（履歴カードのバッジ・フォームの判定表示・振り返り統計などで使う絵文字付きの完全な文言）は無変更で、`trendJudgeBtnsHtml()` の描画時に正規表現で先頭の絵文字＋空白だけを除去する。ボタンのクリック・判定の保存（`mvSetJudge()`）ロジックは無変更。Playwrightで判定ボタンのテキスト（エントリー/保留/スルー）とクリック後の`.on`クラス付与を確認（コンソールエラーなし）。sw.js: v36→v37。

**S65時点**：セッション65で**エントリー足❸の「重なり」をロールリバーサル確認に置換**。ユーザー指摘「エントリー足の重なりの項目を消して、ロールRvに変更したい。上位足のロールRvは消してよい」に対応。エントリー足の`fiboRoll`（フィボ×ロールリバーサルの重なり、重なり有/重なり無/❌）を撤去し、上位足`MV_TF_CHECKS`にあった`rollReversal`（確認/❌）をエントリー足の❸に一本化。上位足からは`rollReversal`を削除（実運用で5分足での確認と重複していたため）。CSV列は`hi_*`が6→5列（`hi_rollReversal`消滅）、`en_*`は4列のまま（`en_fiboRoll`→`en_rollReversal`）。確度スコアの重み（上位足）はRCI各15・MACD35・ラウンドナンバー10（分母90で計算、100固定ではない）。端末間マージ・波マップ・エントリー圏の距離計算・並び順・統計タブは無変更。Playwrightスモークテストで新definitions・パネルHTML（「重なり」文言が消え「❸ロールRv」が出ること）を確認（コンソールエラーなし）。sw.js: v35→v36。

**S64時点**：セッション64で**確度%を🔭一覧の🎯チップに表示、根拠パネルのエントリー足・合算表示を撤去**。ユーザー指摘「確度のパーセントは通貨カードの🎯の横にも表示させて。エントリー足の確度・合算の表示はいらない」に対応。`trendEntryChipHtml()`の🎯チップに上位足の確度%を常時表示（375px以下では従来通り🎯アイコンのみ、title属性で確度を読める）。根拠パネル内の確度表示（`.tp-conf`）は上位足のみに簡略化し`setupConfidence()`を`higherConfidence()`に置換。sw.js: v34→v35。

**S63時点**：セッション63で**根拠パネルの選択肢整理と確度スコアの重み付け**を実施。ユーザー指摘「グランビルは根拠パネルから無くしてよい、RCI/MACDの⏳待ち・ラウンドナンバーの無・ロールリバーサルの未確認・エントリー足の⏳待ちは不要、各項目左端の空欄（デフォルト選択に見える箇所）も無くしてほしい、上位足の確度はRCI/MACDを厚くラウンドナンバー/ロールリバーサルを薄く」に対応。①`MV_TF_CHECKS`から`granville`行を撤去(🔭一覧の行に🌊アイコンで既出のため重複表示だった。`mvGranvilleSyncField()`と`checksHigher.granville`同期も削除、実波記録`w.trend[tf].granville`は無変更)②RCI×3・MACDの「⏳待ち」系、ラウンドナンバーの「無」、ロールリバーサルの「未確認」、エントリー足❶❷❸の「⏳待ち」をすべて撤去。「待ち」概念がアプリ全体から消えたため`pairWaitCount()`/`trendWaitBadgeHtml()`/サマリー「⏳待ち」タイル/「待ちあり」絞り込みも削除(常に0件で機能しなくなるため)③`trendCheckCell()`の空欄ボタン(`—`、未記録時にデフォルト選択されて見えていた)を撤去。解除は選択済みボタンの再タップで従来通り可能 ④`checkConfidence()`に項目単位の`weight`を導入し、上位足はRCI各15・MACD35・ラウンドナンバー10・ロールリバーサル10(合計100、S65でロールリバーサルを外し分母90に変更)に配分。エントリー足は`weight`なし＝均等割りのまま。CSV列は`hi_*`×7→6に変化(`hi_granville`消滅)。端末間マージ・波マップ・`pairEntryDistance()`・並び順・統計タブは無変更。Playwrightスモークテストで新definitions・重み計算・パネルHTMLを確認(コンソールエラーなし)。sw.js: v33→v34。

**S62時点**：セッション62で**エントリー足の根拠を3ステップ確認フローに全面置換**。ユーザー指摘「エントリー足の選択項目は既存の項目を使わず、上位足の確度が高いことを前提にした①ネックライン付近の反転形→②5分足MA抜け→③1波のフィボ×ロールリバーサルの重なり、という確認の流れで記録したい」に対応。①エントリー足専用の定義 `MV_ENTRY_CHECKS`（`necklineForm`／`maBreak`／`fiboRoll`／`entryFibo` の4項目）を新設し、上位足の `MV_TF_CHECKS` からは `entryFibo` を外して7項目に ②`opts` に `side` を持たせ、上位足の方向（`w.trend[tfKey].state`）に合う選択肢だけを出すようにした（↗なら「ダブルボトム／逆三尊」「上抜け」だけ。方向未記録・レンジでは両サイド）。目線は見出しの `.tp-side` バッジに常時表示 ③方向を反転・レンジ化したときに向きの合わない記録だけを落とす `mvSyncEntrySideChecks()` を新設し、`mvWriteTrend()` と `mvSetTf('tfHigher')` から呼ぶ（グランビル・`wpos` の後始末を1箇所に寄せてあるのと同じ規約）④パネルは項目数が違うため横並び2列をやめ、「上位足セクション（7行）→ エントリー足セクション（4行）」の縦2段に。1項目が横幅をフルに使えるので選択肢の折り返しはむしろ減った（375px 実測で各行1〜2段）⑤区分ごとに定義が分かれたことに追随させたのは5箇所：`checkConfidence(checks, list)`（分母が上位足7・エントリー足4）／`pairWaitCount()`／`basisItemsOf(checks, list)`／`CSV_BASIS_COLUMNS`（`MV_CHECK_SNAPSHOTS[].checks` から生成）／`trendPanelHtml()` の `rowsOf()` ⑥`entryFibo` のキー名は据え置き（CSV列 `en_entryFibo` と📚の「エントリーFibo水準別の成績」が依存）。CSV列は `hi_*`×7／`en_*`×4 に入れ替わり、旧列は import 時に未知の列として無視される。端末間マージ・波マップ・エントリー圏の距離計算（`pairEntryDistance()`）・並び順・統計タブは無変更。新設`s62-test.mjs` 48項目通過、s44 64項目・s43 35項目・s42 45項目・s60 17項目・s59 31項目・s55 15項目 全通過（s44/s40 の旧ツールバーid依存はこのセッションで `[data-trend-summary-filter]` へ移行済み）。sw.js: v32→v33。

セッション60時点：**GO/圏内/待ち/未更新のタップ箇所重複を解消**。ユーザー指摘「サマリータイルとツールバーボタンで同じラベルが2箇所に並んでいてどちらを押せばいいか分からない（スクリーンショット添付）」に対応。①サマリーの🚩GO／🎯圏内／⏳待ち／🕐未更新の4タイルを`<span>`から`<button>`化し、`data-trend-summary-filter="go|hot|wait|stale"`でフィルタそのものを兼務させた（クリックで`trendGoOnly`/`trendEntryOnly`/`trendWaitOnly`/`trendStaleOnly`をトグル）②ツールバー1行目にあった同名4ボタン（`#trendFilterGo`/`#trendFilterAligned`/`#trendFilterWait`/`#trendFilterStale`）をDOMごと削除し、ツールバーは「カテゴリ選択・表示する足・🗺波マップ」の1行に統合（S59で分けた2行を再統合）③サマリータイルの選択中表示はCSS `.trend-summary button.item.on { outline: 2px solid currentColor; }` で追加。既存の色分け（`.item.go/.hot/.wait/.stale`）はそのまま流用し、選択中だけ枠を強調する設計 ④クリックハンドラは`trendSummaryEl`への1つのイベント委任に統合し、旧・4つの個別`addEventListener`を削除。`renderTrendList()`内の`classList.toggle('on', ...)`4行も、`renderTrendSummary()`側で`on`フラグから直接クラスを出すように移管したため不要になり削除。巡回タイル（進捗n/mペア）はフィルタ手段が無いため従来通り`<span>`のまま。データモデル・CSV・端末間マージ・統計・並び順ロジック（`pairEntryDistance()`）はすべて無変更。新設`s60-test.mjs`17項目通過（タイルのボタン化・旧ツールバーボタンの不在・フィルタON/OFFのトグル・outline表示・1行化の確認）、`s59-test.mjs`のツールバー2行前提だった④⑤⑥をS60仕様に更新し31項目通過、s55 15項目通過。sw.js: v32のまま（キャッシュ無効化不要、CSSクラス名・DOM構造の変更のみでJSロジックの実質的な副作用なし）。

セッション59時点：**🔭一覧タブ上部（説明文・サマリー・ツールバー）を巡回の実運用に合わせて整理**。ユーザー指摘「一覧タブ上部の文言とソート用の項目を今に最適な形で整理したい」に対応。①説明文をS24〜S58の継ぎ足しで5話題・306文字に肥大していたのを常時2行に圧縮し、波タップ手順・GOの仕様・波マップへの言及は`<details>`の折りたたみへ退避（初回だけ読む使い方なので毎回のスクロールから外した）②サマリーを「🎯圏内2ペア／↗上昇6／↘下降2／─レンジ1／未記録75／🕐未更新25ペア」（ペア単位と足単位が同じピル形状で混在）から**5タイル全てペア単位**（🚩GO／🎯圏内／⏳待ち／🕐未更新／巡回n/mペア）に統一。足単位の方向カウントは各行のトグル色で常時見えており集計の価値が薄いため撤去、代わりに「巡回n/mペア」で今日どこまで見終えたかの進捗を追加 ③【バグ修正】サマリーのCSS配色クラス`.item.rd/.go/.wait`にS44以降JSが出す`.item.hot/.stale`が定義されておらず、圏内・未更新タイルが無色の灰色ピルのままだった（S58で撤去済みの目線3択の残骸`.rd`も削除）④ツールバーを「絞り込み（GOのみ→エントリー圏のみ→待ちあり→未更新のみ→カテゴリ）」と「表示設定・波マップ」の2行に役割分離し、`trendApplyFilters()`の適用順もツールバー表示順に揃えた（AND条件なので結果は不変）。DOM id・データモデル・CSV・端末間マージ・統計・並び順ロジック（`pairEntryDistance()`）はすべて無変更。s42/s43/s44/s55 全159項目通過、新設s59-test.mjs 35項目通過（説明文圧縮・折りたたみ・5タイルの単位統一・配色バグ修正の確認・ツールバー2行分割・絞り込み順入替後のAND結果一致）。sw.js: v31→v32。

セッション58時点：**🔭一覧カードの「目線」3択（📈/〰/🔄）を撤去し、手動GOフラグに置換**。ペア名の直後の同じ位置にGOボタンを新設し、エントリー圏の自動距離とは独立に「近々来る」と自分で判断したペアへ1タップで印を付けられるようにした。GOを付けたペアは一覧最上位の「🚩 GO」グループへ移動し、再タップで解除するまで自動では消えない。ツールバーの目線フィルタ（プルダウン）は「🚩 GOのみ」ボタンに置換。セッション57で**🔭一覧の巡回対象に1時間足（1H）を追加**し、表示順を1H→4H→日足に変更。エントリー圏の距離判定・波マップ・グランビル連動もすべて1Hを含むように拡張。セッション56で**iOS起床トリガー拡充・容量超過リーンモード・resetCloudToLocal復旧関数**を追加し、複数端末同期のロバスト性を強化。セッション55で**🎯トレードタブを廃止し、下部タブバーを🔭一覧／📚振り返り／⚙設定の3タブに再統合**。トレード記録フォーム自体（S54の内容）は削除せず`#tradeModal`としてモーダル化し、🔭一覧タブの根拠パネル「📝 記録フォームへ」・ヘッダ「→」・履歴の「✎ 編集」ボタンから直接開けるようにした。ユーザー指摘「一覧タブだけで完結できるはずなのに、わざわざ別タブへ移動する流れは不要」による設計変更。`openTradeModal()`/`closeTradeModal()`が唯一の開閉口（背景クリック・Escape・✕ボタン・保存成功すべてがここを通る）。`doSave()`/`syncFormFromMarket()`/`pickMarketFields()`/`marketFieldsFromBoard()`/`loadTradeIntoForm()`のロジック・DOM id・データモデル・CSV・端末間マージ・統計はすべて無変更。s42/s43/s44 全141項目通過、新設s55-test.mjs 15項目通過（モーダル開閉・保存・編集・背景クリック・Escape）。sw.js: v27→v28。

セッション54時点：**トレード記録フォームをRR・建玉ブロック・決済モーダル方式から結果タグ・円建て損益方式に全面置換**。実運用フローの変更に伴い、アラート発生日時・時間足の独立記録、結果タグ（REG/BIG/MAX/微益/建値決済/損切/その他）＋円建て損益、建値タッチ、Firebase Storageへのチャート画像添付（fetchのみ・SDK不使用）を新設。旧・建玉ブロック（エントリー価格/損切り価格/損切り根拠）・3分割決済モーダル・合成RR計算・枚目別/損切り根拠別の成績分析はユーザーの明示的な指示によりすべて削除。CSV列・統計タブもこれに合わせて全面刷新。sw.js: v26→v27。

セッション51時点：**🎯タブの環境認識ボード（Step①）を完全廃止**。S33〜S48で🔭一覧の根拠パネルが同じ5フィールドを書ける唯一の入力元として成熟していたため、UIの二重管理だった旧ボードを撤去。ペア削除は🔭一覧の行の🗑ボタンへ統合、記録フォームの「🔭一覧タブへ」導線を新設。データモデル・CSV・端末間マージ・統計はすべて無変更。s42/s43/s44 全141項目通過、実UIフロー（🎯根拠ボタン→判定→記録フォーム）をPlaywrightで実地確認。sw.js: v25→v26。

セッション50時点：🗺波マップの過去日訂正機能を追加。誤った位置・波番号を記録してしまった場合、過去日を表示中に「✏️ 記録を訂正」ボタンでその日の記録を後から修正・削除できる。設計：過去日のレコードを直接書き換えるのではなく、その日の「新しい記録」を新しい id で追記する方式にした理由は、mergeLog が同一 id で先勝ちのため、id を使い回して内容だけ差し替えると他端末に伝播しないから。wmByDay が day ごとに at が最新のものを表示採用する既存仕様を利用するので、既存の同期・保持ロジック（wmPrune・mergeLog）はすべて無変更。s42/s43/s44 全通過、Playwright スポットチェック完了。sw.js: v24→v25。

**S49の機能**: 🗺 波マップに前日比較（薄いドット・軌跡線）の表示ON/OFFトグルを追加。ツールバーの「🕓 前日比較」ボタン（既定ON）で `trendMapShowPrev` を切り替え、OFFのときは `prevDay` を `null` に固定して前日レイヤー計算をスキップする。既存の「今日は比較対象から外す」「絞り込みは散らしより前」等の仕様は不変。s42/s43/s44 全通過。sw.js: v23→v24。

**S47の機能**: グランビル・ピッカーに他時間足の参考重ね表示。ピッカーは開いた1時間足だけをタップで記録するが、上部のトグルボタン（表示中の他の時間足のみ・記録が無い足は disabled）を押すと、その時間足の既存の波位置（タップ済みなら実測・ボタン選択だけなら概算）が波マップと同じ足別配色で図に重なる。読み取り専用機能（`gvRefEntry()` / `gvRefDotHtml()`）なので記録経路・スキーマ・統計は無変更。

セッション46時点：根拠チェックに**「⏳待ち」の選択肢**を追加（RCI短期/中期/長期に「⏳待ち」、MACDに「⏳RD待ち」「⏳HD待ち」）。他の条件が揃っていてもRCI到達待ち・MACD発生待ちのときはエントリー可能性が高まる場面として🔭一覧タブで拾えるように、行ヘッダに⏳バッジ・ツールバーに`⏳待ちあり`絞り込みを追加。確度スコア（`checkConfidence`）は❌と同じ全減点扱い。`MV_TF_CHECKS`の`opts`に足しただけなので**スキーマ・CSV列構成・端末間マージ・統計はすべて無変更**。sw.js: v20→v21。

セッション45時点：🔭一覧の行に出ていた**グランビルの「ミニ波形」表示（`gvMiniSvg()`）を撤去**した。タップ記録した実座標が実アンカー座標と対応しない紛らわしい表示だったため。ラベル（`g.short`）はそのまま。ツールチップだけで区別。s44-test 59項目・s43-test 35項目・s42-test 45項目・s40-test 62項目通過。sw.js: v18→v19。

セッション44時点：**GO / WAIT / NO / RD の自動判定を全廃**し、🔭一覧を**エントリー圏（買②③/売②③）までの距離**を軸に組み替えた。並び順は距離の昇順、圏内の足にだけ 🎯 チップ兼「根拠」ボタンが出て、そこから根拠パネルが開く（押した足がそのまま上位足）。グランビル・ピッカーの8ボタンも撤去し、記録経路は図のタップだけになった。**スキーマ・CSV・端末間同期・統計はすべて無変更**。sw.js: v17→v18。

セッション43時点：🗺 波マップに**エントリー圏**（買②③/売②③のアンカー ±10%）を追加。図の上でタップして記録した位置が圏内のものだけを強調し、凡例の先頭に近い順で並べる／`🎯 エントリー圏のみ` で絞れる。

セッション42時点：🗺 波マップが**日次で記録され**、昨日・一昨日との比較（前日を薄く重ねた軌跡線）と**1銘柄の全時間足同時表示**ができる。グランビル・ピッカーの不反応領域（「売買8つすべて表示」ON のときのバグ）も解消済み。判定・CSV・端末間同期・統計はすべて無変更。JS構文チェック OK、`s42-test.mjs` 46項目・`s40-test.mjs` 65項目すべて通過。

**セッション28-42 の主な成果:**
- S28：データ消失対策＋複数端末同期。Playwright検証83項目すべて通過
- S29：グランビル 3択→8択統一、トレンド一覧→ボード自動反映、双方向移行ボタン新設
- S30：ペア選択「自由入力廃止＋2段カスケードプルダウン化」。既存自由入力ペアは継続表示・保持
- S31：ストレージ診断パネル。複数オリジンアクセスが原因で「1時間で消える」症状を特定・可視化
- S32：セットアップ自動判定（GO/WAIT/NO）。旧 `trendRank()` を置き換え、日足/4Hセットアップを独立判定 → **S44 で全廃**（エントリー圏までの距離に置き換え）
- S33：🔭一覧の根拠パネル。バッジをタップすると上位足が確定して根拠14項目のプルダウンが開き、判定まで入れて記録フォームへ直行できる。書き込み先はボードと同じ5フィールドなのでスキーマ・CSV・同期・統計は無変更（**S44 で入口を「圏内の足の 🎯 根拠ボタン」に変更**）
- S34：根拠パネルの根拠14項目を `<select>` からボタングループ UI に統一。ボード側と同じ視覚表現＆タップ体験。ボタン内でのみ状態を切り替え、パネル全体の再描画を避けてフォーカス・位置保持
- S35：🔭一覧「目線」3択（📈トレンドフォロー / 〰レンジ / 🔄RD逆張り）を追加。巡回で最初に土俵を振り分け → **S58 で撤去**（手動GOフラグに置換）
- S36：目線フィルタ用プルダウン。📈/〰/🔄 で絞り込み可能に。既存フィルタと AND 条件で併用 → **S58 で撤去**（「🚩 GOのみ」ボタンに置換）
- S37：RCI/MACD の選択肢表示名を「上限/60↑/60↓/下限」「RD(転換)/HD(継続)」に変更
- S38：GO の確度スコア（❌の数だけ減点）＆エントリーフィボナッチ記録。バッジに%表示、パネルに内訳表示。エントリー実現性の分析向上に向けて
- S39：📚振り返りタブに「エントリーFibo水準別の成績」タイル追加（23%/38%/50%/61%/78%の勝率・平均RR比較）＆🔭一覧カードに🗑クリアボタン（巡回記録のみリセット、ボード側データ保護）
- S40：グランビルの図を**タップ面**にして波の位置そのものを記録（`wpos`）。最寄りの公式ポジションが自動で波番号になり、🗺 波マップで全通貨の波の位置を1枚で見渡せる。`trend[tf]` に文字列1つを足しただけなので、判定・CSV・端末間同期・統計はすべて無変更
- S41：🎯波位置記録用画像をシンプル版に統合。Python で8点座標を自動検出（PIL + scipy）→ % 座標変換。教科書的な複雑図をモノクロシンプル図に置き換え、タップ精度・見渡し精度・巡回効率向上。WebP圧縮（7.3KB）。スキーマ・判定・CSV・端末間同期・統計は無変更
- S44：**GO/WAIT/NO/RD 判定の全廃**。波の位置がタップで残るようになった今、3段階の離散ラベルは `gvEntryDistance()` の連続量に置き換わった。一覧はエントリー圏に近い順に並び、圏内の足にだけ 🎯 と「根拠」ボタンが出る。ピッカーの8ボタンも撤去し記録経路は図のタップだけに。スキーマ・CSV・端末間同期・統計は無変更
